// GENERATED by scripts/gen-zep.php — DO NOT EDIT.
// Source of truth: the @zep/@zep-construct/@reserved annotations in src/*.h.

namespace AppKit\NS\NSScreen;

class NSScreen
{
    public static function screens() -> array
    {
        var result;

        let result = nsscreen_screens();

        return result;
    }

    public static function mainScreen() -> int
    {
        return nsscreen_main_screen();
    }

    public static function deepestScreen() -> int
    {
        return nsscreen_deepest_screen();
    }

    public static function screensHaveSeparateSpaces() -> bool
    {
        int r;

        let r = nsscreen_screens_have_separate_spaces();

        return r == 1;
    }

    public static function depth(int handle) -> int
    {
        return nsscreen_depth(handle);
    }

    public static function frame(int handle) -> array
    {
        var result;

        let result = nsscreen_frame(handle);

        return result;
    }

    public static function visibleFrame(int handle) -> array
    {
        var result;

        let result = nsscreen_visible_frame(handle);

        return result;
    }

    public static function deviceDescription(int handle) -> int
    {
        return nsscreen_device_description(handle);
    }

    public static function colorSpace(int handle) -> int
    {
        return nsscreen_color_space(handle);
    }

    public static function canRepresentDisplayGamut(int handle, int displayGamut) -> bool
    {
        int r;

        let r = nsscreen_can_represent_display_gamut(handle, displayGamut);

        return r == 1;
    }

    public static function convertRectToBacking(int handle, double x, double y, double width, double height) -> array
    {
        var result;

        let result = nsscreen_convert_rect_to_backing(handle, x, y, width, height);

        return result;
    }

    public static function convertRectFromBacking(int handle, double x, double y, double width, double height) -> array
    {
        var result;

        let result = nsscreen_convert_rect_from_backing(handle, x, y, width, height);

        return result;
    }

    public static function backingAlignedRectOptions(int handle, double x, double y, double width, double height, int options) -> array
    {
        var result;

        let result = nsscreen_backing_aligned_rect_options(handle, x, y, width, height, options);

        return result;
    }

    public static function backingScaleFactor(int handle) -> double
    {
        return nsscreen_backing_scale_factor(handle);
    }

    public static function localizedName(int handle) -> string
    {
        var result;

        let result = nsscreen_localized_name(handle);

        return result;
    }

    public static function safeAreaInsets(int handle) -> array
    {
        var result;

        let result = nsscreen_safe_area_insets(handle);

        return result;
    }

    public static function auxiliaryTopLeftArea(int handle) -> array
    {
        var result;

        let result = nsscreen_auxiliary_top_left_area(handle);

        return result;
    }

    public static function auxiliaryTopRightArea(int handle) -> array
    {
        var result;

        let result = nsscreen_auxiliary_top_right_area(handle);

        return result;
    }

    public static function maximumExtendedDynamicRangeColorComponentValue(int handle) -> double
    {
        return nsscreen_maximum_extended_dynamic_range_color_component_value(handle);
    }

    public static function maximumPotentialExtendedDynamicRangeColorComponentValue(int handle) -> double
    {
        return nsscreen_maximum_potential_extended_dynamic_range_color_component_value(handle);
    }

    public static function maximumReferenceExtendedDynamicRangeColorComponentValue(int handle) -> double
    {
        return nsscreen_maximum_reference_extended_dynamic_range_color_component_value(handle);
    }

    public static function maximumFramesPerSecond(int handle) -> int
    {
        return nsscreen_maximum_frames_per_second(handle);
    }

    public static function minimumRefreshInterval(int handle) -> double
    {
        return nsscreen_minimum_refresh_interval(handle);
    }

    public static function maximumRefreshInterval(int handle) -> double
    {
        return nsscreen_maximum_refresh_interval(handle);
    }

    public static function displayUpdateGranularity(int handle) -> double
    {
        return nsscreen_display_update_granularity(handle);
    }

    public static function lastDisplayUpdateTimestamp(int handle) -> double
    {
        return nsscreen_last_display_update_timestamp(handle);
    }

    public static function displayLinkWithTargetSelector(int handle, int target, string selector) -> int
    {
        return nsscreen_display_link_with_target_selector(handle, target, selector);
    }

    // ---- @reserved (not bound; see src/*.h) ----
    // @reserved @property (readonly) const NSWindowDepth *supportedWindowDepths NS_RETURNS_INNER_POINTER — C depth-list pointer
    // @reserved - (CGFloat)userSpaceScaleFactor API_DEPRECATED — Use -convertRectToBacking: or -backingScaleFactor instead

}
