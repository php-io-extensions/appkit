// GENERATED by scripts/gen-zep.php — DO NOT EDIT.
// Source of truth: the @zep/@zep-construct/@reserved annotations in src/*.h.

namespace AppKit\NS\NSMenu;

class NSMenu
{
    public static function initWithTitle(string title) -> int
    {
        return nsmenu_init_with_title(title);
    }

    public static function title(int handle) -> string
    {
        var result;

        let result = nsmenu_title(handle);

        return result;
    }

    public static function setTitle(int handle, string title) -> void
    {
        nsmenu_set_title(handle, title);
    }

    public static function popUpContextMenuWithEventForView(int menu, int event, int view) -> void
    {
        nsmenu_pop_up_context_menu_with_event_for_view(menu, event, view);
    }

    public static function popUpContextMenuWithEventForViewWithFont(int menu, int event, int view, int font) -> void
    {
        nsmenu_pop_up_context_menu_with_event_for_view_with_font(menu, event, view, font);
    }

    public static function popUpMenuPositioningItemAtLocationInView(int handle, int item, double x, double y, int view) -> bool
    {
        int r;

        let r = nsmenu_pop_up_menu_positioning_item_at_location_in_view(handle, item, x, y, view);

        return r == 1;
    }

    public static function setMenuBarVisible(bool visible) -> void
    {
        nsmenu_set_menu_bar_visible(visible);
    }

    public static function menuBarVisible() -> bool
    {
        int r;

        let r = nsmenu_menu_bar_visible();

        return r == 1;
    }

    public static function supermenu(int handle) -> int
    {
        return nsmenu_supermenu(handle);
    }

    public static function setSupermenu(int handle, int supermenu) -> void
    {
        nsmenu_set_supermenu(handle, supermenu);
    }

    public static function insertItemAtIndex(int handle, int newItem, int index) -> void
    {
        nsmenu_insert_item_at_index(handle, newItem, index);
    }

    public static function addItem(int handle, int newItem) -> void
    {
        nsmenu_add_item(handle, newItem);
    }

    public static function insertItemWithTitleActionKeyEquivalentAtIndex(int handle, string title, var action, string keyEquivalent, int index) -> int
    {
        return nsmenu_insert_item_with_title_action_key_equivalent_at_index(handle, title, action, keyEquivalent, index);
    }

    public static function addItemWithTitleActionKeyEquivalent(int handle, string title, var action, string keyEquivalent) -> int
    {
        return nsmenu_add_item_with_title_action_key_equivalent(handle, title, action, keyEquivalent);
    }

    public static function removeItemAtIndex(int handle, int index) -> void
    {
        nsmenu_remove_item_at_index(handle, index);
    }

    public static function removeItem(int handle, int item) -> void
    {
        nsmenu_remove_item(handle, item);
    }

    public static function setSubmenuForItem(int handle, int menu, int item) -> void
    {
        nsmenu_set_submenu_for_item(handle, menu, item);
    }

    public static function removeAllItems(int handle) -> void
    {
        nsmenu_remove_all_items(handle);
    }

    public static function itemArray(int handle) -> array
    {
        var result;

        let result = nsmenu_item_array(handle);

        return result;
    }

    public static function setItemArray(int handle, array itemArray) -> void
    {
        nsmenu_set_item_array(handle, itemArray);
    }

    public static function numberOfItems(int handle) -> int
    {
        return nsmenu_number_of_items(handle);
    }

    public static function itemAtIndex(int handle, int index) -> int
    {
        return nsmenu_item_at_index(handle, index);
    }

    public static function indexOfItem(int handle, int item) -> int
    {
        return nsmenu_index_of_item(handle, item);
    }

    public static function indexOfItemWithTitle(int handle, string title) -> int
    {
        return nsmenu_index_of_item_with_title(handle, title);
    }

    public static function indexOfItemWithTag(int handle, int tag) -> int
    {
        return nsmenu_index_of_item_with_tag(handle, tag);
    }

    public static function indexOfItemWithRepresentedObject(int handle, int representedObject) -> int
    {
        return nsmenu_index_of_item_with_represented_object(handle, representedObject);
    }

    public static function indexOfItemWithSubmenu(int handle, int submenu) -> int
    {
        return nsmenu_index_of_item_with_submenu(handle, submenu);
    }

    public static function indexOfItemWithTargetAndAction(int handle, int target, var action) -> int
    {
        return nsmenu_index_of_item_with_target_and_action(handle, target, action);
    }

    public static function itemWithTitle(int handle, string title) -> int
    {
        return nsmenu_item_with_title(handle, title);
    }

    public static function itemWithTag(int handle, int tag) -> int
    {
        return nsmenu_item_with_tag(handle, tag);
    }

    public static function autoenablesItems(int handle) -> bool
    {
        int r;

        let r = nsmenu_autoenables_items(handle);

        return r == 1;
    }

    public static function setAutoenablesItems(int handle, bool flag) -> void
    {
        nsmenu_set_autoenables_items(handle, flag);
    }

    public static function update(int handle) -> void
    {
        nsmenu_update(handle);
    }

    public static function performKeyEquivalent(int handle, int event) -> bool
    {
        int r;

        let r = nsmenu_perform_key_equivalent(handle, event);

        return r == 1;
    }

    public static function itemChanged(int handle, int item) -> void
    {
        nsmenu_item_changed(handle, item);
    }

    public static function performActionForItemAtIndex(int handle, int index) -> void
    {
        nsmenu_perform_action_for_item_at_index(handle, index);
    }

    public static function delegate(int handle) -> int
    {
        return nsmenu_delegate(handle);
    }

    public static function setDelegate(int handle, int delegate) -> void
    {
        nsmenu_set_delegate(handle, delegate);
    }

    public static function menuBarHeight(int handle) -> double
    {
        return nsmenu_menu_bar_height(handle);
    }

    public static function cancelTracking(int handle) -> void
    {
        nsmenu_cancel_tracking(handle);
    }

    public static function cancelTrackingWithoutAnimation(int handle) -> void
    {
        nsmenu_cancel_tracking_without_animation(handle);
    }

    public static function highlightedItem(int handle) -> int
    {
        return nsmenu_highlighted_item(handle);
    }

    public static function minimumWidth(int handle) -> double
    {
        return nsmenu_minimum_width(handle);
    }

    public static function setMinimumWidth(int handle, double width) -> void
    {
        nsmenu_set_minimum_width(handle, width);
    }

    public static function size(int handle) -> array
    {
        var result;

        let result = nsmenu_size(handle);

        return result;
    }

    public static function font(int handle) -> int
    {
        return nsmenu_font(handle);
    }

    public static function setFont(int handle, int font) -> void
    {
        nsmenu_set_font(handle, font);
    }

    public static function allowsContextMenuPlugIns(int handle) -> bool
    {
        int r;

        let r = nsmenu_allows_context_menu_plug_ins(handle);

        return r == 1;
    }

    public static function setAllowsContextMenuPlugIns(int handle, bool flag) -> void
    {
        nsmenu_set_allows_context_menu_plug_ins(handle, flag);
    }

    public static function automaticallyInsertsWritingToolsItems(int handle) -> bool
    {
        int r;

        let r = nsmenu_automatically_inserts_writing_tools_items(handle);

        return r == 1;
    }

    public static function setAutomaticallyInsertsWritingToolsItems(int handle, bool flag) -> void
    {
        nsmenu_set_automatically_inserts_writing_tools_items(handle, flag);
    }

    public static function showsStateColumn(int handle) -> bool
    {
        int r;

        let r = nsmenu_shows_state_column(handle);

        return r == 1;
    }

    public static function setShowsStateColumn(int handle, bool flag) -> void
    {
        nsmenu_set_shows_state_column(handle, flag);
    }

    public static function userInterfaceLayoutDirection(int handle) -> int
    {
        return nsmenu_user_interface_layout_direction(handle);
    }

    public static function setUserInterfaceLayoutDirection(int handle, int direction) -> void
    {
        nsmenu_set_user_interface_layout_direction(handle, direction);
    }

    public static function presentationStyle(int handle) -> int
    {
        return nsmenu_presentation_style(handle);
    }

    public static function setPresentationStyle(int handle, int style) -> void
    {
        nsmenu_set_presentation_style(handle, style);
    }

    public static function selectionMode(int handle) -> int
    {
        return nsmenu_selection_mode(handle);
    }

    public static function setSelectionMode(int handle, int mode) -> void
    {
        nsmenu_set_selection_mode(handle, mode);
    }

    public static function selectedItems(int handle) -> array
    {
        var result;

        let result = nsmenu_selected_items(handle);

        return result;
    }

    public static function setSelectedItems(int handle, array selectedItems) -> void
    {
        nsmenu_set_selected_items(handle, selectedItems);
    }

    public static function submenuAction(int handle, int sender) -> void
    {
        nsmenu_submenu_action(handle, sender);
    }

    public static function propertiesToUpdate(int handle) -> int
    {
        return nsmenu_properties_to_update(handle);
    }

    // ---- @reserved (not bound; see src/*.h) ----
    // @reserved - (instancetype)initWithCoder:(NSCoder *)coder NS_DESIGNATED_INITIALIZER
    // @reserved + (instancetype)paletteMenuWithColors:titles:selectionHandler: — block
    // @reserved + (instancetype)paletteMenuWithColors:titles:templateImage:selectionHandler: — block
    // @reserved - (void)setMenuRepresentation:(null_unspecified id)menuRep API_DEPRECATED
    // @reserved - (null_unspecified id)menuRepresentation API_DEPRECATED
    // @reserved - (void)setContextMenuRepresentation:(null_unspecified id)menuRep API_DEPRECATED
    // @reserved - (null_unspecified id)contextMenuRepresentation API_DEPRECATED
    // @reserved - (void)setTearOffMenuRepresentation:(null_unspecified id)menuRep API_DEPRECATED
    // @reserved - (null_unspecified id)tearOffMenuRepresentation API_DEPRECATED
    // @reserved + (null_unspecified NSZone *)menuZone API_DEPRECATED
    // @reserved + (void)setMenuZone:(null_unspecified NSZone *)zone API_DEPRECATED
    // @reserved - (null_unspecified NSMenu *)attachedMenu API_DEPRECATED
    // @reserved - (BOOL)isAttached API_DEPRECATED
    // @reserved - (void)sizeToFit API_DEPRECATED
    // @reserved - (NSPoint)locationForSubmenu:(null_unspecified NSMenu *)submenu API_DEPRECATED
    // @reserved @property BOOL menuChangedMessagesEnabled — getter
    // @reserved @property BOOL menuChangedMessagesEnabled — setter
    // @reserved - (void)helpRequested:(NSEvent *)eventPtr API_DEPRECATED
    // @reserved @property (getter=isTornOff, readonly) BOOL tornOff API_DEPRECATED

}
