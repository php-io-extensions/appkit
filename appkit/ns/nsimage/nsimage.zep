// GENERATED by scripts/gen-zep.php — DO NOT EDIT.
// Source of truth: the @zep/@zep-construct/@reserved annotations in src/*.h.

namespace AppKit\NS\NSImage;

class NSImage
{
    public static function init() -> int
    {
        return nsimage_init();
    }

    public static function imageNamed(string name) -> int
    {
        return nsimage_image_named(name);
    }

    public static function imageWithSystemSymbolNameAccessibilityDescription(string name, var description) -> int
    {
        return nsimage_image_with_system_symbol_name_accessibility_description(name, description);
    }

    public static function imageWithSystemSymbolNameVariableValueAccessibilityDescription(string name, double value, var description) -> int
    {
        return nsimage_image_with_system_symbol_name_variable_value_accessibility_description(name, value, description);
    }

    public static function imageWithSymbolNameVariableValue(string name, double value) -> int
    {
        return nsimage_image_with_symbol_name_variable_value(name, value);
    }

    public static function imageWithSymbolNameBundleVariableValue(string name, int bundle, double value) -> int
    {
        return nsimage_image_with_symbol_name_bundle_variable_value(name, bundle, value);
    }

    public static function initWithSize(double width, double height) -> int
    {
        return nsimage_init_with_size(width, height);
    }

    public static function initWithContentsOfFile(string fileName) -> int
    {
        return nsimage_init_with_contents_of_file(fileName);
    }

    public static function initWithContentsOfURL(int url) -> int
    {
        return nsimage_init_with_contents_of_url(url);
    }

    public static function initByReferencingFile(string fileName) -> int
    {
        return nsimage_init_by_referencing_file(fileName);
    }

    public static function initByReferencingURL(int url) -> int
    {
        return nsimage_init_by_referencing_url(url);
    }

    public static function initWithPasteboard(int pasteboard) -> int
    {
        return nsimage_init_with_pasteboard(pasteboard);
    }

    public static function size(int handle) -> array
    {
        var result;

        let result = nsimage_size(handle);

        return result;
    }

    public static function setSize(int handle, double width, double height) -> void
    {
        nsimage_set_size(handle, width, height);
    }

    public static function setName(int handle, var name) -> bool
    {
        int r;

        let r = nsimage_set_name(handle, name);

        return r == 1;
    }

    public static function name(int handle) -> var
    {
        var result;

        let result = nsimage_name(handle);

        return result;
    }

    public static function backgroundColor(int handle) -> int
    {
        return nsimage_background_color(handle);
    }

    public static function setBackgroundColor(int handle, int backgroundColor) -> void
    {
        nsimage_set_background_color(handle, backgroundColor);
    }

    public static function usesEPSOnResolutionMismatch(int handle) -> bool
    {
        int r;

        let r = nsimage_uses_eps_on_resolution_mismatch(handle);

        return r == 1;
    }

    public static function setUsesEPSOnResolutionMismatch(int handle, bool flag) -> void
    {
        nsimage_set_uses_eps_on_resolution_mismatch(handle, flag);
    }

    public static function prefersColorMatch(int handle) -> bool
    {
        int r;

        let r = nsimage_prefers_color_match(handle);

        return r == 1;
    }

    public static function setPrefersColorMatch(int handle, bool flag) -> void
    {
        nsimage_set_prefers_color_match(handle, flag);
    }

    public static function matchesOnMultipleResolution(int handle) -> bool
    {
        int r;

        let r = nsimage_matches_on_multiple_resolution(handle);

        return r == 1;
    }

    public static function setMatchesOnMultipleResolution(int handle, bool flag) -> void
    {
        nsimage_set_matches_on_multiple_resolution(handle, flag);
    }

    public static function matchesOnlyOnBestFittingAxis(int handle) -> bool
    {
        int r;

        let r = nsimage_matches_only_on_best_fitting_axis(handle);

        return r == 1;
    }

    public static function setMatchesOnlyOnBestFittingAxis(int handle, bool flag) -> void
    {
        nsimage_set_matches_only_on_best_fitting_axis(handle, flag);
    }

    public static function drawAtPointFromRectOperationFraction(int handle, double x, double y, double fromX, double fromY, double fromWidth, double fromHeight, int op, double delta) -> void
    {
        nsimage_draw_at_point_from_rect_operation_fraction(handle, x, y, fromX, fromY, fromWidth, fromHeight, op, delta);
    }

    public static function drawInRectFromRectOperationFraction(int handle, double x, double y, double width, double height, double fromX, double fromY, double fromWidth, double fromHeight, int op, double delta) -> void
    {
        nsimage_draw_in_rect_from_rect_operation_fraction(handle, x, y, width, height, fromX, fromY, fromWidth, fromHeight, op, delta);
    }

    public static function drawInRectFromRectOperationFractionRespectFlippedHints(int handle, double x, double y, double width, double height, double fromX, double fromY, double fromWidth, double fromHeight, int op, double requestedAlpha, bool respectFlipped, var hints) -> void
    {
        nsimage_draw_in_rect_from_rect_operation_fraction_respect_flipped_hints(handle, x, y, width, height, fromX, fromY, fromWidth, fromHeight, op, requestedAlpha, respectFlipped, hints);
    }

    public static function drawRepresentationInRect(int handle, int imageRep, double x, double y, double width, double height) -> bool
    {
        int r;

        let r = nsimage_draw_representation_in_rect(handle, imageRep, x, y, width, height);

        return r == 1;
    }

    public static function drawInRect(int handle, double x, double y, double width, double height) -> void
    {
        nsimage_draw_in_rect(handle, x, y, width, height);
    }

    public static function recache(int handle) -> void
    {
        nsimage_recache(handle);
    }

    public static function representations(int handle) -> array
    {
        var result;

        let result = nsimage_representations(handle);

        return result;
    }

    public static function addRepresentations(int handle, array imageReps) -> void
    {
        nsimage_add_representations(handle, imageReps);
    }

    public static function addRepresentation(int handle, int imageRep) -> void
    {
        nsimage_add_representation(handle, imageRep);
    }

    public static function removeRepresentation(int handle, int imageRep) -> void
    {
        nsimage_remove_representation(handle, imageRep);
    }

    public static function isValid(int handle) -> bool
    {
        int r;

        let r = nsimage_is_valid(handle);

        return r == 1;
    }

    public static function delegate(int handle) -> int
    {
        return nsimage_delegate(handle);
    }

    public static function setDelegate(int handle, int delegate) -> void
    {
        nsimage_set_delegate(handle, delegate);
    }

    public static function imageTypes() -> array
    {
        var result;

        let result = nsimage_image_types();

        return result;
    }

    public static function imageUnfilteredTypes() -> array
    {
        var result;

        let result = nsimage_image_unfiltered_types();

        return result;
    }

    public static function canInitWithPasteboard(int pasteboard) -> bool
    {
        int r;

        let r = nsimage_can_init_with_pasteboard(pasteboard);

        return r == 1;
    }

    public static function cacheMode(int handle) -> int
    {
        return nsimage_cache_mode(handle);
    }

    public static function setCacheMode(int handle, int cacheMode) -> void
    {
        nsimage_set_cache_mode(handle, cacheMode);
    }

    public static function alignmentRect(int handle) -> array
    {
        var result;

        let result = nsimage_alignment_rect(handle);

        return result;
    }

    public static function setAlignmentRect(int handle, double x, double y, double width, double height) -> void
    {
        nsimage_set_alignment_rect(handle, x, y, width, height);
    }

    public static function isTemplate(int handle) -> bool
    {
        int r;

        let r = nsimage_is_template(handle);

        return r == 1;
    }

    public static function setTemplate(int handle, bool flag) -> void
    {
        nsimage_set_template(handle, flag);
    }

    public static function accessibilityDescription(int handle) -> var
    {
        var result;

        let result = nsimage_accessibility_description(handle);

        return result;
    }

    public static function setAccessibilityDescription(int handle, var accessibilityDescription) -> void
    {
        nsimage_set_accessibility_description(handle, accessibilityDescription);
    }

    public static function initWithCGImageSize(int cgImage, double width, double height) -> int
    {
        return nsimage_init_with_cgimage_size(cgImage, width, height);
    }

    public static function bestRepresentationForRectContextHints(int handle, double x, double y, double width, double height, int context, var hints) -> int
    {
        return nsimage_best_representation_for_rect_context_hints(handle, x, y, width, height, context, hints);
    }

    public static function hitTestRectWithImageDestinationRectContextHintsFlipped(int handle, double testX, double testY, double testWidth, double testHeight, double destX, double destY, double destWidth, double destHeight, int context, var hints, bool flipped) -> bool
    {
        int r;

        let r = nsimage_hit_test_rect_with_image_destination_rect_context_hints_flipped(handle, testX, testY, testWidth, testHeight, destX, destY, destWidth, destHeight, context, hints, flipped);

        return r == 1;
    }

    public static function recommendedLayerContentsScale(int handle, double preferredContentsScale) -> double
    {
        return nsimage_recommended_layer_contents_scale(handle, preferredContentsScale);
    }

    public static function layerContentsForContentsScale(int handle, double layerContentsScale) -> int
    {
        return nsimage_layer_contents_for_contents_scale(handle, layerContentsScale);
    }

    public static function capInsets(int handle) -> array
    {
        var result;

        let result = nsimage_cap_insets(handle);

        return result;
    }

    public static function setCapInsets(int handle, double top, double left, double bottom, double right) -> void
    {
        nsimage_set_cap_insets(handle, top, left, bottom, right);
    }

    public static function resizingMode(int handle) -> int
    {
        return nsimage_resizing_mode(handle);
    }

    public static function setResizingMode(int handle, int resizingMode) -> void
    {
        nsimage_set_resizing_mode(handle, resizingMode);
    }

    public static function imageWithSymbolConfiguration(int handle, int configuration) -> int
    {
        return nsimage_image_with_symbol_configuration(handle, configuration);
    }

    public static function symbolConfiguration(int handle) -> int
    {
        return nsimage_symbol_configuration(handle);
    }

    public static function imageWithLocale(int handle, int locale) -> int
    {
        return nsimage_image_with_locale(handle, locale);
    }

    public static function locale(int handle) -> int
    {
        return nsimage_locale(handle);
    }

    // ---- @reserved (not bound; see src/*.h) ----
    // @reserved + (instancetype)new API_UNAVAILABLE(ios);
    // @reserved + (instancetype)allocWithZone:(nullable NSZone *)zone API_UNAVAILABLE(ios);
    // @reserved + (instancetype)alloc API_UNAVAILABLE(ios);
    // @reserved - (instancetype)initWithCoder:(NSCoder *)coder NS_DESIGNATED_INITIALIZER;
    // @reserved - (nullable instancetype)initWithData:(NSData *)data;
    // @reserved - (nullable instancetype)initWithDataIgnoringOrientation:(NSData *)data API_AVAILABLE(macos(10.6));
    // @reserved + (instancetype)imageWithSize:flipped:drawingHandler: — block
    // @reserved @property (nullable, readonly, strong) NSData *TIFFRepresentation — getter
    // @reserved - (nullable NSData *)TIFFRepresentationUsingCompression:(NSTIFFCompression)comp factor:(float)factor;
    // @reserved @property (getter=isTemplate, setter=setTemplate:) BOOL _template — C++/Swift duplicate getter
    // @reserved @property (getter=isTemplate, setter=setTemplate:) BOOL _template — C++/Swift duplicate setter
    // @reserved - (BOOL)isTemplate — C++ duplicate of the property getter
    // @reserved - (void)setTemplate:(BOOL)isTemplate — C++ duplicate of the property setter
    // @reserved - (nullable CGImageRef)CGImageForProposedRect:(nullable NSRect *)proposedDestRect context:(nullable NSGraphicsContext *)referenceContext hints:(nullable NSDictionary<NSImageHintKey, id> *)hints — CGImageRef plus NSRect * inout
    // @reserved + (NSArray<NSString *> *)imageUnfilteredFileTypes API_DEPRECATED
    // @reserved + (NSArray<NSPasteboardType> *)imageUnfilteredPasteboardTypes API_DEPRECATED
    // @reserved + (NSArray<NSString *> *)imageFileTypes API_DEPRECATED
    // @reserved + (NSArray<NSPasteboardType> *)imagePasteboardTypes API_DEPRECATED
    // @reserved - (instancetype)initWithIconRef:(IconRef)iconRef API_DEPRECATED
    // @reserved - (null_unspecified NSImageRep *)bestRepresentationForDevice:(null_unspecified NSDictionary *)deviceDescription API_DEPRECATED
    // @reserved - (void)lockFocus API_DEPRECATED
    // @reserved - (void)lockFocusFlipped:(BOOL)flipped API_DEPRECATED
    // @reserved - (void)unlockFocus API_DEPRECATED
    // @reserved - (void)setFlipped:(BOOL)flag API_DEPRECATED
    // @reserved - (BOOL)isFlipped API_DEPRECATED
    // @reserved - (void)setScalesWhenResized:(BOOL)flag API_DEPRECATED
    // @reserved - (BOOL)scalesWhenResized API_DEPRECATED
    // @reserved - (void)setDataRetained:(BOOL)flag API_DEPRECATED
    // @reserved - (BOOL)isDataRetained API_DEPRECATED
    // @reserved - (void)setCachedSeparately:(BOOL)flag API_DEPRECATED
    // @reserved - (BOOL)isCachedSeparately API_DEPRECATED
    // @reserved - (void)setCacheDepthMatchesImageDepth:(BOOL)flag API_DEPRECATED
    // @reserved - (BOOL)cacheDepthMatchesImageDepth API_DEPRECATED
    // @reserved - (void)dissolveToPoint:(NSPoint)point fraction:(CGFloat)fraction API_DEPRECATED
    // @reserved - (void)dissolveToPoint:(NSPoint)point fromRect:(NSRect)rect fraction:(CGFloat)fraction API_DEPRECATED
    // @reserved - (void)compositeToPoint:(NSPoint)point operation:(NSCompositingOperation)operation API_DEPRECATED
    // @reserved - (void)compositeToPoint:(NSPoint)point fromRect:(NSRect)rect operation:(NSCompositingOperation)operation API_DEPRECATED
    // @reserved - (void)compositeToPoint:(NSPoint)point operation:(NSCompositingOperation)operation fraction:(CGFloat)fraction API_DEPRECATED
    // @reserved - (void)compositeToPoint:(NSPoint)point fromRect:(NSRect)rect operation:(NSCompositingOperation)operation fraction:(CGFloat)fraction API_DEPRECATED
    // @reserved - (void)lockFocusOnRepresentation:(null_unspecified NSImageRep *)imageRepresentation API_DEPRECATED
    // @reserved - (void)cancelIncrementalLoad API_DEPRECATED

}
