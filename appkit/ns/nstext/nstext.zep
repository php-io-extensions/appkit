// GENERATED by scripts/gen-zep.php — DO NOT EDIT.
// Source of truth: the @zep/@zep-construct/@reserved annotations in src/*.h.

namespace AppKit\NS\NSText;

class NSText
{
    public static function initWithFrame(double x, double y, double width, double height) -> int
    {
        return nstext_init_with_frame(x, y, width, height);
    }

    public static function string_(int handle) -> string
    {
        var result;

        let result = nstext_string_(handle);

        return result;
    }

    public static function setString(int handle, string string_) -> void
    {
        nstext_set_string(handle, string_);
    }

    public static function replaceCharactersInRangeWithString(int handle, int location, int length, string string_) -> void
    {
        nstext_replace_characters_in_range_with_string(handle, location, length, string_);
    }

    public static function writeRTFDToFileAtomically(int handle, string path, bool flag) -> bool
    {
        int r;

        let r = nstext_write_rtfd_to_file_atomically(handle, path, flag);

        return r == 1;
    }

    public static function readRTFDFromFile(int handle, string path) -> bool
    {
        int r;

        let r = nstext_read_rtfd_from_file(handle, path);

        return r == 1;
    }

    public static function delegate(int handle) -> int
    {
        return nstext_delegate(handle);
    }

    public static function setDelegate(int handle, int delegate) -> void
    {
        nstext_set_delegate(handle, delegate);
    }

    public static function isEditable(int handle) -> bool
    {
        int r;

        let r = nstext_is_editable(handle);

        return r == 1;
    }

    public static function setEditable(int handle, bool editable) -> void
    {
        nstext_set_editable(handle, editable);
    }

    public static function isSelectable(int handle) -> bool
    {
        int r;

        let r = nstext_is_selectable(handle);

        return r == 1;
    }

    public static function setSelectable(int handle, bool selectable) -> void
    {
        nstext_set_selectable(handle, selectable);
    }

    public static function isRichText(int handle) -> bool
    {
        int r;

        let r = nstext_is_rich_text(handle);

        return r == 1;
    }

    public static function setRichText(int handle, bool richText) -> void
    {
        nstext_set_rich_text(handle, richText);
    }

    public static function importsGraphics(int handle) -> bool
    {
        int r;

        let r = nstext_imports_graphics(handle);

        return r == 1;
    }

    public static function setImportsGraphics(int handle, bool importsGraphics) -> void
    {
        nstext_set_imports_graphics(handle, importsGraphics);
    }

    public static function isFieldEditor(int handle) -> bool
    {
        int r;

        let r = nstext_is_field_editor(handle);

        return r == 1;
    }

    public static function setFieldEditor(int handle, bool fieldEditor) -> void
    {
        nstext_set_field_editor(handle, fieldEditor);
    }

    public static function usesFontPanel(int handle) -> bool
    {
        int r;

        let r = nstext_uses_font_panel(handle);

        return r == 1;
    }

    public static function setUsesFontPanel(int handle, bool usesFontPanel) -> void
    {
        nstext_set_uses_font_panel(handle, usesFontPanel);
    }

    public static function drawsBackground(int handle) -> bool
    {
        int r;

        let r = nstext_draws_background(handle);

        return r == 1;
    }

    public static function setDrawsBackground(int handle, bool drawsBackground) -> void
    {
        nstext_set_draws_background(handle, drawsBackground);
    }

    public static function backgroundColor(int handle) -> int
    {
        return nstext_background_color(handle);
    }

    public static function setBackgroundColor(int handle, int backgroundColor) -> void
    {
        nstext_set_background_color(handle, backgroundColor);
    }

    public static function isRulerVisible(int handle) -> bool
    {
        int r;

        let r = nstext_is_ruler_visible(handle);

        return r == 1;
    }

    public static function selectedRange(int handle) -> array
    {
        var result;

        let result = nstext_selected_range(handle);

        return result;
    }

    public static function setSelectedRange(int handle, int location, int length) -> void
    {
        nstext_set_selected_range(handle, location, length);
    }

    public static function scrollRangeToVisible(int handle, int location, int length) -> void
    {
        nstext_scroll_range_to_visible(handle, location, length);
    }

    public static function font(int handle) -> int
    {
        return nstext_font(handle);
    }

    public static function setFont(int handle, int font) -> void
    {
        nstext_set_font(handle, font);
    }

    public static function textColor(int handle) -> int
    {
        return nstext_text_color(handle);
    }

    public static function setTextColor(int handle, int textColor) -> void
    {
        nstext_set_text_color(handle, textColor);
    }

    public static function alignment(int handle) -> int
    {
        return nstext_alignment(handle);
    }

    public static function setAlignment(int handle, int alignment) -> void
    {
        nstext_set_alignment(handle, alignment);
    }

    public static function baseWritingDirection(int handle) -> int
    {
        return nstext_base_writing_direction(handle);
    }

    public static function setBaseWritingDirection(int handle, int baseWritingDirection) -> void
    {
        nstext_set_base_writing_direction(handle, baseWritingDirection);
    }

    public static function setTextColorRange(int handle, int color, int location, int length) -> void
    {
        nstext_set_text_color_range(handle, color, location, length);
    }

    public static function setFontRange(int handle, int font, int location, int length) -> void
    {
        nstext_set_font_range(handle, font, location, length);
    }

    public static function maxSize(int handle) -> array
    {
        var result;

        let result = nstext_max_size(handle);

        return result;
    }

    public static function setMaxSize(int handle, double width, double height) -> void
    {
        nstext_set_max_size(handle, width, height);
    }

    public static function minSize(int handle) -> array
    {
        var result;

        let result = nstext_min_size(handle);

        return result;
    }

    public static function setMinSize(int handle, double width, double height) -> void
    {
        nstext_set_min_size(handle, width, height);
    }

    public static function isHorizontallyResizable(int handle) -> bool
    {
        int r;

        let r = nstext_is_horizontally_resizable(handle);

        return r == 1;
    }

    public static function setHorizontallyResizable(int handle, bool horizontallyResizable) -> void
    {
        nstext_set_horizontally_resizable(handle, horizontallyResizable);
    }

    public static function isVerticallyResizable(int handle) -> bool
    {
        int r;

        let r = nstext_is_vertically_resizable(handle);

        return r == 1;
    }

    public static function setVerticallyResizable(int handle, bool verticallyResizable) -> void
    {
        nstext_set_vertically_resizable(handle, verticallyResizable);
    }

    public static function sizeToFit(int handle) -> void
    {
        nstext_size_to_fit(handle);
    }

    public static function copy_(int handle, int sender) -> void
    {
        nstext_copy_(handle, sender);
    }

    public static function copyFont(int handle, int sender) -> void
    {
        nstext_copy_font(handle, sender);
    }

    public static function copyRuler(int handle, int sender) -> void
    {
        nstext_copy_ruler(handle, sender);
    }

    public static function cut(int handle, int sender) -> void
    {
        nstext_cut(handle, sender);
    }

    public static function delete(int handle, int sender) -> void
    {
        nstext_delete(handle, sender);
    }

    public static function paste(int handle, int sender) -> void
    {
        nstext_paste(handle, sender);
    }

    public static function pasteFont(int handle, int sender) -> void
    {
        nstext_paste_font(handle, sender);
    }

    public static function pasteRuler(int handle, int sender) -> void
    {
        nstext_paste_ruler(handle, sender);
    }

    public static function selectAll(int handle, int sender) -> void
    {
        nstext_select_all(handle, sender);
    }

    public static function changeFont(int handle, int sender) -> void
    {
        nstext_change_font(handle, sender);
    }

    public static function alignLeft(int handle, int sender) -> void
    {
        nstext_align_left(handle, sender);
    }

    public static function alignRight(int handle, int sender) -> void
    {
        nstext_align_right(handle, sender);
    }

    public static function alignCenter(int handle, int sender) -> void
    {
        nstext_align_center(handle, sender);
    }

    public static function subscript(int handle, int sender) -> void
    {
        nstext_subscript(handle, sender);
    }

    public static function superscript(int handle, int sender) -> void
    {
        nstext_superscript(handle, sender);
    }

    public static function underline(int handle, int sender) -> void
    {
        nstext_underline(handle, sender);
    }

    public static function unscript(int handle, int sender) -> void
    {
        nstext_unscript(handle, sender);
    }

    public static function showGuessPanel(int handle, int sender) -> void
    {
        nstext_show_guess_panel(handle, sender);
    }

    public static function checkSpelling(int handle, int sender) -> void
    {
        nstext_check_spelling(handle, sender);
    }

    public static function toggleRuler(int handle, int sender) -> void
    {
        nstext_toggle_ruler(handle, sender);
    }

    // ---- @reserved (not bound; see src/*.h) ----
    // @reserved - (nullable instancetype)initWithCoder:(NSCoder *)coder NS_DESIGNATED_INITIALIZER;
    // @reserved - (void)replaceCharactersInRange:(NSRange)range withRTF:(NSData *)rtfData;
    // @reserved - (void)replaceCharactersInRange:(NSRange)range withRTFD:(NSData *)rtfdData;
    // @reserved - (nullable NSData *)RTFFromRange:(NSRange)range;
    // @reserved - (nullable NSData *)RTFDFromRange:(NSRange)range;

}
