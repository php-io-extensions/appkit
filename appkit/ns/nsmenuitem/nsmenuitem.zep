// GENERATED by scripts/gen-zep.php — DO NOT EDIT.
// Source of truth: the @zep/@zep-construct/@reserved annotations in src/*.h.

namespace AppKit\NS\NSMenuItem;

class NSMenuItem
{
    public static function usesUserKeyEquivalents() -> bool
    {
        int r;

        let r = nsmenuitem_uses_user_key_equivalents();

        return r == 1;
    }

    public static function setUsesUserKeyEquivalents(bool flag) -> void
    {
        nsmenuitem_set_uses_user_key_equivalents(flag);
    }

    public static function separatorItem() -> int
    {
        return nsmenuitem_separator_item();
    }

    public static function sectionHeaderWithTitle(string title) -> int
    {
        return nsmenuitem_section_header_with_title(title);
    }

    public static function writingToolsItems() -> array
    {
        var result;

        let result = nsmenuitem_writing_tools_items();

        return result;
    }

    public static function initWithTitleActionKeyEquivalent(string title, string action, string keyEquivalent) -> int
    {
        return nsmenuitem_init_with_title_action_key_equivalent(title, action, keyEquivalent);
    }

    public static function menu(int handle) -> int
    {
        return nsmenuitem_menu(handle);
    }

    public static function setMenu(int handle, int menu) -> void
    {
        nsmenuitem_set_menu(handle, menu);
    }

    public static function hasSubmenu(int handle) -> bool
    {
        int r;

        let r = nsmenuitem_has_submenu(handle);

        return r == 1;
    }

    public static function submenu(int handle) -> int
    {
        return nsmenuitem_submenu(handle);
    }

    public static function setSubmenu(int handle, int submenu) -> void
    {
        nsmenuitem_set_submenu(handle, submenu);
    }

    public static function parentItem(int handle) -> int
    {
        return nsmenuitem_parent_item(handle);
    }

    public static function title(int handle) -> string
    {
        var result;

        let result = nsmenuitem_title(handle);

        return result;
    }

    public static function setTitle(int handle, string title) -> void
    {
        nsmenuitem_set_title(handle, title);
    }

    public static function subtitle(int handle) -> var
    {
        var result;

        let result = nsmenuitem_subtitle(handle);

        return result;
    }

    public static function setSubtitle(int handle, var subtitle) -> void
    {
        nsmenuitem_set_subtitle(handle, subtitle);
    }

    public static function isSeparatorItem(int handle) -> bool
    {
        int r;

        let r = nsmenuitem_is_separator_item(handle);

        return r == 1;
    }

    public static function isSectionHeader(int handle) -> bool
    {
        int r;

        let r = nsmenuitem_is_section_header(handle);

        return r == 1;
    }

    public static function keyEquivalent(int handle) -> string
    {
        var result;

        let result = nsmenuitem_key_equivalent(handle);

        return result;
    }

    public static function setKeyEquivalent(int handle, string keyEquivalent) -> void
    {
        nsmenuitem_set_key_equivalent(handle, keyEquivalent);
    }

    public static function keyEquivalentModifierMask(int handle) -> int
    {
        return nsmenuitem_key_equivalent_modifier_mask(handle);
    }

    public static function setKeyEquivalentModifierMask(int handle, int mask) -> void
    {
        nsmenuitem_set_key_equivalent_modifier_mask(handle, mask);
    }

    public static function userKeyEquivalent(int handle) -> string
    {
        var result;

        let result = nsmenuitem_user_key_equivalent(handle);

        return result;
    }

    public static function allowsKeyEquivalentWhenHidden(int handle) -> bool
    {
        int r;

        let r = nsmenuitem_allows_key_equivalent_when_hidden(handle);

        return r == 1;
    }

    public static function setAllowsKeyEquivalentWhenHidden(int handle, bool flag) -> void
    {
        nsmenuitem_set_allows_key_equivalent_when_hidden(handle, flag);
    }

    public static function allowsAutomaticKeyEquivalentLocalization(int handle) -> bool
    {
        int r;

        let r = nsmenuitem_allows_automatic_key_equivalent_localization(handle);

        return r == 1;
    }

    public static function setAllowsAutomaticKeyEquivalentLocalization(int handle, bool flag) -> void
    {
        nsmenuitem_set_allows_automatic_key_equivalent_localization(handle, flag);
    }

    public static function allowsAutomaticKeyEquivalentMirroring(int handle) -> bool
    {
        int r;

        let r = nsmenuitem_allows_automatic_key_equivalent_mirroring(handle);

        return r == 1;
    }

    public static function setAllowsAutomaticKeyEquivalentMirroring(int handle, bool flag) -> void
    {
        nsmenuitem_set_allows_automatic_key_equivalent_mirroring(handle, flag);
    }

    public static function image(int handle) -> int
    {
        return nsmenuitem_image(handle);
    }

    public static function setImage(int handle, int image) -> void
    {
        nsmenuitem_set_image(handle, image);
    }

    public static function state(int handle) -> int
    {
        return nsmenuitem_state(handle);
    }

    public static function setState(int handle, int state) -> void
    {
        nsmenuitem_set_state(handle, state);
    }

    public static function onStateImage(int handle) -> int
    {
        return nsmenuitem_on_state_image(handle);
    }

    public static function setOnStateImage(int handle, int image) -> void
    {
        nsmenuitem_set_on_state_image(handle, image);
    }

    public static function offStateImage(int handle) -> int
    {
        return nsmenuitem_off_state_image(handle);
    }

    public static function setOffStateImage(int handle, int image) -> void
    {
        nsmenuitem_set_off_state_image(handle, image);
    }

    public static function mixedStateImage(int handle) -> int
    {
        return nsmenuitem_mixed_state_image(handle);
    }

    public static function setMixedStateImage(int handle, int image) -> void
    {
        nsmenuitem_set_mixed_state_image(handle, image);
    }

    public static function isEnabled(int handle) -> bool
    {
        int r;

        let r = nsmenuitem_is_enabled(handle);

        return r == 1;
    }

    public static function setEnabled(int handle, bool flag) -> void
    {
        nsmenuitem_set_enabled(handle, flag);
    }

    public static function isAlternate(int handle) -> bool
    {
        int r;

        let r = nsmenuitem_is_alternate(handle);

        return r == 1;
    }

    public static function setAlternate(int handle, bool flag) -> void
    {
        nsmenuitem_set_alternate(handle, flag);
    }

    public static function indentationLevel(int handle) -> int
    {
        return nsmenuitem_indentation_level(handle);
    }

    public static function setIndentationLevel(int handle, int level) -> void
    {
        nsmenuitem_set_indentation_level(handle, level);
    }

    public static function target(int handle) -> int
    {
        return nsmenuitem_target(handle);
    }

    public static function setTarget(int handle, int target) -> void
    {
        nsmenuitem_set_target(handle, target);
    }

    public static function action(int handle) -> var
    {
        var result;

        let result = nsmenuitem_action(handle);

        return result;
    }

    public static function setAction(int handle, var action) -> void
    {
        nsmenuitem_set_action(handle, action);
    }

    public static function tag(int handle) -> int
    {
        return nsmenuitem_tag(handle);
    }

    public static function setTag(int handle, int tag) -> void
    {
        nsmenuitem_set_tag(handle, tag);
    }

    public static function representedObject(int handle) -> int
    {
        return nsmenuitem_represented_object(handle);
    }

    public static function setRepresentedObject(int handle, int representedObject) -> void
    {
        nsmenuitem_set_represented_object(handle, representedObject);
    }

    public static function view(int handle) -> int
    {
        return nsmenuitem_view(handle);
    }

    public static function setView(int handle, int view) -> void
    {
        nsmenuitem_set_view(handle, view);
    }

    public static function isHighlighted(int handle) -> bool
    {
        int r;

        let r = nsmenuitem_is_highlighted(handle);

        return r == 1;
    }

    public static function isHidden(int handle) -> bool
    {
        int r;

        let r = nsmenuitem_is_hidden(handle);

        return r == 1;
    }

    public static function setHidden(int handle, bool flag) -> void
    {
        nsmenuitem_set_hidden(handle, flag);
    }

    public static function isHiddenOrHasHiddenAncestor(int handle) -> bool
    {
        int r;

        let r = nsmenuitem_is_hidden_or_has_hidden_ancestor(handle);

        return r == 1;
    }

    public static function toolTip(int handle) -> var
    {
        var result;

        let result = nsmenuitem_tool_tip(handle);

        return result;
    }

    public static function setToolTip(int handle, var toolTip) -> void
    {
        nsmenuitem_set_tool_tip(handle, toolTip);
    }

    public static function badge(int handle) -> int
    {
        return nsmenuitem_badge(handle);
    }

    public static function setBadge(int handle, int badge) -> void
    {
        nsmenuitem_set_badge(handle, badge);
    }

    // ---- @reserved (not bound; see src/*.h) ----
    // @reserved - (instancetype)initWithCoder:(NSCoder *)coder NS_DESIGNATED_INITIALIZER
    // @reserved @property (nullable, copy) NSAttributedString *attributedTitle — getter
    // @reserved @property (nullable, copy) NSAttributedString *attributedTitle — setter
    // @reserved - (void)setMnemonicLocation:(NSUInteger)location API_DEPRECATED
    // @reserved - (NSUInteger)mnemonicLocation API_DEPRECATED
    // @reserved - (null_unspecified NSString *)mnemonic API_DEPRECATED
    // @reserved - (void)setTitleWithMnemonic:(NSString *)stringWithAmpersand API_DEPRECATED

}
