// GENERATED by scripts/gen-zep.php — DO NOT EDIT.
// Source of truth: the @zep/@zep-construct/@reserved annotations in src/*.h.

namespace AppKit\NS\NSLayoutManager;

class NSLayoutManager
{
    public static function init() -> int
    {
        return nslayoutmanager_init();
    }

    public static function textStorage(int handle) -> int
    {
        return nslayoutmanager_text_storage(handle);
    }

    public static function setTextStorage(int handle, int textStorage) -> void
    {
        nslayoutmanager_set_text_storage(handle, textStorage);
    }

    public static function replaceTextStorage(int handle, int newTextStorage) -> void
    {
        nslayoutmanager_replace_text_storage(handle, newTextStorage);
    }

    public static function textContainers(int handle) -> array
    {
        var result;

        let result = nslayoutmanager_text_containers(handle);

        return result;
    }

    public static function addTextContainer(int handle, int container) -> void
    {
        nslayoutmanager_add_text_container(handle, container);
    }

    public static function insertTextContainerAtIndex(int handle, int container, int index) -> void
    {
        nslayoutmanager_insert_text_container_at_index(handle, container, index);
    }

    public static function removeTextContainerAtIndex(int handle, int index) -> void
    {
        nslayoutmanager_remove_text_container_at_index(handle, index);
    }

    public static function textContainerChangedGeometry(int handle, int container) -> void
    {
        nslayoutmanager_text_container_changed_geometry(handle, container);
    }

    public static function textContainerChangedTextView(int handle, int container) -> void
    {
        nslayoutmanager_text_container_changed_text_view(handle, container);
    }

    public static function delegate(int handle) -> int
    {
        return nslayoutmanager_delegate(handle);
    }

    public static function setDelegate(int handle, int delegate) -> void
    {
        nslayoutmanager_set_delegate(handle, delegate);
    }

    public static function showsInvisibleCharacters(int handle) -> bool
    {
        int r;

        let r = nslayoutmanager_shows_invisible_characters(handle);

        return r == 1;
    }

    public static function setShowsInvisibleCharacters(int handle, bool showsInvisibleCharacters) -> void
    {
        nslayoutmanager_set_shows_invisible_characters(handle, showsInvisibleCharacters);
    }

    public static function showsControlCharacters(int handle) -> bool
    {
        int r;

        let r = nslayoutmanager_shows_control_characters(handle);

        return r == 1;
    }

    public static function setShowsControlCharacters(int handle, bool showsControlCharacters) -> void
    {
        nslayoutmanager_set_shows_control_characters(handle, showsControlCharacters);
    }

    public static function usesDefaultHyphenation(int handle) -> bool
    {
        int r;

        let r = nslayoutmanager_uses_default_hyphenation(handle);

        return r == 1;
    }

    public static function setUsesDefaultHyphenation(int handle, bool usesDefaultHyphenation) -> void
    {
        nslayoutmanager_set_uses_default_hyphenation(handle, usesDefaultHyphenation);
    }

    public static function usesFontLeading(int handle) -> bool
    {
        int r;

        let r = nslayoutmanager_uses_font_leading(handle);

        return r == 1;
    }

    public static function setUsesFontLeading(int handle, bool usesFontLeading) -> void
    {
        nslayoutmanager_set_uses_font_leading(handle, usesFontLeading);
    }

    public static function allowsNonContiguousLayout(int handle) -> bool
    {
        int r;

        let r = nslayoutmanager_allows_non_contiguous_layout(handle);

        return r == 1;
    }

    public static function setAllowsNonContiguousLayout(int handle, bool allowsNonContiguousLayout) -> void
    {
        nslayoutmanager_set_allows_non_contiguous_layout(handle, allowsNonContiguousLayout);
    }

    public static function hasNonContiguousLayout(int handle) -> bool
    {
        int r;

        let r = nslayoutmanager_has_non_contiguous_layout(handle);

        return r == 1;
    }

    public static function limitsLayoutForSuspiciousContents(int handle) -> bool
    {
        int r;

        let r = nslayoutmanager_limits_layout_for_suspicious_contents(handle);

        return r == 1;
    }

    public static function setLimitsLayoutForSuspiciousContents(int handle, bool limitsLayoutForSuspiciousContents) -> void
    {
        nslayoutmanager_set_limits_layout_for_suspicious_contents(handle, limitsLayoutForSuspiciousContents);
    }

    public static function backgroundLayoutEnabled(int handle) -> bool
    {
        int r;

        let r = nslayoutmanager_background_layout_enabled(handle);

        return r == 1;
    }

    public static function setBackgroundLayoutEnabled(int handle, bool backgroundLayoutEnabled) -> void
    {
        nslayoutmanager_set_background_layout_enabled(handle, backgroundLayoutEnabled);
    }

    public static function defaultAttachmentScaling(int handle) -> int
    {
        return nslayoutmanager_default_attachment_scaling(handle);
    }

    public static function setDefaultAttachmentScaling(int handle, int defaultAttachmentScaling) -> void
    {
        nslayoutmanager_set_default_attachment_scaling(handle, defaultAttachmentScaling);
    }

    public static function typesetter(int handle) -> int
    {
        return nslayoutmanager_typesetter(handle);
    }

    public static function setTypesetter(int handle, int typesetter) -> void
    {
        nslayoutmanager_set_typesetter(handle, typesetter);
    }

    public static function typesetterBehavior(int handle) -> int
    {
        return nslayoutmanager_typesetter_behavior(handle);
    }

    public static function setTypesetterBehavior(int handle, int typesetterBehavior) -> void
    {
        nslayoutmanager_set_typesetter_behavior(handle, typesetterBehavior);
    }

    public static function invalidateGlyphsForCharacterRangeChangeInLengthActualCharacterRange(int handle, int location, int length, int delta) -> array
    {
        var result;

        let result = nslayoutmanager_invalidate_glyphs_for_character_range_change_in_length_actual_character_range(handle, location, length, delta);

        return result;
    }

    public static function invalidateLayoutForCharacterRangeActualCharacterRange(int handle, int location, int length) -> array
    {
        var result;

        let result = nslayoutmanager_invalidate_layout_for_character_range_actual_character_range(handle, location, length);

        return result;
    }

    public static function invalidateDisplayForCharacterRange(int handle, int location, int length) -> void
    {
        nslayoutmanager_invalidate_display_for_character_range(handle, location, length);
    }

    public static function invalidateDisplayForGlyphRange(int handle, int location, int length) -> void
    {
        nslayoutmanager_invalidate_display_for_glyph_range(handle, location, length);
    }

    public static function processEditingForTextStorageEditedRangeChangeInLengthInvalidatedRange(int handle, int textStorage, int editMask, int location, int length, int delta, int invalidatedLocation, int invalidatedLength) -> void
    {
        nslayoutmanager_process_editing_for_text_storage_edited_range_change_in_length_invalidated_range(handle, textStorage, editMask, location, length, delta, invalidatedLocation, invalidatedLength);
    }

    public static function ensureGlyphsForCharacterRange(int handle, int location, int length) -> void
    {
        nslayoutmanager_ensure_glyphs_for_character_range(handle, location, length);
    }

    public static function ensureGlyphsForGlyphRange(int handle, int location, int length) -> void
    {
        nslayoutmanager_ensure_glyphs_for_glyph_range(handle, location, length);
    }

    public static function ensureLayoutForCharacterRange(int handle, int location, int length) -> void
    {
        nslayoutmanager_ensure_layout_for_character_range(handle, location, length);
    }

    public static function ensureLayoutForGlyphRange(int handle, int location, int length) -> void
    {
        nslayoutmanager_ensure_layout_for_glyph_range(handle, location, length);
    }

    public static function ensureLayoutForTextContainer(int handle, int container) -> void
    {
        nslayoutmanager_ensure_layout_for_text_container(handle, container);
    }

    public static function ensureLayoutForBoundingRectInTextContainer(int handle, double x, double y, double width, double height, int container) -> void
    {
        nslayoutmanager_ensure_layout_for_bounding_rect_in_text_container(handle, x, y, width, height, container);
    }

    public static function numberOfGlyphs(int handle) -> int
    {
        return nslayoutmanager_number_of_glyphs(handle);
    }

    public static function CGGlyphAtIndexIsValidIndex(int handle, int glyphIndex) -> array
    {
        var result;

        let result = nslayoutmanager_cg_glyph_at_index_is_valid_index(handle, glyphIndex);

        return result;
    }

    public static function CGGlyphAtIndex(int handle, int glyphIndex) -> int
    {
        return nslayoutmanager_cg_glyph_at_index(handle, glyphIndex);
    }

    public static function isValidGlyphIndex(int handle, int glyphIndex) -> bool
    {
        int r;

        let r = nslayoutmanager_is_valid_glyph_index(handle, glyphIndex);

        return r == 1;
    }

    public static function propertyForGlyphAtIndex(int handle, int glyphIndex) -> int
    {
        return nslayoutmanager_property_for_glyph_at_index(handle, glyphIndex);
    }

    public static function characterIndexForGlyphAtIndex(int handle, int glyphIndex) -> int
    {
        return nslayoutmanager_character_index_for_glyph_at_index(handle, glyphIndex);
    }

    public static function glyphIndexForCharacterAtIndex(int handle, int charIndex) -> int
    {
        return nslayoutmanager_glyph_index_for_character_at_index(handle, charIndex);
    }

    public static function setTextContainerForGlyphRange(int handle, int container, int location, int length) -> void
    {
        nslayoutmanager_set_text_container_for_glyph_range(handle, container, location, length);
    }

    public static function setLineFragmentRectForGlyphRangeUsedRect(int handle, double x, double y, double width, double height, int location, int length, double usedX, double usedY, double usedWidth, double usedHeight) -> void
    {
        nslayoutmanager_set_line_fragment_rect_for_glyph_range_used_rect(handle, x, y, width, height, location, length, usedX, usedY, usedWidth, usedHeight);
    }

    public static function setExtraLineFragmentRectUsedRectTextContainer(int handle, double x, double y, double width, double height, double usedX, double usedY, double usedWidth, double usedHeight, int container) -> void
    {
        nslayoutmanager_set_extra_line_fragment_rect_used_rect_text_container(handle, x, y, width, height, usedX, usedY, usedWidth, usedHeight, container);
    }

    public static function setLocationForStartOfGlyphRange(int handle, double x, double y, int location, int length) -> void
    {
        nslayoutmanager_set_location_for_start_of_glyph_range(handle, x, y, location, length);
    }

    public static function setNotShownAttributeForGlyphAtIndex(int handle, bool flag, int glyphIndex) -> void
    {
        nslayoutmanager_set_not_shown_attribute_for_glyph_at_index(handle, flag, glyphIndex);
    }

    public static function setDrawsOutsideLineFragmentForGlyphAtIndex(int handle, bool flag, int glyphIndex) -> void
    {
        nslayoutmanager_set_draws_outside_line_fragment_for_glyph_at_index(handle, flag, glyphIndex);
    }

    public static function setAttachmentSizeForGlyphRange(int handle, double width, double height, int location, int length) -> void
    {
        nslayoutmanager_set_attachment_size_for_glyph_range(handle, width, height, location, length);
    }

    public static function getFirstUnlaidCharacterIndexGlyphIndex(int handle) -> array
    {
        var result;

        let result = nslayoutmanager_get_first_unlaid_character_index_glyph_index(handle);

        return result;
    }

    public static function firstUnlaidCharacterIndex(int handle) -> int
    {
        return nslayoutmanager_first_unlaid_character_index(handle);
    }

    public static function firstUnlaidGlyphIndex(int handle) -> int
    {
        return nslayoutmanager_first_unlaid_glyph_index(handle);
    }

    public static function textContainerForGlyphAtIndexEffectiveRange(int handle, int glyphIndex) -> array
    {
        var result;

        let result = nslayoutmanager_text_container_for_glyph_at_index_effective_range(handle, glyphIndex);

        return result;
    }

    public static function textContainerForGlyphAtIndexEffectiveRangeWithoutAdditionalLayout(int handle, int glyphIndex, bool flag) -> array
    {
        var result;

        let result = nslayoutmanager_text_container_for_glyph_at_index_effective_range_without_additional_layout(handle, glyphIndex, flag);

        return result;
    }

    public static function usedRectForTextContainer(int handle, int container) -> array
    {
        var result;

        let result = nslayoutmanager_used_rect_for_text_container(handle, container);

        return result;
    }

    public static function lineFragmentRectForGlyphAtIndexEffectiveRange(int handle, int glyphIndex) -> array
    {
        var result;

        let result = nslayoutmanager_line_fragment_rect_for_glyph_at_index_effective_range(handle, glyphIndex);

        return result;
    }

    public static function lineFragmentRectForGlyphAtIndexEffectiveRangeWithoutAdditionalLayout(int handle, int glyphIndex, bool flag) -> array
    {
        var result;

        let result = nslayoutmanager_line_fragment_rect_for_glyph_at_index_effective_range_without_additional_layout(handle, glyphIndex, flag);

        return result;
    }

    public static function lineFragmentUsedRectForGlyphAtIndexEffectiveRange(int handle, int glyphIndex) -> array
    {
        var result;

        let result = nslayoutmanager_line_fragment_used_rect_for_glyph_at_index_effective_range(handle, glyphIndex);

        return result;
    }

    public static function lineFragmentUsedRectForGlyphAtIndexEffectiveRangeWithoutAdditionalLayout(int handle, int glyphIndex, bool flag) -> array
    {
        var result;

        let result = nslayoutmanager_line_fragment_used_rect_for_glyph_at_index_effective_range_without_additional_layout(handle, glyphIndex, flag);

        return result;
    }

    public static function extraLineFragmentRect(int handle) -> array
    {
        var result;

        let result = nslayoutmanager_extra_line_fragment_rect(handle);

        return result;
    }

    public static function extraLineFragmentUsedRect(int handle) -> array
    {
        var result;

        let result = nslayoutmanager_extra_line_fragment_used_rect(handle);

        return result;
    }

    public static function extraLineFragmentTextContainer(int handle) -> int
    {
        return nslayoutmanager_extra_line_fragment_text_container(handle);
    }

    public static function locationForGlyphAtIndex(int handle, int glyphIndex) -> array
    {
        var result;

        let result = nslayoutmanager_location_for_glyph_at_index(handle, glyphIndex);

        return result;
    }

    public static function notShownAttributeForGlyphAtIndex(int handle, int glyphIndex) -> bool
    {
        int r;

        let r = nslayoutmanager_not_shown_attribute_for_glyph_at_index(handle, glyphIndex);

        return r == 1;
    }

    public static function drawsOutsideLineFragmentForGlyphAtIndex(int handle, int glyphIndex) -> bool
    {
        int r;

        let r = nslayoutmanager_draws_outside_line_fragment_for_glyph_at_index(handle, glyphIndex);

        return r == 1;
    }

    public static function attachmentSizeForGlyphAtIndex(int handle, int glyphIndex) -> array
    {
        var result;

        let result = nslayoutmanager_attachment_size_for_glyph_at_index(handle, glyphIndex);

        return result;
    }

    public static function truncatedGlyphRangeInLineFragmentForGlyphAtIndex(int handle, int glyphIndex) -> array
    {
        var result;

        let result = nslayoutmanager_truncated_glyph_range_in_line_fragment_for_glyph_at_index(handle, glyphIndex);

        return result;
    }

    public static function glyphRangeForCharacterRangeActualCharacterRange(int handle, int location, int length) -> array
    {
        var result;

        let result = nslayoutmanager_glyph_range_for_character_range_actual_character_range(handle, location, length);

        return result;
    }

    public static function characterRangeForGlyphRangeActualGlyphRange(int handle, int location, int length) -> array
    {
        var result;

        let result = nslayoutmanager_character_range_for_glyph_range_actual_glyph_range(handle, location, length);

        return result;
    }

    public static function glyphRangeForTextContainer(int handle, int container) -> array
    {
        var result;

        let result = nslayoutmanager_glyph_range_for_text_container(handle, container);

        return result;
    }

    public static function rangeOfNominallySpacedGlyphsContainingIndex(int handle, int glyphIndex) -> array
    {
        var result;

        let result = nslayoutmanager_range_of_nominally_spaced_glyphs_containing_index(handle, glyphIndex);

        return result;
    }

    public static function boundingRectForGlyphRangeInTextContainer(int handle, int location, int length, int container) -> array
    {
        var result;

        let result = nslayoutmanager_bounding_rect_for_glyph_range_in_text_container(handle, location, length, container);

        return result;
    }

    public static function glyphRangeForBoundingRectInTextContainer(int handle, double x, double y, double width, double height, int container) -> array
    {
        var result;

        let result = nslayoutmanager_glyph_range_for_bounding_rect_in_text_container(handle, x, y, width, height, container);

        return result;
    }

    public static function glyphRangeForBoundingRectWithoutAdditionalLayoutInTextContainer(int handle, double x, double y, double width, double height, int container) -> array
    {
        var result;

        let result = nslayoutmanager_glyph_range_for_bounding_rect_without_additional_layout_in_text_container(handle, x, y, width, height, container);

        return result;
    }

    public static function glyphIndexForPointInTextContainerFractionOfDistanceThroughGlyph(int handle, double x, double y, int container) -> array
    {
        var result;

        let result = nslayoutmanager_glyph_index_for_point_in_text_container_fraction_of_distance_through_glyph(handle, x, y, container);

        return result;
    }

    public static function glyphIndexForPointInTextContainer(int handle, double x, double y, int container) -> int
    {
        return nslayoutmanager_glyph_index_for_point_in_text_container(handle, x, y, container);
    }

    public static function fractionOfDistanceThroughGlyphForPointInTextContainer(int handle, double x, double y, int container) -> double
    {
        return nslayoutmanager_fraction_of_distance_through_glyph_for_point_in_text_container(handle, x, y, container);
    }

    public static function characterIndexForPointInTextContainerFractionOfDistanceBetweenInsertionPoints(int handle, double x, double y, int container) -> array
    {
        var result;

        let result = nslayoutmanager_character_index_for_point_in_text_container_fraction_of_distance_between_insertion_points(handle, x, y, container);

        return result;
    }

    public static function drawBackgroundForGlyphRangeAtPoint(int handle, int location, int length, double x, double y) -> void
    {
        nslayoutmanager_draw_background_for_glyph_range_at_point(handle, location, length, x, y);
    }

    public static function drawGlyphsForGlyphRangeAtPoint(int handle, int location, int length, double x, double y) -> void
    {
        nslayoutmanager_draw_glyphs_for_glyph_range_at_point(handle, location, length, x, y);
    }

    public static function drawUnderlineForGlyphRangeUnderlineTypeBaselineOffsetLineFragmentRectLineFragmentGlyphRangeContainerOrigin(int handle, int location, int length, int underlineVal, double baselineOffset, double x, double y, double width, double height, int lineLocation, int lineLength, double originX, double originY) -> void
    {
        nslayoutmanager_draw_underline_for_glyph_range_underline_type_baseline_offset_line_fragment_rect_line_fragment_glyph_range_container_origin(handle, location, length, underlineVal, baselineOffset, x, y, width, height, lineLocation, lineLength, originX, originY);
    }

    public static function underlineGlyphRangeUnderlineTypeLineFragmentRectLineFragmentGlyphRangeContainerOrigin(int handle, int location, int length, int underlineVal, double x, double y, double width, double height, int lineLocation, int lineLength, double originX, double originY) -> void
    {
        nslayoutmanager_underline_glyph_range_underline_type_line_fragment_rect_line_fragment_glyph_range_container_origin(handle, location, length, underlineVal, x, y, width, height, lineLocation, lineLength, originX, originY);
    }

    public static function drawStrikethroughForGlyphRangeStrikethroughTypeBaselineOffsetLineFragmentRectLineFragmentGlyphRangeContainerOrigin(int handle, int location, int length, int strikethroughVal, double baselineOffset, double x, double y, double width, double height, int lineLocation, int lineLength, double originX, double originY) -> void
    {
        nslayoutmanager_draw_strikethrough_for_glyph_range_strikethrough_type_baseline_offset_line_fragment_rect_line_fragment_glyph_range_container_origin(handle, location, length, strikethroughVal, baselineOffset, x, y, width, height, lineLocation, lineLength, originX, originY);
    }

    public static function strikethroughGlyphRangeStrikethroughTypeLineFragmentRectLineFragmentGlyphRangeContainerOrigin(int handle, int location, int length, int strikethroughVal, double x, double y, double width, double height, int lineLocation, int lineLength, double originX, double originY) -> void
    {
        nslayoutmanager_strikethrough_glyph_range_strikethrough_type_line_fragment_rect_line_fragment_glyph_range_container_origin(handle, location, length, strikethroughVal, x, y, width, height, lineLocation, lineLength, originX, originY);
    }

    public static function showAttachmentCellInRectCharacterIndex(int handle, int cell, double x, double y, double width, double height, int attachmentIndex) -> void
    {
        nslayoutmanager_show_attachment_cell_in_rect_character_index(handle, cell, x, y, width, height, attachmentIndex);
    }

    public static function setLayoutRectForTextBlockGlyphRange(int handle, double x, double y, double width, double height, int block, int location, int length) -> void
    {
        nslayoutmanager_set_layout_rect_for_text_block_glyph_range(handle, x, y, width, height, block, location, length);
    }

    public static function setBoundsRectForTextBlockGlyphRange(int handle, double x, double y, double width, double height, int block, int location, int length) -> void
    {
        nslayoutmanager_set_bounds_rect_for_text_block_glyph_range(handle, x, y, width, height, block, location, length);
    }

    public static function layoutRectForTextBlockGlyphRange(int handle, int block, int location, int length) -> array
    {
        var result;

        let result = nslayoutmanager_layout_rect_for_text_block_glyph_range(handle, block, location, length);

        return result;
    }

    public static function boundsRectForTextBlockGlyphRange(int handle, int block, int location, int length) -> array
    {
        var result;

        let result = nslayoutmanager_bounds_rect_for_text_block_glyph_range(handle, block, location, length);

        return result;
    }

    public static function layoutRectForTextBlockAtIndexEffectiveRange(int handle, int block, int glyphIndex) -> array
    {
        var result;

        let result = nslayoutmanager_layout_rect_for_text_block_at_index_effective_range(handle, block, glyphIndex);

        return result;
    }

    public static function boundsRectForTextBlockAtIndexEffectiveRange(int handle, int block, int glyphIndex) -> array
    {
        var result;

        let result = nslayoutmanager_bounds_rect_for_text_block_at_index_effective_range(handle, block, glyphIndex);

        return result;
    }

    public static function temporaryAttributesAtCharacterIndexEffectiveRange(int handle, int charIndex) -> array
    {
        var result;

        let result = nslayoutmanager_temporary_attributes_at_character_index_effective_range(handle, charIndex);

        return result;
    }

    public static function setTemporaryAttributesForCharacterRange(int handle, int attrs, int location, int length) -> void
    {
        nslayoutmanager_set_temporary_attributes_for_character_range(handle, attrs, location, length);
    }

    public static function addTemporaryAttributesForCharacterRange(int handle, int attrs, int location, int length) -> void
    {
        nslayoutmanager_add_temporary_attributes_for_character_range(handle, attrs, location, length);
    }

    public static function removeTemporaryAttributeForCharacterRange(int handle, string attrName, int location, int length) -> void
    {
        nslayoutmanager_remove_temporary_attribute_for_character_range(handle, attrName, location, length);
    }

    public static function temporaryAttributeAtCharacterIndexEffectiveRange(int handle, string attrName, int location) -> array
    {
        var result;

        let result = nslayoutmanager_temporary_attribute_at_character_index_effective_range(handle, attrName, location);

        return result;
    }

    public static function temporaryAttributeAtCharacterIndexLongestEffectiveRangeInRange(int handle, string attrName, int location, int rangeLimitLocation, int rangeLimitLength) -> array
    {
        var result;

        let result = nslayoutmanager_temporary_attribute_at_character_index_longest_effective_range_in_range(handle, attrName, location, rangeLimitLocation, rangeLimitLength);

        return result;
    }

    public static function temporaryAttributesAtCharacterIndexLongestEffectiveRangeInRange(int handle, int location, int rangeLimitLocation, int rangeLimitLength) -> array
    {
        var result;

        let result = nslayoutmanager_temporary_attributes_at_character_index_longest_effective_range_in_range(handle, location, rangeLimitLocation, rangeLimitLength);

        return result;
    }

    public static function addTemporaryAttributeValueForCharacterRange(int handle, string attrName, int value, int location, int length) -> void
    {
        nslayoutmanager_add_temporary_attribute_value_for_character_range(handle, attrName, value, location, length);
    }

    public static function defaultLineHeightForFont(int handle, int theFont) -> double
    {
        return nslayoutmanager_default_line_height_for_font(handle, theFont);
    }

    public static function defaultBaselineOffsetForFont(int handle, int theFont) -> double
    {
        return nslayoutmanager_default_baseline_offset_for_font(handle, theFont);
    }

    public static function rulerMarkersForTextViewParagraphStyleRuler(int handle, int view, int style, int ruler) -> array
    {
        var result;

        let result = nslayoutmanager_ruler_markers_for_text_view_paragraph_style_ruler(handle, view, style, ruler);

        return result;
    }

    public static function rulerAccessoryViewForTextViewParagraphStyleRulerEnabled(int handle, int view, int style, int ruler, bool isEnabled) -> int
    {
        return nslayoutmanager_ruler_accessory_view_for_text_view_paragraph_style_ruler_enabled(handle, view, style, ruler, isEnabled);
    }

    public static function layoutManagerOwnsFirstResponderInWindow(int handle, int window) -> bool
    {
        int r;

        let r = nslayoutmanager_layout_manager_owns_first_responder_in_window(handle, window);

        return r == 1;
    }

    public static function firstTextView(int handle) -> int
    {
        return nslayoutmanager_first_text_view(handle);
    }

    public static function textViewForBeginningOfSelection(int handle) -> int
    {
        return nslayoutmanager_text_view_for_beginning_of_selection(handle);
    }

    public static function glyphAtIndexIsValidIndex(int handle, int glyphIndex) -> array
    {
        var result;

        let result = nslayoutmanager_glyph_at_index_is_valid_index(handle, glyphIndex);

        return result;
    }

    public static function glyphAtIndex(int handle, int glyphIndex) -> int
    {
        return nslayoutmanager_glyph_at_index(handle, glyphIndex);
    }

    public static function rectArrayForCharacterRangeWithinSelectedCharacterRangeInTextContainerRectCount(int handle, int location, int length, int selLocation, int selLength, int container) -> array
    {
        var result;

        let result = nslayoutmanager_rect_array_for_character_range_within_selected_character_range_in_text_container_rect_count(handle, location, length, selLocation, selLength, container);

        return result;
    }

    public static function rectArrayForGlyphRangeWithinSelectedGlyphRangeInTextContainerRectCount(int handle, int location, int length, int selLocation, int selLength, int container) -> array
    {
        var result;

        let result = nslayoutmanager_rect_array_for_glyph_range_within_selected_glyph_range_in_text_container_rect_count(handle, location, length, selLocation, selLength, container);

        return result;
    }

    public static function glyphGenerator(int handle) -> int
    {
        return nslayoutmanager_glyph_generator(handle);
    }

    public static function setGlyphGenerator(int handle, int glyphGenerator) -> void
    {
        nslayoutmanager_set_glyph_generator(handle, glyphGenerator);
    }

    // ---- @reserved (not bound; see src/*.h) ----
    // @reserved - (nullable instancetype)initWithCoder:(NSCoder *)coder NS_DESIGNATED_INITIALIZER;
    // @reserved - (void)setGlyphs:(const CGGlyph *)glyphs properties:(const NSGlyphProperty *)props characterIndexes:(const NSUInteger *)charIndexes font:(NSFont *)aFont forGlyphRange:(NSRange)glyphRange — C glyph/property/index buffers
    // @reserved - (NSUInteger)getGlyphsInRange:(NSRange)glyphRange glyphs:(nullable CGGlyph *)glyphBuffer properties:(nullable NSGlyphProperty *)props characterIndexes:(nullable NSUInteger *)charIndexBuffer bidiLevels:(nullable unsigned char *)bidiLevelBuffer — C out-buffers
    // @reserved - (NSUInteger)getLineFragmentInsertionPointsForCharacterAtIndex:(NSUInteger)charIndex alternatePositions:(BOOL)aFlag inDisplayOrder:(BOOL)dFlag positions:(nullable CGFloat *)positions characterIndexes:(nullable NSUInteger *)charIndexes — C out-buffers
    // @reserved - (void)enumerateLineFragmentsForGlyphRange:(NSRange)glyphRange usingBlock:(void (^)(NSRect rect, NSRect usedRect, NSTextContainer *textContainer, NSRange glyphRange, BOOL *stop))block
    // @reserved - (void)enumerateEnclosingRectsForGlyphRange:(NSRange)glyphRange withinSelectedGlyphRange:(NSRange)selectedRange inTextContainer:(NSTextContainer *)textContainer usingBlock:(void (^)(NSRect rect, BOOL *stop))block
    // @reserved - (void)showCGGlyphs:(const CGGlyph *)glyphs positions:(const CGPoint *)positions count:(NSInteger)glyphCount font:(NSFont *)font textMatrix:(CGAffineTransform)textMatrix attributes:(NSDictionary<NSAttributedStringKey, id> *)attributes inContext:(CGContextRef)CGContext — C buffers, CGAffineTransform, CGContextRef
    // @reserved - (void)fillBackgroundRectArray:(const NSRect *)rectArray count:(NSUInteger)rectCount forCharacterRange:(NSRange)charRange color:(NSColor *)color — C rect buffer
    // @reserved @property BOOL usesScreenFonts API_DEPRECATED("", macos(10.0,10.11)); — getter
    // @reserved @property BOOL usesScreenFonts API_DEPRECATED("", macos(10.0,10.11)); — setter
    // @reserved - (NSFont *)substituteFontForFont:(NSFont *)originalFont API_DEPRECATED("", macos(10.0,10.11));
    // @reserved - (void)insertGlyphs:(const NSGlyph *)glyphs length:(NSUInteger)length forStartingGlyphAtIndex:(NSUInteger)glyphIndex characterIndex:(NSUInteger)charIndex API_DEPRECATED
    // @reserved - (void)insertGlyph:(NSGlyph)glyph atGlyphIndex:(NSUInteger)glyphIndex characterIndex:(NSUInteger)charIndex API_DEPRECATED
    // @reserved - (void)replaceGlyphAtIndex:(NSUInteger)glyphIndex withGlyph:(NSGlyph)newGlyph API_DEPRECATED
    // @reserved - (void)deleteGlyphsInRange:(NSRange)glyphRange API_DEPRECATED
    // @reserved - (void)setCharacterIndex:(NSUInteger)charIndex forGlyphAtIndex:(NSUInteger)glyphIndex API_DEPRECATED
    // @reserved - (void)setIntAttribute:(NSInteger)attributeTag value:(NSInteger)val forGlyphAtIndex:(NSUInteger)glyphIndex API_DEPRECATED
    // @reserved - (void)invalidateGlyphsOnLayoutInvalidationForGlyphRange:(NSRange)glyphRange API_DEPRECATED
    // @reserved - (NSInteger)intAttribute:(NSInteger)attributeTag forGlyphAtIndex:(NSUInteger)glyphIndex API_DEPRECATED
    // @reserved - (NSUInteger)getGlyphsInRange:(NSRange)glyphRange glyphs:(nullable NSGlyph *)glyphBuffer characterIndexes:(nullable NSUInteger *)charIndexBuffer glyphInscriptions:(nullable NSGlyphInscription *)inscribeBuffer elasticBits:(nullable BOOL *)elasticBuffer API_DEPRECATED
    // @reserved - (NSUInteger)getGlyphsInRange:(NSRange)glyphRange glyphs:(nullable NSGlyph *)glyphBuffer characterIndexes:(nullable NSUInteger *)charIndexBuffer glyphInscriptions:(nullable NSGlyphInscription *)inscribeBuffer elasticBits:(nullable BOOL *)elasticBuffer bidiLevels:(nullable unsigned char *)bidiLevelBuffer API_DEPRECATED
    // @reserved - (NSUInteger)getGlyphs:(nullable NSGlyph *)glyphArray range:(NSRange)glyphRange API_DEPRECATED
    // @reserved - (void)invalidateLayoutForCharacterRange:(NSRange)charRange isSoft:(BOOL)flag actualCharacterRange:(nullable NSRangePointer)actualCharRange API_DEPRECATED
    // @reserved - (void)textStorage:(NSTextStorage *)str edited:(NSTextStorageEditedOptions)editedMask range:(NSRange)newCharRange changeInLength:(NSInteger)delta invalidatedRange:(NSRange)invalidatedCharRange API_DEPRECATED
    // @reserved - (void)setLocations:(NSPointArray)locations startingGlyphIndexes:(NSUInteger *)glyphIndexes count:(NSUInteger)count forGlyphRange:(NSRange)glyphRange API_DEPRECATED
    // @reserved - (void)showPackedGlyphs:(char *)glyphs length:(NSUInteger)glyphLen glyphRange:(NSRange)glyphRange atPoint:(NSPoint)point font:(NSFont *)font color:(NSColor *)color printingAdjustment:(NSSize)printingAdjustment API_DEPRECATED
    // @reserved - (void)showCGGlyphs:(const CGGlyph *)glyphs positions:(const NSPoint *)positions count:(NSUInteger)glyphCount font:(NSFont *)font matrix:(NSAffineTransform *)textMatrix attributes:(NSDictionary<NSAttributedStringKey, id> *)attributes inContext:(NSGraphicsContext *)graphicsContext API_DEPRECATED
    // @reserved @property float hyphenationFactor API_DEPRECATED; — getter
    // @reserved @property float hyphenationFactor API_DEPRECATED; — setter

}
