// GENERATED by scripts/gen-zep.php — DO NOT EDIT.
// Source of truth: the @zep/@zep-construct/@reserved annotations in src/*.h.

namespace AppKit\NS\NSTextView;

class NSTextView
{
    public static function initWithFrameTextContainer(double x, double y, double width, double height, int container) -> int
    {
        return nstextview_init_with_frame_text_container(x, y, width, height, container);
    }

    public static function initWithFrame(double x, double y, double width, double height) -> int
    {
        return nstextview_init_with_frame(x, y, width, height);
    }

    public static function initUsingTextLayoutManager(bool usingTextLayoutManager) -> int
    {
        return nstextview_init_using_text_layout_manager(usingTextLayoutManager);
    }

    public static function textViewUsingTextLayoutManager(bool usingTextLayoutManager) -> int
    {
        return nstextview_text_view_using_text_layout_manager(usingTextLayoutManager);
    }

    public static function textContainer(int handle) -> int
    {
        return nstextview_text_container(handle);
    }

    public static function setTextContainer(int handle, int textContainer) -> void
    {
        nstextview_set_text_container(handle, textContainer);
    }

    public static function replaceTextContainer(int handle, int newContainer) -> void
    {
        nstextview_replace_text_container(handle, newContainer);
    }

    public static function textContainerInset(int handle) -> array
    {
        var result;

        let result = nstextview_text_container_inset(handle);

        return result;
    }

    public static function setTextContainerInset(int handle, double width, double height) -> void
    {
        nstextview_set_text_container_inset(handle, width, height);
    }

    public static function textContainerOrigin(int handle) -> array
    {
        var result;

        let result = nstextview_text_container_origin(handle);

        return result;
    }

    public static function invalidateTextContainerOrigin(int handle) -> void
    {
        nstextview_invalidate_text_container_origin(handle);
    }

    public static function layoutManager(int handle) -> int
    {
        return nstextview_layout_manager(handle);
    }

    public static function textStorage(int handle) -> int
    {
        return nstextview_text_storage(handle);
    }

    public static function textLayoutManager(int handle) -> int
    {
        return nstextview_text_layout_manager(handle);
    }

    public static function textContentStorage(int handle) -> int
    {
        return nstextview_text_content_storage(handle);
    }

    public static function setConstrainedFrameSize(int handle, double width, double height) -> void
    {
        nstextview_set_constrained_frame_size(handle, width, height);
    }

    public static function setAlignmentRange(int handle, int alignment, int location, int length) -> void
    {
        nstextview_set_alignment_range(handle, alignment, location, length);
    }

    public static function setBaseWritingDirectionRange(int handle, int writingDirection, int location, int length) -> void
    {
        nstextview_set_base_writing_direction_range(handle, writingDirection, location, length);
    }

    public static function turnOffKerning(int handle, int sender) -> void
    {
        nstextview_turn_off_kerning(handle, sender);
    }

    public static function tightenKerning(int handle, int sender) -> void
    {
        nstextview_tighten_kerning(handle, sender);
    }

    public static function loosenKerning(int handle, int sender) -> void
    {
        nstextview_loosen_kerning(handle, sender);
    }

    public static function useStandardKerning(int handle, int sender) -> void
    {
        nstextview_use_standard_kerning(handle, sender);
    }

    public static function turnOffLigatures(int handle, int sender) -> void
    {
        nstextview_turn_off_ligatures(handle, sender);
    }

    public static function useStandardLigatures(int handle, int sender) -> void
    {
        nstextview_use_standard_ligatures(handle, sender);
    }

    public static function useAllLigatures(int handle, int sender) -> void
    {
        nstextview_use_all_ligatures(handle, sender);
    }

    public static function raiseBaseline(int handle, int sender) -> void
    {
        nstextview_raise_baseline(handle, sender);
    }

    public static function lowerBaseline(int handle, int sender) -> void
    {
        nstextview_lower_baseline(handle, sender);
    }

    public static function outline(int handle, int sender) -> void
    {
        nstextview_outline(handle, sender);
    }

    public static function performFindPanelAction(int handle, int sender) -> void
    {
        nstextview_perform_find_panel_action(handle, sender);
    }

    public static function alignJustified(int handle, int sender) -> void
    {
        nstextview_align_justified(handle, sender);
    }

    public static function changeColor(int handle, int sender) -> void
    {
        nstextview_change_color(handle, sender);
    }

    public static function changeAttributes(int handle, int sender) -> void
    {
        nstextview_change_attributes(handle, sender);
    }

    public static function changeDocumentBackgroundColor(int handle, int sender) -> void
    {
        nstextview_change_document_background_color(handle, sender);
    }

    public static function orderFrontSpacingPanel(int handle, int sender) -> void
    {
        nstextview_order_front_spacing_panel(handle, sender);
    }

    public static function orderFrontLinkPanel(int handle, int sender) -> void
    {
        nstextview_order_front_link_panel(handle, sender);
    }

    public static function orderFrontListPanel(int handle, int sender) -> void
    {
        nstextview_order_front_list_panel(handle, sender);
    }

    public static function orderFrontTablePanel(int handle, int sender) -> void
    {
        nstextview_order_front_table_panel(handle, sender);
    }

    public static function rulerViewDidMoveMarker(int handle, int ruler, int marker) -> void
    {
        nstextview_ruler_view_did_move_marker(handle, ruler, marker);
    }

    public static function rulerViewDidRemoveMarker(int handle, int ruler, int marker) -> void
    {
        nstextview_ruler_view_did_remove_marker(handle, ruler, marker);
    }

    public static function rulerViewDidAddMarker(int handle, int ruler, int marker) -> void
    {
        nstextview_ruler_view_did_add_marker(handle, ruler, marker);
    }

    public static function rulerViewShouldMoveMarker(int handle, int ruler, int marker) -> bool
    {
        int r;

        let r = nstextview_ruler_view_should_move_marker(handle, ruler, marker);

        return r == 1;
    }

    public static function rulerViewShouldAddMarker(int handle, int ruler, int marker) -> bool
    {
        int r;

        let r = nstextview_ruler_view_should_add_marker(handle, ruler, marker);

        return r == 1;
    }

    public static function rulerViewWillMoveMarkerToLocation(int handle, int ruler, int marker, double location) -> double
    {
        return nstextview_ruler_view_will_move_marker_to_location(handle, ruler, marker, location);
    }

    public static function rulerViewShouldRemoveMarker(int handle, int ruler, int marker) -> bool
    {
        int r;

        let r = nstextview_ruler_view_should_remove_marker(handle, ruler, marker);

        return r == 1;
    }

    public static function rulerViewWillAddMarkerAtLocation(int handle, int ruler, int marker, double location) -> double
    {
        return nstextview_ruler_view_will_add_marker_at_location(handle, ruler, marker, location);
    }

    public static function rulerViewHandleMouseDown(int handle, int ruler, int event) -> void
    {
        nstextview_ruler_view_handle_mouse_down(handle, ruler, event);
    }

    public static function setNeedsDisplayInRectAvoidAdditionalLayout(int handle, double x, double y, double width, double height, bool flag) -> void
    {
        nstextview_set_needs_display_in_rect_avoid_additional_layout(handle, x, y, width, height, flag);
    }

    public static function shouldDrawInsertionPoint(int handle) -> bool
    {
        int r;

        let r = nstextview_should_draw_insertion_point(handle);

        return r == 1;
    }

    public static function drawInsertionPointInRectColorTurnedOn(int handle, double x, double y, double width, double height, int color, bool flag) -> void
    {
        nstextview_draw_insertion_point_in_rect_color_turned_on(handle, x, y, width, height, color, flag);
    }

    public static function drawViewBackgroundInRect(int handle, double x, double y, double width, double height) -> void
    {
        nstextview_draw_view_background_in_rect(handle, x, y, width, height);
    }

    public static function updateRuler(int handle) -> void
    {
        nstextview_update_ruler(handle);
    }

    public static function updateFontPanel(int handle) -> void
    {
        nstextview_update_font_panel(handle);
    }

    public static function updateDragTypeRegistration(int handle) -> void
    {
        nstextview_update_drag_type_registration(handle);
    }

    public static function selectionRangeForProposedRangeGranularity(int handle, int location, int length, int granularity) -> array
    {
        var result;

        let result = nstextview_selection_range_for_proposed_range_granularity(handle, location, length, granularity);

        return result;
    }

    public static function clickedOnLinkAtIndex(int handle, int link, int charIndex) -> void
    {
        nstextview_clicked_on_link_at_index(handle, link, charIndex);
    }

    public static function startSpeaking(int handle, int sender) -> void
    {
        nstextview_start_speaking(handle, sender);
    }

    public static function stopSpeaking(int handle, int sender) -> void
    {
        nstextview_stop_speaking(handle, sender);
    }

    public static function setLayoutOrientation(int handle, int orientation) -> void
    {
        nstextview_set_layout_orientation(handle, orientation);
    }

    public static function changeLayoutOrientation(int handle, int sender) -> void
    {
        nstextview_change_layout_orientation(handle, sender);
    }

    public static function characterIndexForInsertionAtPoint(int handle, double x, double y) -> int
    {
        return nstextview_character_index_for_insertion_at_point(handle, x, y);
    }

    public static function stronglyReferencesTextStorage() -> bool
    {
        int r;

        let r = nstextview_strongly_references_text_storage();

        return r == 1;
    }

    public static function usesAdaptiveColorMappingForDarkAppearance(int handle) -> bool
    {
        int r;

        let r = nstextview_uses_adaptive_color_mapping_for_dark_appearance(handle);

        return r == 1;
    }

    public static function setUsesAdaptiveColorMappingForDarkAppearance(int handle, bool usesAdaptiveColorMappingForDarkAppearance) -> void
    {
        nstextview_set_uses_adaptive_color_mapping_for_dark_appearance(handle, usesAdaptiveColorMappingForDarkAppearance);
    }

    public static function complete(int handle, int sender) -> void
    {
        nstextview_complete(handle, sender);
    }

    public static function rangeForUserCompletion(int handle) -> array
    {
        var result;

        let result = nstextview_range_for_user_completion(handle);

        return result;
    }

    public static function completionsForPartialWordRangeIndexOfSelectedItem(int handle, int location, int length) -> array
    {
        var result;

        let result = nstextview_completions_for_partial_word_range_index_of_selected_item(handle, location, length);

        return result;
    }

    public static function insertCompletionForPartialWordRangeMovementIsFinal(int handle, string word, int location, int length, int movement, bool flag) -> void
    {
        nstextview_insert_completion_for_partial_word_range_movement_is_final(handle, word, location, length, movement, flag);
    }

    public static function writablePasteboardTypes(int handle) -> array
    {
        var result;

        let result = nstextview_writable_pasteboard_types(handle);

        return result;
    }

    public static function writeSelectionToPasteboardType(int handle, int pboard, string type) -> bool
    {
        int r;

        let r = nstextview_write_selection_to_pasteboard_type(handle, pboard, type);

        return r == 1;
    }

    public static function writeSelectionToPasteboardTypes(int handle, int pboard, array types) -> bool
    {
        int r;

        let r = nstextview_write_selection_to_pasteboard_types(handle, pboard, types);

        return r == 1;
    }

    public static function readablePasteboardTypes(int handle) -> array
    {
        var result;

        let result = nstextview_readable_pasteboard_types(handle);

        return result;
    }

    public static function preferredPasteboardTypeFromArrayRestrictedToTypesFromArray(int handle, array availableTypes, array allowedTypes) -> var
    {
        var result;

        let result = nstextview_preferred_pasteboard_type_from_array_restricted_to_types_from_array(handle, availableTypes, allowedTypes);

        return result;
    }

    public static function readSelectionFromPasteboardType(int handle, int pboard, string type) -> bool
    {
        int r;

        let r = nstextview_read_selection_from_pasteboard_type(handle, pboard, type);

        return r == 1;
    }

    public static function readSelectionFromPasteboard(int handle, int pboard) -> bool
    {
        int r;

        let r = nstextview_read_selection_from_pasteboard(handle, pboard);

        return r == 1;
    }

    public static function registerForServices() -> void
    {
        nstextview_register_for_services();
    }

    public static function validRequestorForSendTypeReturnType(int handle, var sendType, var returnType) -> int
    {
        return nstextview_valid_requestor_for_send_type_return_type(handle, sendType, returnType);
    }

    public static function pasteAsPlainText(int handle, int sender) -> void
    {
        nstextview_paste_as_plain_text(handle, sender);
    }

    public static function pasteAsRichText(int handle, int sender) -> void
    {
        nstextview_paste_as_rich_text(handle, sender);
    }

    public static function dragSelectionWithEventOffsetSlideBack(int handle, int event, double width, double height, bool slideBack) -> bool
    {
        int r;

        let r = nstextview_drag_selection_with_event_offset_slide_back(handle, event, width, height, slideBack);

        return r == 1;
    }

    public static function dragImageForSelectionWithEventOrigin(int handle, int event) -> array
    {
        var result;

        let result = nstextview_drag_image_for_selection_with_event_origin(handle, event);

        return result;
    }

    public static function acceptableDragTypes(int handle) -> array
    {
        var result;

        let result = nstextview_acceptable_drag_types(handle);

        return result;
    }

    public static function dragOperationForDraggingInfoType(int handle, int dragInfo, string type) -> int
    {
        return nstextview_drag_operation_for_dragging_info_type(handle, dragInfo, type);
    }

    public static function cleanUpAfterDragOperation(int handle) -> void
    {
        nstextview_clean_up_after_drag_operation(handle);
    }

    public static function selectedRanges(int handle) -> array
    {
        var result;

        let result = nstextview_selected_ranges(handle);

        return result;
    }

    public static function setSelectedRanges(int handle, array selectedRanges) -> void
    {
        nstextview_set_selected_ranges(handle, selectedRanges);
    }

    public static function setSelectedRangesAffinityStillSelecting(int handle, array ranges, int affinity, bool stillSelectingFlag) -> void
    {
        nstextview_set_selected_ranges_affinity_still_selecting(handle, ranges, affinity, stillSelectingFlag);
    }

    public static function setSelectedRangeAffinityStillSelecting(int handle, int location, int length, int affinity, bool stillSelectingFlag) -> void
    {
        nstextview_set_selected_range_affinity_still_selecting(handle, location, length, affinity, stillSelectingFlag);
    }

    public static function selectionAffinity(int handle) -> int
    {
        return nstextview_selection_affinity(handle);
    }

    public static function selectionGranularity(int handle) -> int
    {
        return nstextview_selection_granularity(handle);
    }

    public static function setSelectionGranularity(int handle, int selectionGranularity) -> void
    {
        nstextview_set_selection_granularity(handle, selectionGranularity);
    }

    public static function selectedTextAttributes(int handle) -> int
    {
        return nstextview_selected_text_attributes(handle);
    }

    public static function setSelectedTextAttributes(int handle, int selectedTextAttributes) -> void
    {
        nstextview_set_selected_text_attributes(handle, selectedTextAttributes);
    }

    public static function insertionPointColor(int handle) -> int
    {
        return nstextview_insertion_point_color(handle);
    }

    public static function setInsertionPointColor(int handle, int insertionPointColor) -> void
    {
        nstextview_set_insertion_point_color(handle, insertionPointColor);
    }

    public static function updateInsertionPointStateAndRestartTimer(int handle, bool restartFlag) -> void
    {
        nstextview_update_insertion_point_state_and_restart_timer(handle, restartFlag);
    }

    public static function markedTextAttributes(int handle) -> int
    {
        return nstextview_marked_text_attributes(handle);
    }

    public static function setMarkedTextAttributes(int handle, int markedTextAttributes) -> void
    {
        nstextview_set_marked_text_attributes(handle, markedTextAttributes);
    }

    public static function linkTextAttributes(int handle) -> int
    {
        return nstextview_link_text_attributes(handle);
    }

    public static function setLinkTextAttributes(int handle, int linkTextAttributes) -> void
    {
        nstextview_set_link_text_attributes(handle, linkTextAttributes);
    }

    public static function displaysLinkToolTips(int handle) -> bool
    {
        int r;

        let r = nstextview_displays_link_tool_tips(handle);

        return r == 1;
    }

    public static function setDisplaysLinkToolTips(int handle, bool displaysLinkToolTips) -> void
    {
        nstextview_set_displays_link_tool_tips(handle, displaysLinkToolTips);
    }

    public static function acceptsGlyphInfo(int handle) -> bool
    {
        int r;

        let r = nstextview_accepts_glyph_info(handle);

        return r == 1;
    }

    public static function setAcceptsGlyphInfo(int handle, bool acceptsGlyphInfo) -> void
    {
        nstextview_set_accepts_glyph_info(handle, acceptsGlyphInfo);
    }

    public static function usesRuler(int handle) -> bool
    {
        int r;

        let r = nstextview_uses_ruler(handle);

        return r == 1;
    }

    public static function setUsesRuler(int handle, bool usesRuler) -> void
    {
        nstextview_set_uses_ruler(handle, usesRuler);
    }

    public static function usesInspectorBar(int handle) -> bool
    {
        int r;

        let r = nstextview_uses_inspector_bar(handle);

        return r == 1;
    }

    public static function setUsesInspectorBar(int handle, bool usesInspectorBar) -> void
    {
        nstextview_set_uses_inspector_bar(handle, usesInspectorBar);
    }

    public static function isContinuousSpellCheckingEnabled(int handle) -> bool
    {
        int r;

        let r = nstextview_is_continuous_spell_checking_enabled(handle);

        return r == 1;
    }

    public static function setContinuousSpellCheckingEnabled(int handle, bool continuousSpellCheckingEnabled) -> void
    {
        nstextview_set_continuous_spell_checking_enabled(handle, continuousSpellCheckingEnabled);
    }

    public static function toggleContinuousSpellChecking(int handle, int sender) -> void
    {
        nstextview_toggle_continuous_spell_checking(handle, sender);
    }

    public static function spellCheckerDocumentTag(int handle) -> int
    {
        return nstextview_spell_checker_document_tag(handle);
    }

    public static function isGrammarCheckingEnabled(int handle) -> bool
    {
        int r;

        let r = nstextview_is_grammar_checking_enabled(handle);

        return r == 1;
    }

    public static function setGrammarCheckingEnabled(int handle, bool grammarCheckingEnabled) -> void
    {
        nstextview_set_grammar_checking_enabled(handle, grammarCheckingEnabled);
    }

    public static function toggleGrammarChecking(int handle, int sender) -> void
    {
        nstextview_toggle_grammar_checking(handle, sender);
    }

    public static function setSpellingStateRange(int handle, int value, int location, int length) -> void
    {
        nstextview_set_spelling_state_range(handle, value, location, length);
    }

    public static function typingAttributes(int handle) -> int
    {
        return nstextview_typing_attributes(handle);
    }

    public static function setTypingAttributes(int handle, int typingAttributes) -> void
    {
        nstextview_set_typing_attributes(handle, typingAttributes);
    }

    public static function shouldChangeTextInRangesReplacementStrings(int handle, array affectedRanges, array replacementStrings) -> bool
    {
        int r;

        let r = nstextview_should_change_text_in_ranges_replacement_strings(handle, affectedRanges, replacementStrings);

        return r == 1;
    }

    public static function rangesForUserTextChange(int handle) -> array
    {
        var result;

        let result = nstextview_ranges_for_user_text_change(handle);

        return result;
    }

    public static function rangesForUserCharacterAttributeChange(int handle) -> array
    {
        var result;

        let result = nstextview_ranges_for_user_character_attribute_change(handle);

        return result;
    }

    public static function rangesForUserParagraphAttributeChange(int handle) -> array
    {
        var result;

        let result = nstextview_ranges_for_user_paragraph_attribute_change(handle);

        return result;
    }

    public static function shouldChangeTextInRangeReplacementString(int handle, int location, int length, var replacementString) -> bool
    {
        int r;

        let r = nstextview_should_change_text_in_range_replacement_string(handle, location, length, replacementString);

        return r == 1;
    }

    public static function didChangeText(int handle) -> void
    {
        nstextview_did_change_text(handle);
    }

    public static function rangeForUserTextChange(int handle) -> array
    {
        var result;

        let result = nstextview_range_for_user_text_change(handle);

        return result;
    }

    public static function rangeForUserCharacterAttributeChange(int handle) -> array
    {
        var result;

        let result = nstextview_range_for_user_character_attribute_change(handle);

        return result;
    }

    public static function rangeForUserParagraphAttributeChange(int handle) -> array
    {
        var result;

        let result = nstextview_range_for_user_paragraph_attribute_change(handle);

        return result;
    }

    public static function allowsDocumentBackgroundColorChange(int handle) -> bool
    {
        int r;

        let r = nstextview_allows_document_background_color_change(handle);

        return r == 1;
    }

    public static function setAllowsDocumentBackgroundColorChange(int handle, bool allowsDocumentBackgroundColorChange) -> void
    {
        nstextview_set_allows_document_background_color_change(handle, allowsDocumentBackgroundColorChange);
    }

    public static function defaultParagraphStyle(int handle) -> int
    {
        return nstextview_default_paragraph_style(handle);
    }

    public static function setDefaultParagraphStyle(int handle, int defaultParagraphStyle) -> void
    {
        nstextview_set_default_paragraph_style(handle, defaultParagraphStyle);
    }

    public static function allowsUndo(int handle) -> bool
    {
        int r;

        let r = nstextview_allows_undo(handle);

        return r == 1;
    }

    public static function setAllowsUndo(int handle, bool allowsUndo) -> void
    {
        nstextview_set_allows_undo(handle, allowsUndo);
    }

    public static function breakUndoCoalescing(int handle) -> void
    {
        nstextview_break_undo_coalescing(handle);
    }

    public static function isCoalescingUndo(int handle) -> bool
    {
        int r;

        let r = nstextview_is_coalescing_undo(handle);

        return r == 1;
    }

    public static function allowsImageEditing(int handle) -> bool
    {
        int r;

        let r = nstextview_allows_image_editing(handle);

        return r == 1;
    }

    public static function setAllowsImageEditing(int handle, bool allowsImageEditing) -> void
    {
        nstextview_set_allows_image_editing(handle, allowsImageEditing);
    }

    public static function showFindIndicatorForRange(int handle, int location, int length) -> void
    {
        nstextview_show_find_indicator_for_range(handle, location, length);
    }

    public static function usesRolloverButtonForSelection(int handle) -> bool
    {
        int r;

        let r = nstextview_uses_rollover_button_for_selection(handle);

        return r == 1;
    }

    public static function setUsesRolloverButtonForSelection(int handle, bool usesRolloverButtonForSelection) -> void
    {
        nstextview_set_uses_rollover_button_for_selection(handle, usesRolloverButtonForSelection);
    }

    public static function delegate(int handle) -> int
    {
        return nstextview_delegate(handle);
    }

    public static function setDelegate(int handle, int delegate) -> void
    {
        nstextview_set_delegate(handle, delegate);
    }

    public static function isEditable(int handle) -> bool
    {
        int r;

        let r = nstextview_is_editable(handle);

        return r == 1;
    }

    public static function setEditable(int handle, bool editable) -> void
    {
        nstextview_set_editable(handle, editable);
    }

    public static function isSelectable(int handle) -> bool
    {
        int r;

        let r = nstextview_is_selectable(handle);

        return r == 1;
    }

    public static function setSelectable(int handle, bool selectable) -> void
    {
        nstextview_set_selectable(handle, selectable);
    }

    public static function isRichText(int handle) -> bool
    {
        int r;

        let r = nstextview_is_rich_text(handle);

        return r == 1;
    }

    public static function setRichText(int handle, bool richText) -> void
    {
        nstextview_set_rich_text(handle, richText);
    }

    public static function importsGraphics(int handle) -> bool
    {
        int r;

        let r = nstextview_imports_graphics(handle);

        return r == 1;
    }

    public static function setImportsGraphics(int handle, bool importsGraphics) -> void
    {
        nstextview_set_imports_graphics(handle, importsGraphics);
    }

    public static function drawsBackground(int handle) -> bool
    {
        int r;

        let r = nstextview_draws_background(handle);

        return r == 1;
    }

    public static function setDrawsBackground(int handle, bool drawsBackground) -> void
    {
        nstextview_set_draws_background(handle, drawsBackground);
    }

    public static function backgroundColor(int handle) -> int
    {
        return nstextview_background_color(handle);
    }

    public static function setBackgroundColor(int handle, int backgroundColor) -> void
    {
        nstextview_set_background_color(handle, backgroundColor);
    }

    public static function isFieldEditor(int handle) -> bool
    {
        int r;

        let r = nstextview_is_field_editor(handle);

        return r == 1;
    }

    public static function setFieldEditor(int handle, bool fieldEditor) -> void
    {
        nstextview_set_field_editor(handle, fieldEditor);
    }

    public static function usesFontPanel(int handle) -> bool
    {
        int r;

        let r = nstextview_uses_font_panel(handle);

        return r == 1;
    }

    public static function setUsesFontPanel(int handle, bool usesFontPanel) -> void
    {
        nstextview_set_uses_font_panel(handle, usesFontPanel);
    }

    public static function isRulerVisible(int handle) -> bool
    {
        int r;

        let r = nstextview_is_ruler_visible(handle);

        return r == 1;
    }

    public static function setRulerVisible(int handle, bool rulerVisible) -> void
    {
        nstextview_set_ruler_visible(handle, rulerVisible);
    }

    public static function setSelectedRange(int handle, int location, int length) -> void
    {
        nstextview_set_selected_range(handle, location, length);
    }

    public static function allowedInputSourceLocales(int handle) -> array
    {
        var result;

        let result = nstextview_allowed_input_source_locales(handle);

        return result;
    }

    public static function setAllowedInputSourceLocales(int handle, array allowedInputSourceLocales) -> void
    {
        nstextview_set_allowed_input_source_locales(handle, allowedInputSourceLocales);
    }

    public static function isWritingToolsActive(int handle) -> bool
    {
        int r;

        let r = nstextview_is_writing_tools_active(handle);

        return r == 1;
    }

    public static function writingToolsBehavior(int handle) -> int
    {
        return nstextview_writing_tools_behavior(handle);
    }

    public static function setWritingToolsBehavior(int handle, int writingToolsBehavior) -> void
    {
        nstextview_set_writing_tools_behavior(handle, writingToolsBehavior);
    }

    public static function allowedWritingToolsResultOptions(int handle) -> int
    {
        return nstextview_allowed_writing_tools_result_options(handle);
    }

    public static function setAllowedWritingToolsResultOptions(int handle, int allowedWritingToolsResultOptions) -> void
    {
        nstextview_set_allowed_writing_tools_result_options(handle, allowedWritingToolsResultOptions);
    }

    public static function smartInsertDeleteEnabled(int handle) -> bool
    {
        int r;

        let r = nstextview_smart_insert_delete_enabled(handle);

        return r == 1;
    }

    public static function setSmartInsertDeleteEnabled(int handle, bool smartInsertDeleteEnabled) -> void
    {
        nstextview_set_smart_insert_delete_enabled(handle, smartInsertDeleteEnabled);
    }

    public static function smartDeleteRangeForProposedRange(int handle, int location, int length) -> array
    {
        var result;

        let result = nstextview_smart_delete_range_for_proposed_range(handle, location, length);

        return result;
    }

    public static function toggleSmartInsertDelete(int handle, int sender) -> void
    {
        nstextview_toggle_smart_insert_delete(handle, sender);
    }

    public static function smartInsertForStringReplacingRangeBeforeStringAfterString(int handle, string pasteString, int location, int length) -> array
    {
        var result;

        let result = nstextview_smart_insert_for_string_replacing_range_before_string_after_string(handle, pasteString, location, length);

        return result;
    }

    public static function smartInsertBeforeStringForStringReplacingRange(int handle, string pasteString, int location, int length) -> var
    {
        var result;

        let result = nstextview_smart_insert_before_string_for_string_replacing_range(handle, pasteString, location, length);

        return result;
    }

    public static function smartInsertAfterStringForStringReplacingRange(int handle, string pasteString, int location, int length) -> var
    {
        var result;

        let result = nstextview_smart_insert_after_string_for_string_replacing_range(handle, pasteString, location, length);

        return result;
    }

    public static function isAutomaticQuoteSubstitutionEnabled(int handle) -> bool
    {
        int r;

        let r = nstextview_is_automatic_quote_substitution_enabled(handle);

        return r == 1;
    }

    public static function setAutomaticQuoteSubstitutionEnabled(int handle, bool automaticQuoteSubstitutionEnabled) -> void
    {
        nstextview_set_automatic_quote_substitution_enabled(handle, automaticQuoteSubstitutionEnabled);
    }

    public static function toggleAutomaticQuoteSubstitution(int handle, int sender) -> void
    {
        nstextview_toggle_automatic_quote_substitution(handle, sender);
    }

    public static function isAutomaticLinkDetectionEnabled(int handle) -> bool
    {
        int r;

        let r = nstextview_is_automatic_link_detection_enabled(handle);

        return r == 1;
    }

    public static function setAutomaticLinkDetectionEnabled(int handle, bool automaticLinkDetectionEnabled) -> void
    {
        nstextview_set_automatic_link_detection_enabled(handle, automaticLinkDetectionEnabled);
    }

    public static function toggleAutomaticLinkDetection(int handle, int sender) -> void
    {
        nstextview_toggle_automatic_link_detection(handle, sender);
    }

    public static function isAutomaticDataDetectionEnabled(int handle) -> bool
    {
        int r;

        let r = nstextview_is_automatic_data_detection_enabled(handle);

        return r == 1;
    }

    public static function setAutomaticDataDetectionEnabled(int handle, bool automaticDataDetectionEnabled) -> void
    {
        nstextview_set_automatic_data_detection_enabled(handle, automaticDataDetectionEnabled);
    }

    public static function toggleAutomaticDataDetection(int handle, int sender) -> void
    {
        nstextview_toggle_automatic_data_detection(handle, sender);
    }

    public static function isAutomaticDashSubstitutionEnabled(int handle) -> bool
    {
        int r;

        let r = nstextview_is_automatic_dash_substitution_enabled(handle);

        return r == 1;
    }

    public static function setAutomaticDashSubstitutionEnabled(int handle, bool automaticDashSubstitutionEnabled) -> void
    {
        nstextview_set_automatic_dash_substitution_enabled(handle, automaticDashSubstitutionEnabled);
    }

    public static function toggleAutomaticDashSubstitution(int handle, int sender) -> void
    {
        nstextview_toggle_automatic_dash_substitution(handle, sender);
    }

    public static function isAutomaticTextReplacementEnabled(int handle) -> bool
    {
        int r;

        let r = nstextview_is_automatic_text_replacement_enabled(handle);

        return r == 1;
    }

    public static function setAutomaticTextReplacementEnabled(int handle, bool automaticTextReplacementEnabled) -> void
    {
        nstextview_set_automatic_text_replacement_enabled(handle, automaticTextReplacementEnabled);
    }

    public static function toggleAutomaticTextReplacement(int handle, int sender) -> void
    {
        nstextview_toggle_automatic_text_replacement(handle, sender);
    }

    public static function isAutomaticSpellingCorrectionEnabled(int handle) -> bool
    {
        int r;

        let r = nstextview_is_automatic_spelling_correction_enabled(handle);

        return r == 1;
    }

    public static function setAutomaticSpellingCorrectionEnabled(int handle, bool automaticSpellingCorrectionEnabled) -> void
    {
        nstextview_set_automatic_spelling_correction_enabled(handle, automaticSpellingCorrectionEnabled);
    }

    public static function toggleAutomaticSpellingCorrection(int handle, int sender) -> void
    {
        nstextview_toggle_automatic_spelling_correction(handle, sender);
    }

    public static function enabledTextCheckingTypes(int handle) -> int
    {
        return nstextview_enabled_text_checking_types(handle);
    }

    public static function setEnabledTextCheckingTypes(int handle, int enabledTextCheckingTypes) -> void
    {
        nstextview_set_enabled_text_checking_types(handle, enabledTextCheckingTypes);
    }

    public static function checkTextInRangeTypesOptions(int handle, int location, int length, int checkingTypes, int options) -> void
    {
        nstextview_check_text_in_range_types_options(handle, location, length, checkingTypes, options);
    }

    public static function handleTextCheckingResultsForRangeTypesOptionsOrthographyWordCount(int handle, array results, int location, int length, int checkingTypes, int options, int orthography, int wordCount) -> void
    {
        nstextview_handle_text_checking_results_for_range_types_options_orthography_word_count(handle, results, location, length, checkingTypes, options, orthography, wordCount);
    }

    public static function orderFrontSubstitutionsPanel(int handle, int sender) -> void
    {
        nstextview_order_front_substitutions_panel(handle, sender);
    }

    public static function checkTextInSelection(int handle, int sender) -> void
    {
        nstextview_check_text_in_selection(handle, sender);
    }

    public static function checkTextInDocument(int handle, int sender) -> void
    {
        nstextview_check_text_in_document(handle, sender);
    }

    public static function usesFindPanel(int handle) -> bool
    {
        int r;

        let r = nstextview_uses_find_panel(handle);

        return r == 1;
    }

    public static function setUsesFindPanel(int handle, bool usesFindPanel) -> void
    {
        nstextview_set_uses_find_panel(handle, usesFindPanel);
    }

    public static function usesFindBar(int handle) -> bool
    {
        int r;

        let r = nstextview_uses_find_bar(handle);

        return r == 1;
    }

    public static function setUsesFindBar(int handle, bool usesFindBar) -> void
    {
        nstextview_set_uses_find_bar(handle, usesFindBar);
    }

    public static function isIncrementalSearchingEnabled(int handle) -> bool
    {
        int r;

        let r = nstextview_is_incremental_searching_enabled(handle);

        return r == 1;
    }

    public static function setIncrementalSearchingEnabled(int handle, bool incrementalSearchingEnabled) -> void
    {
        nstextview_set_incremental_searching_enabled(handle, incrementalSearchingEnabled);
    }

    public static function inlinePredictionType(int handle) -> int
    {
        return nstextview_inline_prediction_type(handle);
    }

    public static function setInlinePredictionType(int handle, int inlinePredictionType) -> void
    {
        nstextview_set_inline_prediction_type(handle, inlinePredictionType);
    }

    public static function mathExpressionCompletionType(int handle) -> int
    {
        return nstextview_math_expression_completion_type(handle);
    }

    public static function setMathExpressionCompletionType(int handle, int mathExpressionCompletionType) -> void
    {
        nstextview_set_math_expression_completion_type(handle, mathExpressionCompletionType);
    }

    public static function toggleQuickLookPreviewPanel(int handle, int sender) -> void
    {
        nstextview_toggle_quick_look_preview_panel(handle, sender);
    }

    public static function quickLookPreviewableItemsInRanges(int handle, array ranges) -> array
    {
        var result;

        let result = nstextview_quick_look_previewable_items_in_ranges(handle, ranges);

        return result;
    }

    public static function updateQuickLookPreviewPanel(int handle) -> void
    {
        nstextview_update_quick_look_preview_panel(handle);
    }

    public static function orderFrontSharingServicePicker(int handle, int sender) -> void
    {
        nstextview_order_front_sharing_service_picker(handle, sender);
    }

    public static function isAutomaticTextCompletionEnabled(int handle) -> bool
    {
        int r;

        let r = nstextview_is_automatic_text_completion_enabled(handle);

        return r == 1;
    }

    public static function setAutomaticTextCompletionEnabled(int handle, bool automaticTextCompletionEnabled) -> void
    {
        nstextview_set_automatic_text_completion_enabled(handle, automaticTextCompletionEnabled);
    }

    public static function toggleAutomaticTextCompletion(int handle, int sender) -> void
    {
        nstextview_toggle_automatic_text_completion(handle, sender);
    }

    public static function allowsCharacterPickerTouchBarItem(int handle) -> bool
    {
        int r;

        let r = nstextview_allows_character_picker_touch_bar_item(handle);

        return r == 1;
    }

    public static function setAllowsCharacterPickerTouchBarItem(int handle, bool allowsCharacterPickerTouchBarItem) -> void
    {
        nstextview_set_allows_character_picker_touch_bar_item(handle, allowsCharacterPickerTouchBarItem);
    }

    public static function updateTouchBarItemIdentifiers(int handle) -> void
    {
        nstextview_update_touch_bar_item_identifiers(handle);
    }

    public static function updateTextTouchBarItems(int handle) -> void
    {
        nstextview_update_text_touch_bar_items(handle);
    }

    public static function updateCandidates(int handle) -> void
    {
        nstextview_update_candidates(handle);
    }

    public static function candidateListTouchBarItem(int handle) -> int
    {
        return nstextview_candidate_list_touch_bar_item(handle);
    }

    public static function scrollableTextView() -> int
    {
        return nstextview_scrollable_text_view();
    }

    public static function fieldEditor() -> int
    {
        return nstextview_field_editor();
    }

    public static function scrollableDocumentContentTextView() -> int
    {
        return nstextview_scrollable_document_content_text_view();
    }

    public static function scrollablePlainDocumentContentTextView() -> int
    {
        return nstextview_scrollable_plain_document_content_text_view();
    }

    public static function textHighlightAttributes(int handle) -> int
    {
        return nstextview_text_highlight_attributes(handle);
    }

    public static function setTextHighlightAttributes(int handle, int textHighlightAttributes) -> void
    {
        nstextview_set_text_highlight_attributes(handle, textHighlightAttributes);
    }

    public static function drawTextHighlightBackgroundForTextRangeOrigin(int handle, int textRange, double x, double y) -> void
    {
        nstextview_draw_text_highlight_background_for_text_range_origin(handle, textRange, x, y);
    }

    public static function highlight(int handle, int sender) -> void
    {
        nstextview_highlight(handle, sender);
    }

    // ---- @reserved (not bound; see src/*.h) ----
    // @reserved - (nullable instancetype)initWithCoder:(NSCoder *)coder NS_DESIGNATED_INITIALIZER;
    // @reserved - (void)insertText:(id)insertString API_DEPRECATED;
    // @reserved - (void)toggleTraditionalCharacterShape:(nullable id)sender API_DEPRECATED;
    // @reserved - (BOOL)performValidatedReplacementInRange:(NSRange)range withAttributedString:(NSAttributedString *)attributedString;
    // @reserved - (void)toggleBaseWritingDirection:(nullable id)sender API_DEPRECATED;

}
