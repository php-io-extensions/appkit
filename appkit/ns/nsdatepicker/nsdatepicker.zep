// GENERATED by scripts/gen-zep.php — DO NOT EDIT.
// Source of truth: the @zep/@zep-construct/@reserved annotations in src/*.h.

namespace AppKit\NS\NSDatePicker;

class NSDatePicker
{
    public static function initWithFrame(double x, double y, double width, double height) -> int
    {
        return nsdatepicker_init_with_frame(x, y, width, height);
    }

    public static function datePickerStyle(int handle) -> int
    {
        return nsdatepicker_date_picker_style(handle);
    }

    public static function setDatePickerStyle(int handle, int datePickerStyle) -> void
    {
        nsdatepicker_set_date_picker_style(handle, datePickerStyle);
    }

    public static function isBezeled(int handle) -> bool
    {
        int r;

        let r = nsdatepicker_is_bezeled(handle);

        return r == 1;
    }

    public static function setBezeled(int handle, bool bezeled) -> void
    {
        nsdatepicker_set_bezeled(handle, bezeled);
    }

    public static function isBordered(int handle) -> bool
    {
        int r;

        let r = nsdatepicker_is_bordered(handle);

        return r == 1;
    }

    public static function setBordered(int handle, bool bordered) -> void
    {
        nsdatepicker_set_bordered(handle, bordered);
    }

    public static function drawsBackground(int handle) -> bool
    {
        int r;

        let r = nsdatepicker_draws_background(handle);

        return r == 1;
    }

    public static function setDrawsBackground(int handle, bool drawsBackground) -> void
    {
        nsdatepicker_set_draws_background(handle, drawsBackground);
    }

    public static function backgroundColor(int handle) -> int
    {
        return nsdatepicker_background_color(handle);
    }

    public static function setBackgroundColor(int handle, int backgroundColor) -> void
    {
        nsdatepicker_set_background_color(handle, backgroundColor);
    }

    public static function textColor(int handle) -> int
    {
        return nsdatepicker_text_color(handle);
    }

    public static function setTextColor(int handle, int textColor) -> void
    {
        nsdatepicker_set_text_color(handle, textColor);
    }

    public static function datePickerMode(int handle) -> int
    {
        return nsdatepicker_date_picker_mode(handle);
    }

    public static function setDatePickerMode(int handle, int datePickerMode) -> void
    {
        nsdatepicker_set_date_picker_mode(handle, datePickerMode);
    }

    public static function datePickerElements(int handle) -> int
    {
        return nsdatepicker_date_picker_elements(handle);
    }

    public static function setDatePickerElements(int handle, int datePickerElements) -> void
    {
        nsdatepicker_set_date_picker_elements(handle, datePickerElements);
    }

    public static function calendar(int handle) -> int
    {
        return nsdatepicker_calendar(handle);
    }

    public static function setCalendar(int handle, int calendar) -> void
    {
        nsdatepicker_set_calendar(handle, calendar);
    }

    public static function locale(int handle) -> int
    {
        return nsdatepicker_locale(handle);
    }

    public static function setLocale(int handle, int locale) -> void
    {
        nsdatepicker_set_locale(handle, locale);
    }

    public static function timeZone(int handle) -> int
    {
        return nsdatepicker_time_zone(handle);
    }

    public static function setTimeZone(int handle, int timeZone) -> void
    {
        nsdatepicker_set_time_zone(handle, timeZone);
    }

    public static function dateValue(int handle) -> int
    {
        return nsdatepicker_date_value(handle);
    }

    public static function setDateValue(int handle, int dateValue) -> void
    {
        nsdatepicker_set_date_value(handle, dateValue);
    }

    public static function timeInterval(int handle) -> double
    {
        return nsdatepicker_time_interval(handle);
    }

    public static function setTimeInterval(int handle, double timeInterval) -> void
    {
        nsdatepicker_set_time_interval(handle, timeInterval);
    }

    public static function minDate(int handle) -> int
    {
        return nsdatepicker_min_date(handle);
    }

    public static function setMinDate(int handle, int minDate) -> void
    {
        nsdatepicker_set_min_date(handle, minDate);
    }

    public static function maxDate(int handle) -> int
    {
        return nsdatepicker_max_date(handle);
    }

    public static function setMaxDate(int handle, int maxDate) -> void
    {
        nsdatepicker_set_max_date(handle, maxDate);
    }

    public static function presentsCalendarOverlay(int handle) -> bool
    {
        int r;

        let r = nsdatepicker_presents_calendar_overlay(handle);

        return r == 1;
    }

    public static function setPresentsCalendarOverlay(int handle, bool presentsCalendarOverlay) -> void
    {
        nsdatepicker_set_presents_calendar_overlay(handle, presentsCalendarOverlay);
    }

    public static function delegate(int handle) -> int
    {
        return nsdatepicker_delegate(handle);
    }

    public static function setDelegate(int handle, int delegate) -> void
    {
        nsdatepicker_set_delegate(handle, delegate);
    }
}
