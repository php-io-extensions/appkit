// GENERATED by scripts/gen-zep.php — DO NOT EDIT.
// Source of truth: the @zep/@zep-construct/@reserved annotations in src/*.h.

namespace AppKit\NS\NSApplication;

class NSApplication
{
    public static function sharedApplication() -> int
    {
        return nsapplication_shared_application();
    }

    public static function delegate(int handle) -> int
    {
        return nsapplication_delegate(handle);
    }

    public static function setDelegate(int handle, int delegate) -> void
    {
        nsapplication_set_delegate(handle, delegate);
    }

    public static function hide(int handle, int sender) -> void
    {
        nsapplication_hide(handle, sender);
    }

    public static function unhide(int handle, int sender) -> void
    {
        nsapplication_unhide(handle, sender);
    }

    public static function unhideWithoutActivation(int handle) -> void
    {
        nsapplication_unhide_without_activation(handle);
    }

    public static function windowWithWindowNumber(int handle, int windowNum) -> int
    {
        return nsapplication_window_with_window_number(handle, windowNum);
    }

    public static function mainWindow(int handle) -> int
    {
        return nsapplication_main_window(handle);
    }

    public static function keyWindow(int handle) -> int
    {
        return nsapplication_key_window(handle);
    }

    public static function isActive(int handle) -> bool
    {
        int r;

        let r = nsapplication_is_active(handle);

        return r == 1;
    }

    public static function isHidden(int handle) -> bool
    {
        int r;

        let r = nsapplication_is_hidden(handle);

        return r == 1;
    }

    public static function isRunning(int handle) -> bool
    {
        int r;

        let r = nsapplication_is_running(handle);

        return r == 1;
    }

    public static function deactivate(int handle) -> void
    {
        nsapplication_deactivate(handle);
    }

    public static function activate(int handle) -> void
    {
        nsapplication_activate(handle);
    }

    public static function yieldActivationToApplication(int handle, int application) -> void
    {
        nsapplication_yield_activation_to_application(handle, application);
    }

    public static function yieldActivationToApplicationWithBundleIdentifier(int handle, string bundleIdentifier) -> void
    {
        nsapplication_yield_activation_to_application_with_bundle_identifier(handle, bundleIdentifier);
    }

    public static function hideOtherApplications(int handle, int sender) -> void
    {
        nsapplication_hide_other_applications(handle, sender);
    }

    public static function unhideAllApplications(int handle, int sender) -> void
    {
        nsapplication_unhide_all_applications(handle, sender);
    }

    public static function finishLaunching(int handle) -> void
    {
        nsapplication_finish_launching(handle);
    }

    public static function run(int handle) -> void
    {
        nsapplication_run(handle);
    }

    public static function runModalForWindow(int handle, int window) -> int
    {
        return nsapplication_run_modal_for_window(handle, window);
    }

    public static function stop(int handle, int sender) -> void
    {
        nsapplication_stop(handle, sender);
    }

    public static function stopModal(int handle) -> void
    {
        nsapplication_stop_modal(handle);
    }

    public static function stopModalWithCode(int handle, int returnCode) -> void
    {
        nsapplication_stop_modal_with_code(handle, returnCode);
    }

    public static function abortModal(int handle) -> void
    {
        nsapplication_abort_modal(handle);
    }

    public static function modalWindow(int handle) -> int
    {
        return nsapplication_modal_window(handle);
    }

    public static function beginModalSessionForWindow(int handle, int window) -> int
    {
        return nsapplication_begin_modal_session_for_window(handle, window);
    }

    public static function runModalSession(int handle, int session) -> int
    {
        return nsapplication_run_modal_session(handle, session);
    }

    public static function endModalSession(int handle, int session) -> void
    {
        nsapplication_end_modal_session(handle, session);
    }

    public static function terminate(int handle, int sender) -> void
    {
        nsapplication_terminate(handle, sender);
    }

    public static function requestUserAttention(int handle, int requestType) -> int
    {
        return nsapplication_request_user_attention(handle, requestType);
    }

    public static function cancelUserAttentionRequest(int handle, int request) -> void
    {
        nsapplication_cancel_user_attention_request(handle, request);
    }

    public static function preventWindowOrdering(int handle) -> void
    {
        nsapplication_prevent_window_ordering(handle);
    }

    public static function windows(int handle) -> array
    {
        var result;

        let result = nsapplication_windows(handle);

        return result;
    }

    public static function setWindowsNeedUpdate(int handle, bool needUpdate) -> void
    {
        nsapplication_set_windows_need_update(handle, needUpdate);
    }

    public static function updateWindows(int handle) -> void
    {
        nsapplication_update_windows(handle);
    }

    public static function mainMenu(int handle) -> int
    {
        return nsapplication_main_menu(handle);
    }

    public static function setMainMenu(int handle, int menu) -> void
    {
        nsapplication_set_main_menu(handle, menu);
    }

    public static function helpMenu(int handle) -> int
    {
        return nsapplication_help_menu(handle);
    }

    public static function setHelpMenu(int handle, int menu) -> void
    {
        nsapplication_set_help_menu(handle, menu);
    }

    public static function applicationIconImage(int handle) -> int
    {
        return nsapplication_application_icon_image(handle);
    }

    public static function setApplicationIconImage(int handle, int image) -> void
    {
        nsapplication_set_application_icon_image(handle, image);
    }

    public static function activationPolicy(int handle) -> int
    {
        return nsapplication_activation_policy(handle);
    }

    public static function setActivationPolicy(int handle, int activationPolicy) -> bool
    {
        int r;

        let r = nsapplication_set_activation_policy(handle, activationPolicy);

        return r == 1;
    }

    public static function dockTile(int handle) -> int
    {
        return nsapplication_dock_tile(handle);
    }

    public static function reportException(int handle, int exception) -> void
    {
        nsapplication_report_exception(handle, exception);
    }

    public static function detachDrawingThread(string selector, int target, int argument) -> void
    {
        nsapplication_detach_drawing_thread(selector, target, argument);
    }

    public static function replyToApplicationShouldTerminate(int handle, bool shouldTerminate) -> void
    {
        nsapplication_reply_to_application_should_terminate(handle, shouldTerminate);
    }

    public static function replyToOpenOrPrint(int handle, int reply) -> void
    {
        nsapplication_reply_to_open_or_print(handle, reply);
    }

    public static function orderFrontCharacterPalette(int handle, int sender) -> void
    {
        nsapplication_order_front_character_palette(handle, sender);
    }

    public static function presentationOptions(int handle) -> int
    {
        return nsapplication_presentation_options(handle);
    }

    public static function setPresentationOptions(int handle, int options) -> void
    {
        nsapplication_set_presentation_options(handle, options);
    }

    public static function currentSystemPresentationOptions(int handle) -> int
    {
        return nsapplication_current_system_presentation_options(handle);
    }

    public static function occlusionState(int handle) -> int
    {
        return nsapplication_occlusion_state(handle);
    }

    public static function isProtectedDataAvailable(int handle) -> bool
    {
        int r;

        let r = nsapplication_is_protected_data_available(handle);

        return r == 1;
    }

    public static function appearance(int handle) -> int
    {
        return nsapplication_appearance(handle);
    }

    public static function setAppearance(int handle, int appearance) -> void
    {
        nsapplication_set_appearance(handle, appearance);
    }

    public static function effectiveAppearance(int handle) -> int
    {
        return nsapplication_effective_appearance(handle);
    }

    public static function sendEvent(int handle, int event) -> void
    {
        nsapplication_send_event(handle, event);
    }

    public static function postEventAtStart(int handle, int event, bool atStart) -> void
    {
        nsapplication_post_event_at_start(handle, event, atStart);
    }

    public static function currentEvent(int handle) -> int
    {
        return nsapplication_current_event(handle);
    }

    public static function nextEventMatchingMaskUntilDateInModeDequeue(int handle, int mask, int expiration, string mode, bool deqFlag) -> int
    {
        return nsapplication_next_event_matching_mask_until_date_in_mode_dequeue(handle, mask, expiration, mode, deqFlag);
    }

    public static function discardEventsMatchingMaskBeforeEvent(int handle, int mask, int lastEvent) -> void
    {
        nsapplication_discard_events_matching_mask_before_event(handle, mask, lastEvent);
    }

    public static function sendActionToFrom(int handle, string action, int target, int sender) -> bool
    {
        int r;

        let r = nsapplication_send_action_to_from(handle, action, target, sender);

        return r == 1;
    }

    public static function targetForAction(int handle, string action) -> int
    {
        return nsapplication_target_for_action(handle, action);
    }

    public static function targetForActionToFrom(int handle, string action, int target, int sender) -> int
    {
        return nsapplication_target_for_action_to_from(handle, action, target, sender);
    }

    public static function tryToPerformWith(int handle, string action, int object_) -> bool
    {
        int r;

        let r = nsapplication_try_to_perform_with(handle, action, object_);

        return r == 1;
    }

    public static function validRequestorForSendTypeReturnType(int handle, var sendType, var returnType) -> int
    {
        return nsapplication_valid_requestor_for_send_type_return_type(handle, sendType, returnType);
    }

    public static function windowsMenu(int handle) -> int
    {
        return nsapplication_windows_menu(handle);
    }

    public static function setWindowsMenu(int handle, int menu) -> void
    {
        nsapplication_set_windows_menu(handle, menu);
    }

    public static function arrangeInFront(int handle, int sender) -> void
    {
        nsapplication_arrange_in_front(handle, sender);
    }

    public static function removeWindowsItem(int handle, int win) -> void
    {
        nsapplication_remove_windows_item(handle, win);
    }

    public static function addWindowsItemTitleFilename(int handle, int win, string title, bool isFilename) -> void
    {
        nsapplication_add_windows_item_title_filename(handle, win, title, isFilename);
    }

    public static function changeWindowsItemTitleFilename(int handle, int win, string title, bool isFilename) -> void
    {
        nsapplication_change_windows_item_title_filename(handle, win, title, isFilename);
    }

    public static function updateWindowsItem(int handle, int win) -> void
    {
        nsapplication_update_windows_item(handle, win);
    }

    public static function miniaturizeAll(int handle, int sender) -> void
    {
        nsapplication_miniaturize_all(handle, sender);
    }

    public static function isFullKeyboardAccessEnabled(int handle) -> bool
    {
        int r;

        let r = nsapplication_is_full_keyboard_access_enabled(handle);

        return r == 1;
    }

    public static function servicesMenu(int handle) -> int
    {
        return nsapplication_services_menu(handle);
    }

    public static function setServicesMenu(int handle, int menu) -> void
    {
        nsapplication_set_services_menu(handle, menu);
    }

    public static function registerServicesMenuSendTypesReturnTypes(int handle, array sendTypes, array returnTypes) -> void
    {
        nsapplication_register_services_menu_send_types_return_types(handle, sendTypes, returnTypes);
    }

    public static function servicesProvider(int handle) -> int
    {
        return nsapplication_services_provider(handle);
    }

    public static function setServicesProvider(int handle, int provider) -> void
    {
        nsapplication_set_services_provider(handle, provider);
    }

    public static function orderFrontStandardAboutPanel(int handle, int sender) -> void
    {
        nsapplication_order_front_standard_about_panel(handle, sender);
    }

    public static function orderFrontStandardAboutPanelWithOptions(int handle, var options) -> void
    {
        nsapplication_order_front_standard_about_panel_with_options(handle, options);
    }

    public static function userInterfaceLayoutDirection(int handle) -> int
    {
        return nsapplication_user_interface_layout_direction(handle);
    }

    public static function disableRelaunchOnLogin(int handle) -> void
    {
        nsapplication_disable_relaunch_on_login(handle);
    }

    public static function enableRelaunchOnLogin(int handle) -> void
    {
        nsapplication_enable_relaunch_on_login(handle);
    }

    public static function registerForRemoteNotifications(int handle) -> void
    {
        nsapplication_register_for_remote_notifications(handle);
    }

    public static function unregisterForRemoteNotifications(int handle) -> void
    {
        nsapplication_unregister_for_remote_notifications(handle);
    }

    public static function isRegisteredForRemoteNotifications(int handle) -> bool
    {
        int r;

        let r = nsapplication_is_registered_for_remote_notifications(handle);

        return r == 1;
    }

    public static function registerForRemoteNotificationTypes(int handle, int types) -> void
    {
        nsapplication_register_for_remote_notification_types(handle, types);
    }

    public static function enabledRemoteNotificationTypes(int handle) -> int
    {
        return nsapplication_enabled_remote_notification_types(handle);
    }

    // ---- @reserved (not bound; see src/*.h) ----
    // @reserved - (void)activateIgnoringOtherApps:(BOOL)ignoreOtherApps
    // @reserved - (void)enumerateWindowsWithOptions:(NSWindowListOptions)options usingBlock:(void (NS_NOESCAPE ^)(NSWindow *window, BOOL *stop))block
    // @reserved - (NSInteger)runModalForWindow:(NSWindow *)window relativeToWindow:(NSWindow *)docWindow
    // @reserved - (NSModalSession)beginModalSessionForWindow:(NSWindow *)window relativeToWindow:(NSWindow *)docWindow
    // @reserved - (void)application:(NSApplication *)sender printFiles:(NSArray<NSString *> *)filenames
    // @reserved - (void)beginSheet:(NSWindow *)sheet modalForWindow:(NSWindow *)docWindow modalDelegate:(id)modalDelegate didEndSelector:(SEL)didEndSelector contextInfo:(void *)contextInfo
    // @reserved - (void)endSheet:(NSWindow *)sheet
    // @reserved - (void)endSheet:(NSWindow *)sheet returnCode:(NSInteger)returnCode
    // @reserved - (nullable NSWindow *)makeWindowsPerform:(SEL)selector inOrder:(BOOL)inOrder
    // @reserved @property (nullable, readonly, strong) NSGraphicsContext *context

}
