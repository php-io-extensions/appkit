// GENERATED by scripts/gen-zep.php — DO NOT EDIT.
// Source of truth: the @zep/@zep-construct/@reserved annotations in src/*.h.

namespace AppKit\NS\NSWindow;

class NSWindow
{
    public static function frameRectForContentRectStyleMask(double x, double y, double width, double height, int style) -> array
    {
        var result;

        let result = nswindow_frame_rect_for_content_rect_style_mask(x, y, width, height, style);

        return result;
    }

    public static function contentRectForFrameRectStyleMask(double x, double y, double width, double height, int style) -> array
    {
        var result;

        let result = nswindow_content_rect_for_frame_rect_style_mask(x, y, width, height, style);

        return result;
    }

    public static function minFrameWidthWithTitleStyleMask(string title, int style) -> double
    {
        return nswindow_min_frame_width_with_title_style_mask(title, style);
    }

    public static function defaultDepthLimit() -> int
    {
        return nswindow_default_depth_limit();
    }

    public static function frameRectForContentRect(int handle, double x, double y, double width, double height) -> array
    {
        var result;

        let result = nswindow_frame_rect_for_content_rect(handle, x, y, width, height);

        return result;
    }

    public static function contentRectForFrameRect(int handle, double x, double y, double width, double height) -> array
    {
        var result;

        let result = nswindow_content_rect_for_frame_rect(handle, x, y, width, height);

        return result;
    }

    public static function initWithContentRectStyleMaskBackingDefer(double x, double y, double width, double height, int style, int backingStoreType, bool flag) -> int
    {
        return nswindow_init_with_content_rect_style_mask_backing_defer(x, y, width, height, style, backingStoreType, flag);
    }

    public static function initWithContentRectStyleMaskBackingDeferScreen(double x, double y, double width, double height, int style, int backingStoreType, bool flag, int screen) -> int
    {
        return nswindow_init_with_content_rect_style_mask_backing_defer_screen(x, y, width, height, style, backingStoreType, flag, screen);
    }

    public static function title(int handle) -> string
    {
        var result;

        let result = nswindow_title(handle);

        return result;
    }

    public static function setTitle(int handle, string title) -> void
    {
        nswindow_set_title(handle, title);
    }

    public static function subtitle(int handle) -> string
    {
        var result;

        let result = nswindow_subtitle(handle);

        return result;
    }

    public static function setSubtitle(int handle, string subtitle) -> void
    {
        nswindow_set_subtitle(handle, subtitle);
    }

    public static function titleVisibility(int handle) -> int
    {
        return nswindow_title_visibility(handle);
    }

    public static function setTitleVisibility(int handle, int titleVisibility) -> void
    {
        nswindow_set_title_visibility(handle, titleVisibility);
    }

    public static function titlebarAppearsTransparent(int handle) -> bool
    {
        int r;

        let r = nswindow_titlebar_appears_transparent(handle);

        return r == 1;
    }

    public static function setTitlebarAppearsTransparent(int handle, bool titlebarAppearsTransparent) -> void
    {
        nswindow_set_titlebar_appears_transparent(handle, titlebarAppearsTransparent);
    }

    public static function toolbarStyle(int handle) -> int
    {
        return nswindow_toolbar_style(handle);
    }

    public static function setToolbarStyle(int handle, int toolbarStyle) -> void
    {
        nswindow_set_toolbar_style(handle, toolbarStyle);
    }

    public static function contentLayoutRect(int handle) -> array
    {
        var result;

        let result = nswindow_content_layout_rect(handle);

        return result;
    }

    public static function contentLayoutGuide(int handle) -> int
    {
        return nswindow_content_layout_guide(handle);
    }

    public static function titlebarAccessoryViewControllers(int handle) -> array
    {
        var result;

        let result = nswindow_titlebar_accessory_view_controllers(handle);

        return result;
    }

    public static function setTitlebarAccessoryViewControllers(int handle, array titlebarAccessoryViewControllers) -> void
    {
        nswindow_set_titlebar_accessory_view_controllers(handle, titlebarAccessoryViewControllers);
    }

    public static function addTitlebarAccessoryViewController(int handle, int childViewController) -> void
    {
        nswindow_add_titlebar_accessory_view_controller(handle, childViewController);
    }

    public static function insertTitlebarAccessoryViewControllerAtIndex(int handle, int childViewController, int index) -> void
    {
        nswindow_insert_titlebar_accessory_view_controller_at_index(handle, childViewController, index);
    }

    public static function removeTitlebarAccessoryViewControllerAtIndex(int handle, int index) -> void
    {
        nswindow_remove_titlebar_accessory_view_controller_at_index(handle, index);
    }

    public static function representedURL(int handle) -> int
    {
        return nswindow_represented_url(handle);
    }

    public static function setRepresentedURL(int handle, int representedURL) -> void
    {
        nswindow_set_represented_url(handle, representedURL);
    }

    public static function representedFilename(int handle) -> string
    {
        var result;

        let result = nswindow_represented_filename(handle);

        return result;
    }

    public static function setRepresentedFilename(int handle, string representedFilename) -> void
    {
        nswindow_set_represented_filename(handle, representedFilename);
    }

    public static function setTitleWithRepresentedFilename(int handle, string filename) -> void
    {
        nswindow_set_title_with_represented_filename(handle, filename);
    }

    public static function isExcludedFromWindowsMenu(int handle) -> bool
    {
        int r;

        let r = nswindow_is_excluded_from_windows_menu(handle);

        return r == 1;
    }

    public static function setExcludedFromWindowsMenu(int handle, bool excludedFromWindowsMenu) -> void
    {
        nswindow_set_excluded_from_windows_menu(handle, excludedFromWindowsMenu);
    }

    public static function contentView(int handle) -> int
    {
        return nswindow_content_view(handle);
    }

    public static function setContentView(int handle, int contentView) -> void
    {
        nswindow_set_content_view(handle, contentView);
    }

    public static function delegate(int handle) -> int
    {
        return nswindow_delegate(handle);
    }

    public static function setDelegate(int handle, int delegate) -> void
    {
        nswindow_set_delegate(handle, delegate);
    }

    public static function windowNumber(int handle) -> int
    {
        return nswindow_window_number(handle);
    }

    public static function styleMask(int handle) -> int
    {
        return nswindow_style_mask(handle);
    }

    public static function setStyleMask(int handle, int styleMask) -> void
    {
        nswindow_set_style_mask(handle, styleMask);
    }

    public static function fieldEditorForObject(int handle, bool createFlag, int object_) -> int
    {
        return nswindow_field_editor_for_object(handle, createFlag, object_);
    }

    public static function endEditingFor(int handle, int object_) -> void
    {
        nswindow_end_editing_for(handle, object_);
    }

    public static function constrainFrameRectToScreen(int handle, double x, double y, double width, double height, int screen) -> array
    {
        var result;

        let result = nswindow_constrain_frame_rect_to_screen(handle, x, y, width, height, screen);

        return result;
    }

    public static function setFrameDisplay(int handle, double x, double y, double width, double height, bool flag) -> void
    {
        nswindow_set_frame_display(handle, x, y, width, height, flag);
    }

    public static function setContentSize(int handle, double width, double height) -> void
    {
        nswindow_set_content_size(handle, width, height);
    }

    public static function setFrameOrigin(int handle, double x, double y) -> void
    {
        nswindow_set_frame_origin(handle, x, y);
    }

    public static function setFrameTopLeftPoint(int handle, double x, double y) -> void
    {
        nswindow_set_frame_top_left_point(handle, x, y);
    }

    public static function cascadeTopLeftFromPoint(int handle, double x, double y) -> array
    {
        var result;

        let result = nswindow_cascade_top_left_from_point(handle, x, y);

        return result;
    }

    public static function cascadingReferenceFrame(int handle) -> array
    {
        var result;

        let result = nswindow_cascading_reference_frame(handle);

        return result;
    }

    public static function frame(int handle) -> array
    {
        var result;

        let result = nswindow_frame(handle);

        return result;
    }

    public static function animationResizeTime(int handle, double x, double y, double width, double height) -> double
    {
        return nswindow_animation_resize_time(handle, x, y, width, height);
    }

    public static function setFrameDisplayAnimate(int handle, double x, double y, double width, double height, bool displayFlag, bool animateFlag) -> void
    {
        nswindow_set_frame_display_animate(handle, x, y, width, height, displayFlag, animateFlag);
    }

    public static function inLiveResize(int handle) -> bool
    {
        int r;

        let r = nswindow_in_live_resize(handle);

        return r == 1;
    }

    public static function resizeIncrements(int handle) -> array
    {
        var result;

        let result = nswindow_resize_increments(handle);

        return result;
    }

    public static function setResizeIncrements(int handle, double width, double height) -> void
    {
        nswindow_set_resize_increments(handle, width, height);
    }

    public static function aspectRatio(int handle) -> array
    {
        var result;

        let result = nswindow_aspect_ratio(handle);

        return result;
    }

    public static function setAspectRatio(int handle, double width, double height) -> void
    {
        nswindow_set_aspect_ratio(handle, width, height);
    }

    public static function contentResizeIncrements(int handle) -> array
    {
        var result;

        let result = nswindow_content_resize_increments(handle);

        return result;
    }

    public static function setContentResizeIncrements(int handle, double width, double height) -> void
    {
        nswindow_set_content_resize_increments(handle, width, height);
    }

    public static function contentAspectRatio(int handle) -> array
    {
        var result;

        let result = nswindow_content_aspect_ratio(handle);

        return result;
    }

    public static function setContentAspectRatio(int handle, double width, double height) -> void
    {
        nswindow_set_content_aspect_ratio(handle, width, height);
    }

    public static function viewsNeedDisplay(int handle) -> bool
    {
        int r;

        let r = nswindow_views_need_display(handle);

        return r == 1;
    }

    public static function setViewsNeedDisplay(int handle, bool viewsNeedDisplay) -> void
    {
        nswindow_set_views_need_display(handle, viewsNeedDisplay);
    }

    public static function displayIfNeeded(int handle) -> void
    {
        nswindow_display_if_needed(handle);
    }

    public static function display(int handle) -> void
    {
        nswindow_display(handle);
    }

    public static function preservesContentDuringLiveResize(int handle) -> bool
    {
        int r;

        let r = nswindow_preserves_content_during_live_resize(handle);

        return r == 1;
    }

    public static function setPreservesContentDuringLiveResize(int handle, bool preservesContentDuringLiveResize) -> void
    {
        nswindow_set_preserves_content_during_live_resize(handle, preservesContentDuringLiveResize);
    }

    public static function update(int handle) -> void
    {
        nswindow_update(handle);
    }

    public static function makeFirstResponder(int handle, int responder) -> bool
    {
        int r;

        let r = nswindow_make_first_responder(handle, responder);

        return r == 1;
    }

    public static function firstResponder(int handle) -> int
    {
        return nswindow_first_responder(handle);
    }

    public static function resizeFlags(int handle) -> int
    {
        return nswindow_resize_flags(handle);
    }

    public static function close(int handle) -> void
    {
        nswindow_close(handle);
    }

    public static function isReleasedWhenClosed(int handle) -> bool
    {
        int r;

        let r = nswindow_is_released_when_closed(handle);

        return r == 1;
    }

    public static function setReleasedWhenClosed(int handle, bool releasedWhenClosed) -> void
    {
        nswindow_set_released_when_closed(handle, releasedWhenClosed);
    }

    public static function miniaturize(int handle, int sender) -> void
    {
        nswindow_miniaturize(handle, sender);
    }

    public static function deminiaturize(int handle, int sender) -> void
    {
        nswindow_deminiaturize(handle, sender);
    }

    public static function isZoomed(int handle) -> bool
    {
        int r;

        let r = nswindow_is_zoomed(handle);

        return r == 1;
    }

    public static function zoom(int handle, int sender) -> void
    {
        nswindow_zoom(handle, sender);
    }

    public static function isMiniaturized(int handle) -> bool
    {
        int r;

        let r = nswindow_is_miniaturized(handle);

        return r == 1;
    }

    public static function tryToPerformWith(int handle, string action, int object_) -> bool
    {
        int r;

        let r = nswindow_try_to_perform_with(handle, action, object_);

        return r == 1;
    }

    public static function validRequestorForSendTypeReturnType(int handle, var sendType, var returnType) -> int
    {
        return nswindow_valid_requestor_for_send_type_return_type(handle, sendType, returnType);
    }

    public static function backgroundColor(int handle) -> int
    {
        return nswindow_background_color(handle);
    }

    public static function setBackgroundColor(int handle, int backgroundColor) -> void
    {
        nswindow_set_background_color(handle, backgroundColor);
    }

    public static function setContentBorderThicknessForEdge(int handle, double thickness, int edge) -> void
    {
        nswindow_set_content_border_thickness_for_edge(handle, thickness, edge);
    }

    public static function contentBorderThicknessForEdge(int handle, int edge) -> double
    {
        return nswindow_content_border_thickness_for_edge(handle, edge);
    }

    public static function setAutorecalculatesContentBorderThicknessForEdge(int handle, bool flag, int edge) -> void
    {
        nswindow_set_autorecalculates_content_border_thickness_for_edge(handle, flag, edge);
    }

    public static function autorecalculatesContentBorderThicknessForEdge(int handle, int edge) -> bool
    {
        int r;

        let r = nswindow_autorecalculates_content_border_thickness_for_edge(handle, edge);

        return r == 1;
    }

    public static function isMovable(int handle) -> bool
    {
        int r;

        let r = nswindow_is_movable(handle);

        return r == 1;
    }

    public static function setMovable(int handle, bool movable) -> void
    {
        nswindow_set_movable(handle, movable);
    }

    public static function isMovableByWindowBackground(int handle) -> bool
    {
        int r;

        let r = nswindow_is_movable_by_window_background(handle);

        return r == 1;
    }

    public static function setMovableByWindowBackground(int handle, bool movableByWindowBackground) -> void
    {
        nswindow_set_movable_by_window_background(handle, movableByWindowBackground);
    }

    public static function hidesOnDeactivate(int handle) -> bool
    {
        int r;

        let r = nswindow_hides_on_deactivate(handle);

        return r == 1;
    }

    public static function setHidesOnDeactivate(int handle, bool hidesOnDeactivate) -> void
    {
        nswindow_set_hides_on_deactivate(handle, hidesOnDeactivate);
    }

    public static function canHide(int handle) -> bool
    {
        int r;

        let r = nswindow_can_hide(handle);

        return r == 1;
    }

    public static function setCanHide(int handle, bool canHide) -> void
    {
        nswindow_set_can_hide(handle, canHide);
    }

    public static function center(int handle) -> void
    {
        nswindow_center(handle);
    }

    public static function makeKeyAndOrderFront(int handle, int sender) -> void
    {
        nswindow_make_key_and_order_front(handle, sender);
    }

    public static function orderFront(int handle, int sender) -> void
    {
        nswindow_order_front(handle, sender);
    }

    public static function orderBack(int handle, int sender) -> void
    {
        nswindow_order_back(handle, sender);
    }

    public static function orderOut(int handle, int sender) -> void
    {
        nswindow_order_out(handle, sender);
    }

    public static function orderWindowRelativeTo(int handle, int place, int otherWin) -> void
    {
        nswindow_order_window_relative_to(handle, place, otherWin);
    }

    public static function orderFrontRegardless(int handle) -> void
    {
        nswindow_order_front_regardless(handle);
    }

    public static function miniwindowImage(int handle) -> int
    {
        return nswindow_miniwindow_image(handle);
    }

    public static function setMiniwindowImage(int handle, int miniwindowImage) -> void
    {
        nswindow_set_miniwindow_image(handle, miniwindowImage);
    }

    public static function miniwindowTitle(int handle) -> var
    {
        var result;

        let result = nswindow_miniwindow_title(handle);

        return result;
    }

    public static function setMiniwindowTitle(int handle, var miniwindowTitle) -> void
    {
        nswindow_set_miniwindow_title(handle, miniwindowTitle);
    }

    public static function dockTile(int handle) -> int
    {
        return nswindow_dock_tile(handle);
    }

    public static function isDocumentEdited(int handle) -> bool
    {
        int r;

        let r = nswindow_is_document_edited(handle);

        return r == 1;
    }

    public static function setDocumentEdited(int handle, bool documentEdited) -> void
    {
        nswindow_set_document_edited(handle, documentEdited);
    }

    public static function isVisible(int handle) -> bool
    {
        int r;

        let r = nswindow_is_visible(handle);

        return r == 1;
    }

    public static function isKeyWindow(int handle) -> bool
    {
        int r;

        let r = nswindow_is_key_window(handle);

        return r == 1;
    }

    public static function isMainWindow(int handle) -> bool
    {
        int r;

        let r = nswindow_is_main_window(handle);

        return r == 1;
    }

    public static function canBecomeKeyWindow(int handle) -> bool
    {
        int r;

        let r = nswindow_can_become_key_window(handle);

        return r == 1;
    }

    public static function canBecomeMainWindow(int handle) -> bool
    {
        int r;

        let r = nswindow_can_become_main_window(handle);

        return r == 1;
    }

    public static function makeKeyWindow(int handle) -> void
    {
        nswindow_make_key_window(handle);
    }

    public static function makeMainWindow(int handle) -> void
    {
        nswindow_make_main_window(handle);
    }

    public static function becomeKeyWindow(int handle) -> void
    {
        nswindow_become_key_window(handle);
    }

    public static function resignKeyWindow(int handle) -> void
    {
        nswindow_resign_key_window(handle);
    }

    public static function becomeMainWindow(int handle) -> void
    {
        nswindow_become_main_window(handle);
    }

    public static function resignMainWindow(int handle) -> void
    {
        nswindow_resign_main_window(handle);
    }

    public static function worksWhenModal(int handle) -> bool
    {
        int r;

        let r = nswindow_works_when_modal(handle);

        return r == 1;
    }

    public static function preventsApplicationTerminationWhenModal(int handle) -> bool
    {
        int r;

        let r = nswindow_prevents_application_termination_when_modal(handle);

        return r == 1;
    }

    public static function setPreventsApplicationTerminationWhenModal(int handle, bool preventsApplicationTerminationWhenModal) -> void
    {
        nswindow_set_prevents_application_termination_when_modal(handle, preventsApplicationTerminationWhenModal);
    }

    public static function convertRectToScreen(int handle, double x, double y, double width, double height) -> array
    {
        var result;

        let result = nswindow_convert_rect_to_screen(handle, x, y, width, height);

        return result;
    }

    public static function convertRectFromScreen(int handle, double x, double y, double width, double height) -> array
    {
        var result;

        let result = nswindow_convert_rect_from_screen(handle, x, y, width, height);

        return result;
    }

    public static function convertPointToScreen(int handle, double x, double y) -> array
    {
        var result;

        let result = nswindow_convert_point_to_screen(handle, x, y);

        return result;
    }

    public static function convertPointFromScreen(int handle, double x, double y) -> array
    {
        var result;

        let result = nswindow_convert_point_from_screen(handle, x, y);

        return result;
    }

    public static function convertRectToBacking(int handle, double x, double y, double width, double height) -> array
    {
        var result;

        let result = nswindow_convert_rect_to_backing(handle, x, y, width, height);

        return result;
    }

    public static function convertRectFromBacking(int handle, double x, double y, double width, double height) -> array
    {
        var result;

        let result = nswindow_convert_rect_from_backing(handle, x, y, width, height);

        return result;
    }

    public static function convertPointToBacking(int handle, double x, double y) -> array
    {
        var result;

        let result = nswindow_convert_point_to_backing(handle, x, y);

        return result;
    }

    public static function convertPointFromBacking(int handle, double x, double y) -> array
    {
        var result;

        let result = nswindow_convert_point_from_backing(handle, x, y);

        return result;
    }

    public static function backingAlignedRectOptions(int handle, double x, double y, double width, double height, int options) -> array
    {
        var result;

        let result = nswindow_backing_aligned_rect_options(handle, x, y, width, height, options);

        return result;
    }

    public static function backingScaleFactor(int handle) -> double
    {
        return nswindow_backing_scale_factor(handle);
    }

    public static function performClose(int handle, int sender) -> void
    {
        nswindow_perform_close(handle, sender);
    }

    public static function performMiniaturize(int handle, int sender) -> void
    {
        nswindow_perform_miniaturize(handle, sender);
    }

    public static function performZoom(int handle, int sender) -> void
    {
        nswindow_perform_zoom(handle, sender);
    }

    public static function print_(int handle, int sender) -> void
    {
        nswindow_print(handle, sender);
    }

    public static function allowsToolTipsWhenApplicationIsInactive(int handle) -> bool
    {
        int r;

        let r = nswindow_allows_tool_tips_when_application_is_inactive(handle);

        return r == 1;
    }

    public static function setAllowsToolTipsWhenApplicationIsInactive(int handle, bool allowsToolTipsWhenApplicationIsInactive) -> void
    {
        nswindow_set_allows_tool_tips_when_application_is_inactive(handle, allowsToolTipsWhenApplicationIsInactive);
    }

    public static function backingType(int handle) -> int
    {
        return nswindow_backing_type(handle);
    }

    public static function setBackingType(int handle, int backingType) -> void
    {
        nswindow_set_backing_type(handle, backingType);
    }

    public static function level(int handle) -> int
    {
        return nswindow_level(handle);
    }

    public static function setLevel(int handle, int level) -> void
    {
        nswindow_set_level(handle, level);
    }

    public static function depthLimit(int handle) -> int
    {
        return nswindow_depth_limit(handle);
    }

    public static function setDepthLimit(int handle, int depthLimit) -> void
    {
        nswindow_set_depth_limit(handle, depthLimit);
    }

    public static function setDynamicDepthLimit(int handle, bool flag) -> void
    {
        nswindow_set_dynamic_depth_limit(handle, flag);
    }

    public static function hasDynamicDepthLimit(int handle) -> bool
    {
        int r;

        let r = nswindow_has_dynamic_depth_limit(handle);

        return r == 1;
    }

    public static function screen(int handle) -> int
    {
        return nswindow_screen(handle);
    }

    public static function deepestScreen(int handle) -> int
    {
        return nswindow_deepest_screen(handle);
    }

    public static function hasShadow(int handle) -> bool
    {
        int r;

        let r = nswindow_has_shadow(handle);

        return r == 1;
    }

    public static function setHasShadow(int handle, bool hasShadow) -> void
    {
        nswindow_set_has_shadow(handle, hasShadow);
    }

    public static function invalidateShadow(int handle) -> void
    {
        nswindow_invalidate_shadow(handle);
    }

    public static function alphaValue(int handle) -> double
    {
        return nswindow_alpha_value(handle);
    }

    public static function setAlphaValue(int handle, double alphaValue) -> void
    {
        nswindow_set_alpha_value(handle, alphaValue);
    }

    public static function isOpaque(int handle) -> bool
    {
        int r;

        let r = nswindow_is_opaque(handle);

        return r == 1;
    }

    public static function setOpaque(int handle, bool opaque) -> void
    {
        nswindow_set_opaque(handle, opaque);
    }

    public static function sharingType(int handle) -> int
    {
        return nswindow_sharing_type(handle);
    }

    public static function setSharingType(int handle, int sharingType) -> void
    {
        nswindow_set_sharing_type(handle, sharingType);
    }

    public static function allowsConcurrentViewDrawing(int handle) -> bool
    {
        int r;

        let r = nswindow_allows_concurrent_view_drawing(handle);

        return r == 1;
    }

    public static function setAllowsConcurrentViewDrawing(int handle, bool allowsConcurrentViewDrawing) -> void
    {
        nswindow_set_allows_concurrent_view_drawing(handle, allowsConcurrentViewDrawing);
    }

    public static function displaysWhenScreenProfileChanges(int handle) -> bool
    {
        int r;

        let r = nswindow_displays_when_screen_profile_changes(handle);

        return r == 1;
    }

    public static function setDisplaysWhenScreenProfileChanges(int handle, bool displaysWhenScreenProfileChanges) -> void
    {
        nswindow_set_displays_when_screen_profile_changes(handle, displaysWhenScreenProfileChanges);
    }

    public static function canBecomeVisibleWithoutLogin(int handle) -> bool
    {
        int r;

        let r = nswindow_can_become_visible_without_login(handle);

        return r == 1;
    }

    public static function setCanBecomeVisibleWithoutLogin(int handle, bool canBecomeVisibleWithoutLogin) -> void
    {
        nswindow_set_can_become_visible_without_login(handle, canBecomeVisibleWithoutLogin);
    }

    public static function collectionBehavior(int handle) -> int
    {
        return nswindow_collection_behavior(handle);
    }

    public static function setCollectionBehavior(int handle, int collectionBehavior) -> void
    {
        nswindow_set_collection_behavior(handle, collectionBehavior);
    }

    public static function animationBehavior(int handle) -> int
    {
        return nswindow_animation_behavior(handle);
    }

    public static function setAnimationBehavior(int handle, int animationBehavior) -> void
    {
        nswindow_set_animation_behavior(handle, animationBehavior);
    }

    public static function isOnActiveSpace(int handle) -> bool
    {
        int r;

        let r = nswindow_is_on_active_space(handle);

        return r == 1;
    }

    public static function toggleFullScreen(int handle, int sender) -> void
    {
        nswindow_toggle_full_screen(handle, sender);
    }

    public static function stringWithSavedFrame(int handle) -> string
    {
        var result;

        let result = nswindow_string_with_saved_frame(handle);

        return result;
    }

    public static function setFrameFromString(int handle, string string_) -> void
    {
        nswindow_set_frame_from_string(handle, string_);
    }

    public static function saveFrameUsingName(int handle, string name) -> void
    {
        nswindow_save_frame_using_name(handle, name);
    }

    public static function setFrameUsingNameForce(int handle, string name, bool force) -> bool
    {
        int r;

        let r = nswindow_set_frame_using_name_force(handle, name, force);

        return r == 1;
    }

    public static function setFrameUsingName(int handle, string name) -> bool
    {
        int r;

        let r = nswindow_set_frame_using_name(handle, name);

        return r == 1;
    }

    public static function setFrameAutosaveName(int handle, string name) -> bool
    {
        int r;

        let r = nswindow_set_frame_autosave_name(handle, name);

        return r == 1;
    }

    public static function frameAutosaveName(int handle) -> string
    {
        var result;

        let result = nswindow_frame_autosave_name(handle);

        return result;
    }

    public static function removeFrameUsingName(string name) -> void
    {
        nswindow_remove_frame_using_name(name);
    }

    public static function minSize(int handle) -> array
    {
        var result;

        let result = nswindow_min_size(handle);

        return result;
    }

    public static function setMinSize(int handle, double width, double height) -> void
    {
        nswindow_set_min_size(handle, width, height);
    }

    public static function maxSize(int handle) -> array
    {
        var result;

        let result = nswindow_max_size(handle);

        return result;
    }

    public static function setMaxSize(int handle, double width, double height) -> void
    {
        nswindow_set_max_size(handle, width, height);
    }

    public static function contentMinSize(int handle) -> array
    {
        var result;

        let result = nswindow_content_min_size(handle);

        return result;
    }

    public static function setContentMinSize(int handle, double width, double height) -> void
    {
        nswindow_set_content_min_size(handle, width, height);
    }

    public static function contentMaxSize(int handle) -> array
    {
        var result;

        let result = nswindow_content_max_size(handle);

        return result;
    }

    public static function setContentMaxSize(int handle, double width, double height) -> void
    {
        nswindow_set_content_max_size(handle, width, height);
    }

    public static function minFullScreenContentSize(int handle) -> array
    {
        var result;

        let result = nswindow_min_full_screen_content_size(handle);

        return result;
    }

    public static function setMinFullScreenContentSize(int handle, double width, double height) -> void
    {
        nswindow_set_min_full_screen_content_size(handle, width, height);
    }

    public static function maxFullScreenContentSize(int handle) -> array
    {
        var result;

        let result = nswindow_max_full_screen_content_size(handle);

        return result;
    }

    public static function setMaxFullScreenContentSize(int handle, double width, double height) -> void
    {
        nswindow_set_max_full_screen_content_size(handle, width, height);
    }

    public static function deviceDescription(int handle) -> int
    {
        return nswindow_device_description(handle);
    }

    public static function windowController(int handle) -> int
    {
        return nswindow_window_controller(handle);
    }

    public static function setWindowController(int handle, int windowController) -> void
    {
        nswindow_set_window_controller(handle, windowController);
    }

    public static function endSheet(int handle, int sheetWindow) -> void
    {
        nswindow_end_sheet(handle, sheetWindow);
    }

    public static function endSheetReturnCode(int handle, int sheetWindow, int returnCode) -> void
    {
        nswindow_end_sheet_return_code(handle, sheetWindow, returnCode);
    }

    public static function sheets(int handle) -> array
    {
        var result;

        let result = nswindow_sheets(handle);

        return result;
    }

    public static function attachedSheet(int handle) -> int
    {
        return nswindow_attached_sheet(handle);
    }

    public static function isSheet(int handle) -> bool
    {
        int r;

        let r = nswindow_is_sheet(handle);

        return r == 1;
    }

    public static function sheetParent(int handle) -> int
    {
        return nswindow_sheet_parent(handle);
    }

    public static function standardWindowButtonForStyleMask(int b, int styleMask) -> int
    {
        return nswindow_standard_window_button_for_style_mask(b, styleMask);
    }

    public static function standardWindowButton(int handle, int b) -> int
    {
        return nswindow_standard_window_button(handle, b);
    }

    public static function addChildWindowOrdered(int handle, int childWin, int place) -> void
    {
        nswindow_add_child_window_ordered(handle, childWin, place);
    }

    public static function removeChildWindow(int handle, int childWin) -> void
    {
        nswindow_remove_child_window(handle, childWin);
    }

    public static function childWindows(int handle) -> array
    {
        var result;

        let result = nswindow_child_windows(handle);

        return result;
    }

    public static function parentWindow(int handle) -> int
    {
        return nswindow_parent_window(handle);
    }

    public static function setParentWindow(int handle, int parentWindow) -> void
    {
        nswindow_set_parent_window(handle, parentWindow);
    }

    public static function appearanceSource(int handle) -> int
    {
        return nswindow_appearance_source(handle);
    }

    public static function setAppearanceSource(int handle, int appearanceSource) -> void
    {
        nswindow_set_appearance_source(handle, appearanceSource);
    }

    public static function colorSpace(int handle) -> int
    {
        return nswindow_color_space(handle);
    }

    public static function setColorSpace(int handle, int colorSpace) -> void
    {
        nswindow_set_color_space(handle, colorSpace);
    }

    public static function canRepresentDisplayGamut(int handle, int displayGamut) -> bool
    {
        int r;

        let r = nswindow_can_represent_display_gamut(handle, displayGamut);

        return r == 1;
    }

    public static function windowNumbersWithOptions(int options) -> array
    {
        var result;

        let result = nswindow_window_numbers_with_options(options);

        return result;
    }

    public static function windowNumberAtPointBelowWindowWithWindowNumber(double x, double y, int windowNumber) -> int
    {
        return nswindow_window_number_at_point_below_window_with_window_number(x, y, windowNumber);
    }

    public static function occlusionState(int handle) -> int
    {
        return nswindow_occlusion_state(handle);
    }

    public static function titlebarSeparatorStyle(int handle) -> int
    {
        return nswindow_titlebar_separator_style(handle);
    }

    public static function setTitlebarSeparatorStyle(int handle, int titlebarSeparatorStyle) -> void
    {
        nswindow_set_titlebar_separator_style(handle, titlebarSeparatorStyle);
    }

    public static function contentViewController(int handle) -> int
    {
        return nswindow_content_view_controller(handle);
    }

    public static function setContentViewController(int handle, int contentViewController) -> void
    {
        nswindow_set_content_view_controller(handle, contentViewController);
    }

    public static function windowWithContentViewController(int contentViewController) -> int
    {
        return nswindow_window_with_content_view_controller(contentViewController);
    }

    public static function performWindowDragWithEvent(int handle, int event) -> void
    {
        nswindow_perform_window_drag_with_event(handle, event);
    }

    public static function initialFirstResponder(int handle) -> int
    {
        return nswindow_initial_first_responder(handle);
    }

    public static function setInitialFirstResponder(int handle, int initialFirstResponder) -> void
    {
        nswindow_set_initial_first_responder(handle, initialFirstResponder);
    }

    public static function selectNextKeyView(int handle, int sender) -> void
    {
        nswindow_select_next_key_view(handle, sender);
    }

    public static function selectPreviousKeyView(int handle, int sender) -> void
    {
        nswindow_select_previous_key_view(handle, sender);
    }

    public static function selectKeyViewFollowingView(int handle, int view) -> void
    {
        nswindow_select_key_view_following_view(handle, view);
    }

    public static function selectKeyViewPrecedingView(int handle, int view) -> void
    {
        nswindow_select_key_view_preceding_view(handle, view);
    }

    public static function keyViewSelectionDirection(int handle) -> int
    {
        return nswindow_key_view_selection_direction(handle);
    }

    public static function defaultButtonCell(int handle) -> int
    {
        return nswindow_default_button_cell(handle);
    }

    public static function setDefaultButtonCell(int handle, int defaultButtonCell) -> void
    {
        nswindow_set_default_button_cell(handle, defaultButtonCell);
    }

    public static function disableKeyEquivalentForDefaultButtonCell(int handle) -> void
    {
        nswindow_disable_key_equivalent_for_default_button_cell(handle);
    }

    public static function enableKeyEquivalentForDefaultButtonCell(int handle) -> void
    {
        nswindow_enable_key_equivalent_for_default_button_cell(handle);
    }

    public static function autorecalculatesKeyViewLoop(int handle) -> bool
    {
        int r;

        let r = nswindow_autorecalculates_key_view_loop(handle);

        return r == 1;
    }

    public static function setAutorecalculatesKeyViewLoop(int handle, bool autorecalculatesKeyViewLoop) -> void
    {
        nswindow_set_autorecalculates_key_view_loop(handle, autorecalculatesKeyViewLoop);
    }

    public static function recalculateKeyViewLoop(int handle) -> void
    {
        nswindow_recalculate_key_view_loop(handle);
    }

    public static function toolbar(int handle) -> int
    {
        return nswindow_toolbar(handle);
    }

    public static function setToolbar(int handle, int toolbar) -> void
    {
        nswindow_set_toolbar(handle, toolbar);
    }

    public static function toggleToolbarShown(int handle, int sender) -> void
    {
        nswindow_toggle_toolbar_shown(handle, sender);
    }

    public static function runToolbarCustomizationPalette(int handle, int sender) -> void
    {
        nswindow_run_toolbar_customization_palette(handle, sender);
    }

    public static function allowsAutomaticWindowTabbing() -> bool
    {
        int r;

        let r = nswindow_allows_automatic_window_tabbing();

        return r == 1;
    }

    public static function setAllowsAutomaticWindowTabbing(bool allowsAutomaticWindowTabbing) -> void
    {
        nswindow_set_allows_automatic_window_tabbing(allowsAutomaticWindowTabbing);
    }

    public static function userTabbingPreference() -> int
    {
        return nswindow_user_tabbing_preference();
    }

    public static function tabbingMode(int handle) -> int
    {
        return nswindow_tabbing_mode(handle);
    }

    public static function setTabbingMode(int handle, int tabbingMode) -> void
    {
        nswindow_set_tabbing_mode(handle, tabbingMode);
    }

    public static function tabbingIdentifier(int handle) -> string
    {
        var result;

        let result = nswindow_tabbing_identifier(handle);

        return result;
    }

    public static function setTabbingIdentifier(int handle, string tabbingIdentifier) -> void
    {
        nswindow_set_tabbing_identifier(handle, tabbingIdentifier);
    }

    public static function selectNextTab(int handle, int sender) -> void
    {
        nswindow_select_next_tab(handle, sender);
    }

    public static function selectPreviousTab(int handle, int sender) -> void
    {
        nswindow_select_previous_tab(handle, sender);
    }

    public static function moveTabToNewWindow(int handle, int sender) -> void
    {
        nswindow_move_tab_to_new_window(handle, sender);
    }

    public static function mergeAllWindows(int handle, int sender) -> void
    {
        nswindow_merge_all_windows(handle, sender);
    }

    public static function toggleTabBar(int handle, int sender) -> void
    {
        nswindow_toggle_tab_bar(handle, sender);
    }

    public static function toggleTabOverview(int handle, int sender) -> void
    {
        nswindow_toggle_tab_overview(handle, sender);
    }

    public static function tabbedWindows(int handle) -> array
    {
        var result;

        let result = nswindow_tabbed_windows(handle);

        return result;
    }

    public static function addTabbedWindowOrdered(int handle, int window, int ordered) -> void
    {
        nswindow_add_tabbed_window_ordered(handle, window, ordered);
    }

    public static function tab(int handle) -> int
    {
        return nswindow_tab(handle);
    }

    public static function tabGroup(int handle) -> int
    {
        return nswindow_tab_group(handle);
    }

    public static function hasActiveWindowSharingSession(int handle) -> bool
    {
        int r;

        let r = nswindow_has_active_window_sharing_session(handle);

        return r == 1;
    }

    public static function windowTitlebarLayoutDirection(int handle) -> int
    {
        return nswindow_window_titlebar_layout_direction(handle);
    }

    public static function nextEventMatchingMask(int handle, int mask) -> int
    {
        return nswindow_next_event_matching_mask(handle, mask);
    }

    public static function nextEventMatchingMaskUntilDateInModeDequeue(int handle, int mask, int expiration, string mode, bool deqFlag) -> int
    {
        return nswindow_next_event_matching_mask_until_date_in_mode_dequeue(handle, mask, expiration, mode, deqFlag);
    }

    public static function discardEventsMatchingMaskBeforeEvent(int handle, int mask, int lastEvent) -> void
    {
        nswindow_discard_events_matching_mask_before_event(handle, mask, lastEvent);
    }

    public static function postEventAtStart(int handle, int event, bool flag) -> void
    {
        nswindow_post_event_at_start(handle, event, flag);
    }

    public static function sendEvent(int handle, int event) -> void
    {
        nswindow_send_event(handle, event);
    }

    public static function currentEvent(int handle) -> int
    {
        return nswindow_current_event(handle);
    }

    public static function acceptsMouseMovedEvents(int handle) -> bool
    {
        int r;

        let r = nswindow_accepts_mouse_moved_events(handle);

        return r == 1;
    }

    public static function setAcceptsMouseMovedEvents(int handle, bool acceptsMouseMovedEvents) -> void
    {
        nswindow_set_accepts_mouse_moved_events(handle, acceptsMouseMovedEvents);
    }

    public static function ignoresMouseEvents(int handle) -> bool
    {
        int r;

        let r = nswindow_ignores_mouse_events(handle);

        return r == 1;
    }

    public static function setIgnoresMouseEvents(int handle, bool ignoresMouseEvents) -> void
    {
        nswindow_set_ignores_mouse_events(handle, ignoresMouseEvents);
    }

    public static function mouseLocationOutsideOfEventStream(int handle) -> array
    {
        var result;

        let result = nswindow_mouse_location_outside_of_event_stream(handle);

        return result;
    }

    public static function disableCursorRects(int handle) -> void
    {
        nswindow_disable_cursor_rects(handle);
    }

    public static function enableCursorRects(int handle) -> void
    {
        nswindow_enable_cursor_rects(handle);
    }

    public static function discardCursorRects(int handle) -> void
    {
        nswindow_discard_cursor_rects(handle);
    }

    public static function areCursorRectsEnabled(int handle) -> bool
    {
        int r;

        let r = nswindow_are_cursor_rects_enabled(handle);

        return r == 1;
    }

    public static function invalidateCursorRectsForView(int handle, int view) -> void
    {
        nswindow_invalidate_cursor_rects_for_view(handle, view);
    }

    public static function resetCursorRects(int handle) -> void
    {
        nswindow_reset_cursor_rects(handle);
    }

    public static function beginDraggingSessionWithItemsEventSource(int handle, array items, int event, int source) -> int
    {
        return nswindow_begin_dragging_session_with_items_event_source(handle, items, event, source);
    }

    public static function registerForDraggedTypes(int handle, array newTypes) -> void
    {
        nswindow_register_for_dragged_types(handle, newTypes);
    }

    public static function unregisterDraggedTypes(int handle) -> void
    {
        nswindow_unregister_dragged_types(handle);
    }

    public static function displayLinkWithTargetSelector(int handle, int target, string selector) -> int
    {
        return nswindow_display_link_with_target_selector(handle, target, selector);
    }

    // ---- @reserved (not bound; see src/*.h) ----
    // @reserved - (instancetype)initWithCoder:(NSCoder *)coder NS_UNAVAILABLE;
    // @reserved - (NSData *)dataWithEPSInsideRect:(NSRect)rect;
    // @reserved - (NSData *)dataWithPDFInsideRect:(NSRect)rect;
    // @reserved - (void)beginSheet:(NSWindow *)sheetWindow completionHandler:(void (^ _Nullable)(NSModalResponse returnCode))handler API_AVAILABLE(macos(10.9));
    // @reserved - (void)beginCriticalSheet:(NSWindow *)sheetWindow completionHandler:(void (^ _Nullable)(NSModalResponse returnCode))handler API_AVAILABLE(macos(10.9));
    // @reserved @property BOOL showsToolbarButton API_DEPRECATED("This property has no effect", macos(10.0, API_TO_BE_DEPRECATED)); — getter
    // @reserved @property BOOL showsToolbarButton API_DEPRECATED("This property has no effect", macos(10.0, API_TO_BE_DEPRECATED)); — setter
    // @reserved - (void)transferWindowSharingToWindow:(NSWindow *)window completionHandler:(void(^)(NSError * _Nullable error))completionHandler API_AVAILABLE(macos(13.3));
    // @reserved - (void)requestSharingOfWindow:(NSWindow *)window completionHandler:(void (^)(NSError * _Nullable error))completionHandler NS_SWIFT_NAME(requestSharingOfWindow(_:completionHandler:)) API_AVAILABLE(macos(15.0));
    // @reserved - (void)requestSharingOfWindowUsingPreview:(NSImage *)image title:(NSString *)title completionHandler:(void (^)(NSError * _Nullable error))completionHandler NS_SWIFT_NAME(requestSharingOfWindow(usingPreview:title:completionHandler:)) API_AVAILABLE(macos(15.0));
    // @reserved - (void)trackEventsMatchingMask:(NSEventMask)mask timeout:(NSTimeInterval)timeout mode:(NSRunLoopMode)mode handler:(void (NS_NOESCAPE ^)(NSEvent *_Nullable event, BOOL *stop))trackingHandler API_AVAILABLE(macos(10.10));
    // @reserved - (void)dragImage:(NSImage *)image at:(NSPoint)baseLocation offset:(NSSize)initialOffset event:(NSEvent *)event pasteboard:(NSPasteboard *)pboard source:(id)sourceObj slideBack:(BOOL)slideFlag API_DEPRECATED("Use -[NSWindow beginDraggingSessionWithItems:event:source:] instead.", macos(10.0, API_TO_BE_DEPRECATED));
    // @reserved - (void)cacheImageInRect:(NSRect)rect API_DEPRECATED("This method shouldn’t be used as it doesn’t work in all drawing situations; instead, a subview should be used that implements the desired drawing behavior", macos(10.0,10.13));
    // @reserved - (void)restoreCachedImage API_DEPRECATED("This method shouldn’t be used as it doesn’t work in all drawing situations; instead, a subview should be used that implements the desired drawing behavior", macos(10.0,10.13));
    // @reserved - (void)discardCachedImage API_DEPRECATED("This method shouldn’t be used as it doesn’t work in all drawing situations; instead, a subview should be used that implements the desired drawing behavior", macos(10.0,10.13));
    // @reserved + (void)menuChanged:(NSMenu *)menu API_DEPRECATED("This method does not do anything and should not be called.", macos(10.0,10.11));
    // @reserved - (NSInteger)gState API_DEPRECATED("This method is unused and should not be called.", macos(10.0,10.10));
    // @reserved - (NSPoint)convertBaseToScreen:(NSPoint)point API_DEPRECATED("Use -convertRectToScreen: or -convertPointToScreen: instead", macos(10.0,10.7));
    // @reserved - (NSPoint)convertScreenToBase:(NSPoint)point API_DEPRECATED("Use -convertRectFromScreen or -convertPointFromScreen: instead", macos(10.0,10.7));
    // @reserved - (CGFloat)userSpaceScaleFactor API_DEPRECATED("Use -convertRectToBacking: and -backingScaleFactor instead", macos(10.4,10.7));
    // @reserved - (void)useOptimizedDrawing:(BOOL)flag API_DEPRECATED("This method does not do anything and should not be called.", macos(10.0,10.10));
    // @reserved - (BOOL)canStoreColor API_DEPRECATED("This method does not do anything and should not be called.", macos(10.0,10.10));
    // @reserved - (void)disableFlushWindow API_DEPRECATED("Use +[NSAnimationContext runAnimationGroup:completionHandler:] to perform atomic updates across runloop invocations.", macos(10.0,10.14));
    // @reserved - (void)enableFlushWindow API_DEPRECATED("Use +[NSAnimationContext runAnimationGroup:completionHandler:] to perform atomic updates across runloop invocations.", macos(10.0,10.14));
    // @reserved @property (getter=isFlushWindowDisabled, readonly) BOOL flushWindowDisabled API_DEPRECATED("Use +[NSAnimationContext runAnimationGroup:completionHandler:] to perform atomic updates across runloop invocations.", macos(10.0,10.14)); — getter
    // @reserved - (void)flushWindow API_DEPRECATED("Allow AppKit's automatic deferred display mechanism to take care of flushing any graphics contexts as needed.", macos(10.0,10.14));
    // @reserved - (void)flushWindowIfNeeded API_DEPRECATED("Allow AppKit's automatic deferred display mechanism to take care of flushing any graphics contexts as needed.", macos(10.0,10.14));
    // @reserved @property (getter=isAutodisplay) BOOL autodisplay API_DEPRECATED("Use +[NSAnimationContext runAnimationGroup:completionHandler:] to temporarily prevent AppKit's automatic deferred display mechanism from drawing.", macos(10.0,10.14)); — getter
    // @reserved @property (getter=isAutodisplay) BOOL autodisplay API_DEPRECATED("Use +[NSAnimationContext runAnimationGroup:completionHandler:] to temporarily prevent AppKit's automatic deferred display mechanism from drawing.", macos(10.0,10.14)); — setter
    // @reserved @property (nullable, readonly, strong) NSGraphicsContext *graphicsContext API_DEPRECATED("Add instances of NSView to display content in a window.", macos(10.0,10.14)); — getter
    // @reserved @property (getter=isOneShot) BOOL oneShot API_DEPRECATED("This property does not do anything and should not be used", macos(10.0,10.14)); — getter
    // @reserved @property (getter=isOneShot) BOOL oneShot API_DEPRECATED("This property does not do anything and should not be used", macos(10.0,10.14)); — setter
    // @reserved @property NSWindowBackingLocation preferredBackingLocation API_DEPRECATED("This property does not do anything and should not be used", macos(10.5,10.14)); — getter
    // @reserved @property NSWindowBackingLocation preferredBackingLocation API_DEPRECATED("This property does not do anything and should not be used", macos(10.5,10.14)); — setter
    // @reserved @property (readonly) NSWindowBackingLocation backingLocation API_DEPRECATED("This property does not do anything and should not be used", macos(10.5,10.14)); — getter
    // @reserved @property BOOL showsResizeIndicator API_DEPRECATED("This property does not do anything and should not be used.", macos(10.0,15.0)); — getter
    // @reserved @property BOOL showsResizeIndicator API_DEPRECATED("This property does not do anything and should not be used.", macos(10.0,15.0)); — setter
    // @reserved - (nullable NSWindow *)initWithWindowRef:(void * WindowRef)windowRef API_DEPRECATED("This method should not be used.", macos(10.0,15.0));
    // @reserved @property (readonly) void * WindowRef windowRef NS_RETURNS_INNER_POINTER API_DEPRECATED("This method should not be used.", macos(10.0,15.0)); — getter
    // @reserved - (void)disableScreenUpdatesUntilFlush API_DEPRECATED("This method does not do anything and should not be called.", macos(10.0,15.0));

}
