// GENERATED by scripts/gen-zep.php — DO NOT EDIT.
// Source of truth: the @zep/@zep-construct/@reserved annotations in src/*.h.

namespace AppKit\NS\NSGridRow;

class NSGridRow
{
    public static function init() -> int
    {
        return nsgridrow_init();
    }

    public static function gridView(int handle) -> int
    {
        return nsgridrow_grid_view(handle);
    }

    public static function numberOfCells(int handle) -> int
    {
        return nsgridrow_number_of_cells(handle);
    }

    public static function cellAtIndex(int handle, int index) -> int
    {
        return nsgridrow_cell_at_index(handle, index);
    }

    public static function yPlacement(int handle) -> int
    {
        return nsgridrow_y_placement(handle);
    }

    public static function setYPlacement(int handle, int yPlacement) -> void
    {
        nsgridrow_set_y_placement(handle, yPlacement);
    }

    public static function rowAlignment(int handle) -> int
    {
        return nsgridrow_row_alignment(handle);
    }

    public static function setRowAlignment(int handle, int rowAlignment) -> void
    {
        nsgridrow_set_row_alignment(handle, rowAlignment);
    }

    public static function height(int handle) -> double
    {
        return nsgridrow_height(handle);
    }

    public static function setHeight(int handle, double height) -> void
    {
        nsgridrow_set_height(handle, height);
    }

    public static function topPadding(int handle) -> double
    {
        return nsgridrow_top_padding(handle);
    }

    public static function setTopPadding(int handle, double topPadding) -> void
    {
        nsgridrow_set_top_padding(handle, topPadding);
    }

    public static function bottomPadding(int handle) -> double
    {
        return nsgridrow_bottom_padding(handle);
    }

    public static function setBottomPadding(int handle, double bottomPadding) -> void
    {
        nsgridrow_set_bottom_padding(handle, bottomPadding);
    }

    public static function isHidden(int handle) -> bool
    {
        int r;

        let r = nsgridrow_is_hidden(handle);

        return r == 1;
    }

    public static function setHidden(int handle, bool hidden) -> void
    {
        nsgridrow_set_hidden(handle, hidden);
    }

    public static function mergeCellsInRange(int handle, int location, int length) -> void
    {
        nsgridrow_merge_cells_in_range(handle, location, length);
    }
}
