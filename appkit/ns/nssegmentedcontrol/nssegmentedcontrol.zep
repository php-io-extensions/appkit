// GENERATED by scripts/gen-zep.php — DO NOT EDIT.
// Source of truth: the @zep/@zep-construct/@reserved annotations in src/*.h.

namespace AppKit\NS\NSSegmentedControl;

class NSSegmentedControl
{
    public static function initWithFrame(double x, double y, double width, double height) -> int
    {
        return nssegmentedcontrol_init_with_frame(x, y, width, height);
    }

    public static function segmentCount(int handle) -> int
    {
        return nssegmentedcontrol_segment_count(handle);
    }

    public static function setSegmentCount(int handle, int segmentCount) -> void
    {
        nssegmentedcontrol_set_segment_count(handle, segmentCount);
    }

    public static function selectedSegment(int handle) -> int
    {
        return nssegmentedcontrol_selected_segment(handle);
    }

    public static function setSelectedSegment(int handle, int selectedSegment) -> void
    {
        nssegmentedcontrol_set_selected_segment(handle, selectedSegment);
    }

    public static function selectSegmentWithTag(int handle, int tag) -> bool
    {
        int r;

        let r = nssegmentedcontrol_select_segment_with_tag(handle, tag);

        return r == 1;
    }

    public static function setWidthForSegment(int handle, double width, int segment) -> void
    {
        nssegmentedcontrol_set_width_for_segment(handle, width, segment);
    }

    public static function widthForSegment(int handle, int segment) -> double
    {
        return nssegmentedcontrol_width_for_segment(handle, segment);
    }

    public static function setImageForSegment(int handle, int image, int segment) -> void
    {
        nssegmentedcontrol_set_image_for_segment(handle, image, segment);
    }

    public static function imageForSegment(int handle, int segment) -> int
    {
        return nssegmentedcontrol_image_for_segment(handle, segment);
    }

    public static function setImageScalingForSegment(int handle, int scaling, int segment) -> void
    {
        nssegmentedcontrol_set_image_scaling_for_segment(handle, scaling, segment);
    }

    public static function imageScalingForSegment(int handle, int segment) -> int
    {
        return nssegmentedcontrol_image_scaling_for_segment(handle, segment);
    }

    public static function setLabelForSegment(int handle, string label, int segment) -> void
    {
        nssegmentedcontrol_set_label_for_segment(handle, label, segment);
    }

    public static function labelForSegment(int handle, int segment) -> var
    {
        var result;

        let result = nssegmentedcontrol_label_for_segment(handle, segment);

        return result;
    }

    public static function setMenuForSegment(int handle, int menu, int segment) -> void
    {
        nssegmentedcontrol_set_menu_for_segment(handle, menu, segment);
    }

    public static function menuForSegment(int handle, int segment) -> int
    {
        return nssegmentedcontrol_menu_for_segment(handle, segment);
    }

    public static function setSelectedForSegment(int handle, bool selected, int segment) -> void
    {
        nssegmentedcontrol_set_selected_for_segment(handle, selected, segment);
    }

    public static function isSelectedForSegment(int handle, int segment) -> bool
    {
        int r;

        let r = nssegmentedcontrol_is_selected_for_segment(handle, segment);

        return r == 1;
    }

    public static function setEnabledForSegment(int handle, bool enabled, int segment) -> void
    {
        nssegmentedcontrol_set_enabled_for_segment(handle, enabled, segment);
    }

    public static function isEnabledForSegment(int handle, int segment) -> bool
    {
        int r;

        let r = nssegmentedcontrol_is_enabled_for_segment(handle, segment);

        return r == 1;
    }

    public static function setToolTipForSegment(int handle, var toolTip, int segment) -> void
    {
        nssegmentedcontrol_set_tool_tip_for_segment(handle, toolTip, segment);
    }

    public static function toolTipForSegment(int handle, int segment) -> var
    {
        var result;

        let result = nssegmentedcontrol_tool_tip_for_segment(handle, segment);

        return result;
    }

    public static function setTagForSegment(int handle, int tag, int segment) -> void
    {
        nssegmentedcontrol_set_tag_for_segment(handle, tag, segment);
    }

    public static function tagForSegment(int handle, int segment) -> int
    {
        return nssegmentedcontrol_tag_for_segment(handle, segment);
    }

    public static function setShowsMenuIndicatorForSegment(int handle, bool showsMenuIndicator, int segment) -> void
    {
        nssegmentedcontrol_set_shows_menu_indicator_for_segment(handle, showsMenuIndicator, segment);
    }

    public static function showsMenuIndicatorForSegment(int handle, int segment) -> bool
    {
        int r;

        let r = nssegmentedcontrol_shows_menu_indicator_for_segment(handle, segment);

        return r == 1;
    }

    public static function segmentStyle(int handle) -> int
    {
        return nssegmentedcontrol_segment_style(handle);
    }

    public static function setSegmentStyle(int handle, int segmentStyle) -> void
    {
        nssegmentedcontrol_set_segment_style(handle, segmentStyle);
    }

    public static function isSpringLoaded(int handle) -> bool
    {
        int r;

        let r = nssegmentedcontrol_is_spring_loaded(handle);

        return r == 1;
    }

    public static function setSpringLoaded(int handle, bool springLoaded) -> void
    {
        nssegmentedcontrol_set_spring_loaded(handle, springLoaded);
    }

    public static function trackingMode(int handle) -> int
    {
        return nssegmentedcontrol_tracking_mode(handle);
    }

    public static function setTrackingMode(int handle, int trackingMode) -> void
    {
        nssegmentedcontrol_set_tracking_mode(handle, trackingMode);
    }

    public static function doubleValueForSelectedSegment(int handle) -> double
    {
        return nssegmentedcontrol_double_value_for_selected_segment(handle);
    }

    public static function selectedSegmentBezelColor(int handle) -> int
    {
        return nssegmentedcontrol_selected_segment_bezel_color(handle);
    }

    public static function setSelectedSegmentBezelColor(int handle, int selectedSegmentBezelColor) -> void
    {
        nssegmentedcontrol_set_selected_segment_bezel_color(handle, selectedSegmentBezelColor);
    }

    public static function indexOfSelectedItem(int handle) -> int
    {
        return nssegmentedcontrol_index_of_selected_item(handle);
    }

    public static function setAlignmentForSegment(int handle, int alignment, int segment) -> void
    {
        nssegmentedcontrol_set_alignment_for_segment(handle, alignment, segment);
    }

    public static function alignmentForSegment(int handle, int segment) -> int
    {
        return nssegmentedcontrol_alignment_for_segment(handle, segment);
    }

    public static function segmentDistribution(int handle) -> int
    {
        return nssegmentedcontrol_segment_distribution(handle);
    }

    public static function setSegmentDistribution(int handle, int segmentDistribution) -> void
    {
        nssegmentedcontrol_set_segment_distribution(handle, segmentDistribution);
    }

    public static function compressWithPrioritizedCompressionOptions(int handle, array prioritizedOptions) -> void
    {
        nssegmentedcontrol_compress_with_prioritized_compression_options(handle, prioritizedOptions);
    }

    public static function minimumSizeWithPrioritizedCompressionOptions(int handle, array prioritizedOptions) -> array
    {
        var result;

        let result = nssegmentedcontrol_minimum_size_with_prioritized_compression_options(handle, prioritizedOptions);

        return result;
    }

    public static function activeCompressionOptions(int handle) -> int
    {
        return nssegmentedcontrol_active_compression_options(handle);
    }

    public static function segmentedControlWithLabelsTrackingModeTargetAction(array labels, int trackingMode, int target, var action) -> int
    {
        return nssegmentedcontrol_segmented_control_with_labels_tracking_mode_target_action(labels, trackingMode, target, action);
    }

    public static function segmentedControlWithImagesTrackingModeTargetAction(array images, int trackingMode, int target, var action) -> int
    {
        return nssegmentedcontrol_segmented_control_with_images_tracking_mode_target_action(images, trackingMode, target, action);
    }
}
