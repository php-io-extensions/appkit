// GENERATED by scripts/gen-zep.php — DO NOT EDIT.
// Source of truth: the @zep/@zep-construct/@reserved annotations in src/*.h.

namespace AppKit\NS\NSToolbarItemGroup;

class NSToolbarItemGroup
{
    public static function initWithItemIdentifier(string itemIdentifier) -> int
    {
        return nstoolbaritemgroup_init_with_item_identifier(itemIdentifier);
    }

    public static function groupWithItemIdentifierTitlesSelectionModeLabelsTargetAction(string itemIdentifier, array titles, int selectionMode, var labels, int target, var action) -> int
    {
        return nstoolbaritemgroup_group_with_item_identifier_titles_selection_mode_labels_target_action(itemIdentifier, titles, selectionMode, labels, target, action);
    }

    public static function groupWithItemIdentifierImagesSelectionModeLabelsTargetAction(string itemIdentifier, array images, int selectionMode, var labels, int target, var action) -> int
    {
        return nstoolbaritemgroup_group_with_item_identifier_images_selection_mode_labels_target_action(itemIdentifier, images, selectionMode, labels, target, action);
    }

    public static function subitems(int handle) -> array
    {
        var result;

        let result = nstoolbaritemgroup_subitems(handle);

        return result;
    }

    public static function setSubitems(int handle, array subitems) -> void
    {
        nstoolbaritemgroup_set_subitems(handle, subitems);
    }

    public static function controlRepresentation(int handle) -> int
    {
        return nstoolbaritemgroup_control_representation(handle);
    }

    public static function setControlRepresentation(int handle, int controlRepresentation) -> void
    {
        nstoolbaritemgroup_set_control_representation(handle, controlRepresentation);
    }

    public static function selectionMode(int handle) -> int
    {
        return nstoolbaritemgroup_selection_mode(handle);
    }

    public static function setSelectionMode(int handle, int selectionMode) -> void
    {
        nstoolbaritemgroup_set_selection_mode(handle, selectionMode);
    }

    public static function selectedIndex(int handle) -> int
    {
        return nstoolbaritemgroup_selected_index(handle);
    }

    public static function setSelectedIndex(int handle, int selectedIndex) -> void
    {
        nstoolbaritemgroup_set_selected_index(handle, selectedIndex);
    }

    public static function setSelectedAtIndex(int handle, bool selected, int index) -> void
    {
        nstoolbaritemgroup_set_selected_at_index(handle, selected, index);
    }

    public static function isSelectedAtIndex(int handle, int index) -> bool
    {
        int r;

        let r = nstoolbaritemgroup_is_selected_at_index(handle, index);

        return r == 1;
    }

    // ---- @reserved (not bound; see src/*.h) ----
    // @reserved + (instancetype)groupWithItemIdentifier:(NSToolbarItemIdentifier)itemIdentifier images:(NSArray<UIImage *> *)images selectionMode:(NSToolbarItemGroupSelectionMode)selectionMode labels:(nullable NSArray<NSString *> *)labels target:(nullable id)target action:(nullable SEL)action — iOS duplicate

}
