// GENERATED by scripts/gen-zep.php — DO NOT EDIT.
// Source of truth: the @zep/@zep-construct/@reserved annotations in src/*.h.

namespace AppKit\NS\NSPopUpButton;

class NSPopUpButton
{
    public static function popUpButtonWithMenuTargetAction(int menu, int target, var action) -> int
    {
        return nspopupbutton_pop_up_button_with_menu_target_action(menu, target, action);
    }

    public static function pullDownButtonWithTitleMenu(string title, int menu) -> int
    {
        return nspopupbutton_pull_down_button_with_title_menu(title, menu);
    }

    public static function pullDownButtonWithImageMenu(int image, int menu) -> int
    {
        return nspopupbutton_pull_down_button_with_image_menu(image, menu);
    }

    public static function pullDownButtonWithTitleImageMenu(string title, int image, int menu) -> int
    {
        return nspopupbutton_pull_down_button_with_title_image_menu(title, image, menu);
    }

    public static function initWithFramePullsDown(double x, double y, double width, double height, bool flag) -> int
    {
        return nspopupbutton_init_with_frame_pulls_down(x, y, width, height, flag);
    }

    public static function menu(int handle) -> int
    {
        return nspopupbutton_menu(handle);
    }

    public static function setMenu(int handle, int menu) -> void
    {
        nspopupbutton_set_menu(handle, menu);
    }

    public static function pullsDown(int handle) -> bool
    {
        int r;

        let r = nspopupbutton_pulls_down(handle);

        return r == 1;
    }

    public static function setPullsDown(int handle, bool pullsDown) -> void
    {
        nspopupbutton_set_pulls_down(handle, pullsDown);
    }

    public static function autoenablesItems(int handle) -> bool
    {
        int r;

        let r = nspopupbutton_autoenables_items(handle);

        return r == 1;
    }

    public static function setAutoenablesItems(int handle, bool autoenablesItems) -> void
    {
        nspopupbutton_set_autoenables_items(handle, autoenablesItems);
    }

    public static function preferredEdge(int handle) -> int
    {
        return nspopupbutton_preferred_edge(handle);
    }

    public static function setPreferredEdge(int handle, int preferredEdge) -> void
    {
        nspopupbutton_set_preferred_edge(handle, preferredEdge);
    }

    public static function usesItemFromMenu(int handle) -> bool
    {
        int r;

        let r = nspopupbutton_uses_item_from_menu(handle);

        return r == 1;
    }

    public static function setUsesItemFromMenu(int handle, bool usesItemFromMenu) -> void
    {
        nspopupbutton_set_uses_item_from_menu(handle, usesItemFromMenu);
    }

    public static function altersStateOfSelectedItem(int handle) -> bool
    {
        int r;

        let r = nspopupbutton_alters_state_of_selected_item(handle);

        return r == 1;
    }

    public static function setAltersStateOfSelectedItem(int handle, bool altersStateOfSelectedItem) -> void
    {
        nspopupbutton_set_alters_state_of_selected_item(handle, altersStateOfSelectedItem);
    }

    public static function addItemWithTitle(int handle, string title) -> void
    {
        nspopupbutton_add_item_with_title(handle, title);
    }

    public static function addItemsWithTitles(int handle, array itemTitles) -> void
    {
        nspopupbutton_add_items_with_titles(handle, itemTitles);
    }

    public static function insertItemWithTitleAtIndex(int handle, string title, int index) -> void
    {
        nspopupbutton_insert_item_with_title_at_index(handle, title, index);
    }

    public static function removeItemWithTitle(int handle, string title) -> void
    {
        nspopupbutton_remove_item_with_title(handle, title);
    }

    public static function removeItemAtIndex(int handle, int index) -> void
    {
        nspopupbutton_remove_item_at_index(handle, index);
    }

    public static function removeAllItems(int handle) -> void
    {
        nspopupbutton_remove_all_items(handle);
    }

    public static function itemArray(int handle) -> array
    {
        var result;

        let result = nspopupbutton_item_array(handle);

        return result;
    }

    public static function numberOfItems(int handle) -> int
    {
        return nspopupbutton_number_of_items(handle);
    }

    public static function indexOfItem(int handle, int item) -> int
    {
        return nspopupbutton_index_of_item(handle, item);
    }

    public static function indexOfItemWithTitle(int handle, string title) -> int
    {
        return nspopupbutton_index_of_item_with_title(handle, title);
    }

    public static function indexOfItemWithTag(int handle, int tag) -> int
    {
        return nspopupbutton_index_of_item_with_tag(handle, tag);
    }

    public static function indexOfItemWithRepresentedObject(int handle, int obj) -> int
    {
        return nspopupbutton_index_of_item_with_represented_object(handle, obj);
    }

    public static function indexOfItemWithTargetAndAction(int handle, int target, var action) -> int
    {
        return nspopupbutton_index_of_item_with_target_and_action(handle, target, action);
    }

    public static function itemAtIndex(int handle, int index) -> int
    {
        return nspopupbutton_item_at_index(handle, index);
    }

    public static function itemWithTitle(int handle, string title) -> int
    {
        return nspopupbutton_item_with_title(handle, title);
    }

    public static function lastItem(int handle) -> int
    {
        return nspopupbutton_last_item(handle);
    }

    public static function selectItem(int handle, int item) -> void
    {
        nspopupbutton_select_item(handle, item);
    }

    public static function selectItemAtIndex(int handle, int index) -> void
    {
        nspopupbutton_select_item_at_index(handle, index);
    }

    public static function selectItemWithTitle(int handle, string title) -> void
    {
        nspopupbutton_select_item_with_title(handle, title);
    }

    public static function selectItemWithTag(int handle, int tag) -> bool
    {
        int r;

        let r = nspopupbutton_select_item_with_tag(handle, tag);

        return r == 1;
    }

    public static function setTitle(int handle, string title) -> void
    {
        nspopupbutton_set_title(handle, title);
    }

    public static function selectedItem(int handle) -> int
    {
        return nspopupbutton_selected_item(handle);
    }

    public static function indexOfSelectedItem(int handle) -> int
    {
        return nspopupbutton_index_of_selected_item(handle);
    }

    public static function selectedTag(int handle) -> int
    {
        return nspopupbutton_selected_tag(handle);
    }

    public static function synchronizeTitleAndSelectedItem(int handle) -> void
    {
        nspopupbutton_synchronize_title_and_selected_item(handle);
    }

    public static function itemTitleAtIndex(int handle, int index) -> string
    {
        var result;

        let result = nspopupbutton_item_title_at_index(handle, index);

        return result;
    }

    public static function itemTitles(int handle) -> array
    {
        var result;

        let result = nspopupbutton_item_titles(handle);

        return result;
    }

    public static function titleOfSelectedItem(int handle) -> var
    {
        var result;

        let result = nspopupbutton_title_of_selected_item(handle);

        return result;
    }
}
