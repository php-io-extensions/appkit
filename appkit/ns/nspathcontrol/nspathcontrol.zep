// GENERATED by scripts/gen-zep.php — DO NOT EDIT.
// Source of truth: the @zep/@zep-construct/@reserved annotations in src/*.h.

namespace AppKit\NS\NSPathControl;

class NSPathControl
{
    public static function initWithFrame(double x, double y, double width, double height) -> int
    {
        return nspathcontrol_init_with_frame(x, y, width, height);
    }

    public static function isEditable(int handle) -> bool
    {
        int r;

        let r = nspathcontrol_is_editable(handle);

        return r == 1;
    }

    public static function setEditable(int handle, bool editable) -> void
    {
        nspathcontrol_set_editable(handle, editable);
    }

    public static function allowedTypes(int handle) -> var
    {
        var result;

        let result = nspathcontrol_allowed_types(handle);

        return result;
    }

    public static function setAllowedTypes(int handle, var allowedTypes) -> void
    {
        nspathcontrol_set_allowed_types(handle, allowedTypes);
    }

    public static function placeholderString(int handle) -> var
    {
        var result;

        let result = nspathcontrol_placeholder_string(handle);

        return result;
    }

    public static function setPlaceholderString(int handle, var placeholderString) -> void
    {
        nspathcontrol_set_placeholder_string(handle, placeholderString);
    }

    public static function Url(int handle) -> int
    {
        return nspathcontrol_url(handle);
    }

    public static function setURL(int handle, int Url) -> void
    {
        nspathcontrol_set_url(handle, Url);
    }

    public static function doubleAction(int handle) -> var
    {
        var result;

        let result = nspathcontrol_double_action(handle);

        return result;
    }

    public static function setDoubleAction(int handle, var doubleAction) -> void
    {
        nspathcontrol_set_double_action(handle, doubleAction);
    }

    public static function pathStyle(int handle) -> int
    {
        return nspathcontrol_path_style(handle);
    }

    public static function setPathStyle(int handle, int pathStyle) -> void
    {
        nspathcontrol_set_path_style(handle, pathStyle);
    }

    public static function clickedPathItem(int handle) -> int
    {
        return nspathcontrol_clicked_path_item(handle);
    }

    public static function pathItems(int handle) -> array
    {
        var result;

        let result = nspathcontrol_path_items(handle);

        return result;
    }

    public static function setPathItems(int handle, array pathItems) -> void
    {
        nspathcontrol_set_path_items(handle, pathItems);
    }

    public static function backgroundColor(int handle) -> int
    {
        return nspathcontrol_background_color(handle);
    }

    public static function setBackgroundColor(int handle, int backgroundColor) -> void
    {
        nspathcontrol_set_background_color(handle, backgroundColor);
    }

    public static function delegate(int handle) -> int
    {
        return nspathcontrol_delegate(handle);
    }

    public static function setDelegate(int handle, int delegate) -> void
    {
        nspathcontrol_set_delegate(handle, delegate);
    }

    public static function setDraggingSourceOperationMaskForLocal(int handle, int mask, bool isLocal) -> void
    {
        nspathcontrol_set_dragging_source_operation_mask_for_local(handle, mask, isLocal);
    }

    public static function menu(int handle) -> int
    {
        return nspathcontrol_menu(handle);
    }

    public static function setMenu(int handle, int menu) -> void
    {
        nspathcontrol_set_menu(handle, menu);
    }

    // ---- @reserved (not bound; see src/*.h) ----
    // @reserved @property (nullable, copy) NSAttributedString *placeholderAttributedString — getter
    // @reserved @property (nullable, copy) NSAttributedString *placeholderAttributedString — setter
    // @reserved - (nullable NSPathComponentCell *)clickedPathComponentCell API_DEPRECATED
    // @reserved - (NSArray<NSPathComponentCell *> *)pathComponentCells API_DEPRECATED
    // @reserved - (void)setPathComponentCells:(NSArray<NSPathComponentCell *> *)cells API_DEPRECATED

}
