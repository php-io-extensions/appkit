// GENERATED by scripts/gen-zep.php — DO NOT EDIT.
// Source of truth: the @zep/@zep-construct/@reserved annotations in src/*.h.

namespace AppKit\NS\NSURL;

class NSURL
{
    public static function fileURLWithPath(string path) -> int
    {
        return nsurl_file_url_with_path(path);
    }

    public static function URLWithString(string str) -> int
    {
        return nsurl_url_with_string(str);
    }

    public static function absoluteString(int handle) -> var
    {
        var result;

        let result = nsurl_absolute_string(handle);

        return result;
    }

    public static function path(int handle) -> var
    {
        var result;

        let result = nsurl_path(handle);

        return result;
    }

    public static function isFileURL(int handle) -> bool
    {
        int r;

        let r = nsurl_is_file_url(handle);

        return r == 1;
    }
}
