// GENERATED by scripts/gen-zep.php — DO NOT EDIT.
// Source of truth: the @zep/@zep-construct/@reserved annotations in src/*.h.

namespace AppKit\NS\NSControl;

class NSControl
{
    public static function initWithFrame(double x, double y, double width, double height) -> int
    {
        return nscontrol_init_with_frame(x, y, width, height);
    }

    public static function target(int handle) -> int
    {
        return nscontrol_target(handle);
    }

    public static function setTarget(int handle, int target) -> void
    {
        nscontrol_set_target(handle, target);
    }

    public static function action(int handle) -> var
    {
        var result;

        let result = nscontrol_action(handle);

        return result;
    }

    public static function setAction(int handle, var action) -> void
    {
        nscontrol_set_action(handle, action);
    }

    public static function tag(int handle) -> int
    {
        return nscontrol_tag(handle);
    }

    public static function setTag(int handle, int tag) -> void
    {
        nscontrol_set_tag(handle, tag);
    }

    public static function ignoresMultiClick(int handle) -> bool
    {
        int r;

        let r = nscontrol_ignores_multi_click(handle);

        return r == 1;
    }

    public static function setIgnoresMultiClick(int handle, bool ignoresMultiClick) -> void
    {
        nscontrol_set_ignores_multi_click(handle, ignoresMultiClick);
    }

    public static function isContinuous(int handle) -> bool
    {
        int r;

        let r = nscontrol_is_continuous(handle);

        return r == 1;
    }

    public static function setContinuous(int handle, bool continuous) -> void
    {
        nscontrol_set_continuous(handle, continuous);
    }

    public static function isEnabled(int handle) -> bool
    {
        int r;

        let r = nscontrol_is_enabled(handle);

        return r == 1;
    }

    public static function setEnabled(int handle, bool enabled) -> void
    {
        nscontrol_set_enabled(handle, enabled);
    }

    public static function refusesFirstResponder(int handle) -> bool
    {
        int r;

        let r = nscontrol_refuses_first_responder(handle);

        return r == 1;
    }

    public static function setRefusesFirstResponder(int handle, bool refusesFirstResponder) -> void
    {
        nscontrol_set_refuses_first_responder(handle, refusesFirstResponder);
    }

    public static function isHighlighted(int handle) -> bool
    {
        int r;

        let r = nscontrol_is_highlighted(handle);

        return r == 1;
    }

    public static function setHighlighted(int handle, bool highlighted) -> void
    {
        nscontrol_set_highlighted(handle, highlighted);
    }

    public static function controlSize(int handle) -> int
    {
        return nscontrol_control_size(handle);
    }

    public static function setControlSize(int handle, int controlSize) -> void
    {
        nscontrol_set_control_size(handle, controlSize);
    }

    public static function formatter(int handle) -> int
    {
        return nscontrol_formatter(handle);
    }

    public static function setFormatter(int handle, int formatter) -> void
    {
        nscontrol_set_formatter(handle, formatter);
    }

    public static function objectValue(int handle) -> int
    {
        return nscontrol_object_value(handle);
    }

    public static function setObjectValue(int handle, int objectValue) -> void
    {
        nscontrol_set_object_value(handle, objectValue);
    }

    public static function stringValue(int handle) -> string
    {
        var result;

        let result = nscontrol_string_value(handle);

        return result;
    }

    public static function setStringValue(int handle, string stringValue) -> void
    {
        nscontrol_set_string_value(handle, stringValue);
    }

    public static function intValue(int handle) -> int
    {
        return nscontrol_int_value(handle);
    }

    public static function setIntValue(int handle, int intValue) -> void
    {
        nscontrol_set_int_value(handle, intValue);
    }

    public static function integerValue(int handle) -> int
    {
        return nscontrol_integer_value(handle);
    }

    public static function setIntegerValue(int handle, int integerValue) -> void
    {
        nscontrol_set_integer_value(handle, integerValue);
    }

    public static function floatValue(int handle) -> double
    {
        return nscontrol_float_value(handle);
    }

    public static function setFloatValue(int handle, double floatValue) -> void
    {
        nscontrol_set_float_value(handle, floatValue);
    }

    public static function doubleValue(int handle) -> double
    {
        return nscontrol_double_value(handle);
    }

    public static function setDoubleValue(int handle, double doubleValue) -> void
    {
        nscontrol_set_double_value(handle, doubleValue);
    }

    public static function sizeThatFits(int handle, double width, double height) -> array
    {
        var result;

        let result = nscontrol_size_that_fits(handle, width, height);

        return result;
    }

    public static function sizeToFit(int handle) -> void
    {
        nscontrol_size_to_fit(handle);
    }

    public static function sendActionOn(int handle, int mask) -> int
    {
        return nscontrol_send_action_on(handle, mask);
    }

    public static function sendActionTo(int handle, var action, int target) -> bool
    {
        int r;

        let r = nscontrol_send_action_to(handle, action, target);

        return r == 1;
    }

    public static function takeIntValueFrom(int handle, int sender) -> void
    {
        nscontrol_take_int_value_from(handle, sender);
    }

    public static function takeFloatValueFrom(int handle, int sender) -> void
    {
        nscontrol_take_float_value_from(handle, sender);
    }

    public static function takeDoubleValueFrom(int handle, int sender) -> void
    {
        nscontrol_take_double_value_from(handle, sender);
    }

    public static function takeStringValueFrom(int handle, int sender) -> void
    {
        nscontrol_take_string_value_from(handle, sender);
    }

    public static function takeObjectValueFrom(int handle, int sender) -> void
    {
        nscontrol_take_object_value_from(handle, sender);
    }

    public static function takeIntegerValueFrom(int handle, int sender) -> void
    {
        nscontrol_take_integer_value_from(handle, sender);
    }

    public static function mouseDown(int handle, int event) -> void
    {
        nscontrol_mouse_down(handle, event);
    }

    public static function performClick(int handle, int sender) -> void
    {
        nscontrol_perform_click(handle, sender);
    }

    public static function font(int handle) -> int
    {
        return nscontrol_font(handle);
    }

    public static function setFont(int handle, int font) -> void
    {
        nscontrol_set_font(handle, font);
    }

    public static function usesSingleLineMode(int handle) -> bool
    {
        int r;

        let r = nscontrol_uses_single_line_mode(handle);

        return r == 1;
    }

    public static function setUsesSingleLineMode(int handle, bool usesSingleLineMode) -> void
    {
        nscontrol_set_uses_single_line_mode(handle, usesSingleLineMode);
    }

    public static function lineBreakMode(int handle) -> int
    {
        return nscontrol_line_break_mode(handle);
    }

    public static function setLineBreakMode(int handle, int lineBreakMode) -> void
    {
        nscontrol_set_line_break_mode(handle, lineBreakMode);
    }

    public static function alignment(int handle) -> int
    {
        return nscontrol_alignment(handle);
    }

    public static function setAlignment(int handle, int alignment) -> void
    {
        nscontrol_set_alignment(handle, alignment);
    }

    public static function baseWritingDirection(int handle) -> int
    {
        return nscontrol_base_writing_direction(handle);
    }

    public static function setBaseWritingDirection(int handle, int baseWritingDirection) -> void
    {
        nscontrol_set_base_writing_direction(handle, baseWritingDirection);
    }

    public static function allowsExpansionToolTips(int handle) -> bool
    {
        int r;

        let r = nscontrol_allows_expansion_tool_tips(handle);

        return r == 1;
    }

    public static function setAllowsExpansionToolTips(int handle, bool allowsExpansionToolTips) -> void
    {
        nscontrol_set_allows_expansion_tool_tips(handle, allowsExpansionToolTips);
    }

    public static function expansionFrameWithFrame(int handle, double x, double y, double width, double height) -> array
    {
        var result;

        let result = nscontrol_expansion_frame_with_frame(handle, x, y, width, height);

        return result;
    }

    public static function drawWithExpansionFrameInView(int handle, double x, double y, double width, double height, int view) -> void
    {
        nscontrol_draw_with_expansion_frame_in_view(handle, x, y, width, height, view);
    }

    public static function currentEditor(int handle) -> int
    {
        return nscontrol_current_editor(handle);
    }

    public static function abortEditing(int handle) -> bool
    {
        int r;

        let r = nscontrol_abort_editing(handle);

        return r == 1;
    }

    public static function validateEditing(int handle) -> void
    {
        nscontrol_validate_editing(handle);
    }

    public static function editWithFrameEditorDelegateEvent(int handle, double x, double y, double width, double height, int textObj, int delegate, int event) -> void
    {
        nscontrol_edit_with_frame_editor_delegate_event(handle, x, y, width, height, textObj, delegate, event);
    }

    public static function selectWithFrameEditorDelegateStartLength(int handle, double x, double y, double width, double height, int textObj, int delegate, int start, int length) -> void
    {
        nscontrol_select_with_frame_editor_delegate_start_length(handle, x, y, width, height, textObj, delegate, start, length);
    }

    public static function endEditing(int handle, int textObj) -> void
    {
        nscontrol_end_editing(handle, textObj);
    }

    public static function cell(int handle) -> int
    {
        return nscontrol_cell(handle);
    }

    public static function setCell(int handle, int cell) -> void
    {
        nscontrol_set_cell(handle, cell);
    }

    public static function selectedCell(int handle) -> int
    {
        return nscontrol_selected_cell(handle);
    }

    public static function selectedTag(int handle) -> int
    {
        return nscontrol_selected_tag(handle);
    }

    public static function updateCell(int handle, int cell) -> void
    {
        nscontrol_update_cell(handle, cell);
    }

    public static function updateCellInside(int handle, int cell) -> void
    {
        nscontrol_update_cell_inside(handle, cell);
    }

    public static function drawCellInside(int handle, int cell) -> void
    {
        nscontrol_draw_cell_inside(handle, cell);
    }

    public static function drawCell(int handle, int cell) -> void
    {
        nscontrol_draw_cell(handle, cell);
    }

    public static function selectCell(int handle, int cell) -> void
    {
        nscontrol_select_cell(handle, cell);
    }

    // ---- @reserved (not bound; see src/*.h) ----
    // @reserved - (nullable instancetype)initWithCoder:(NSCoder *)coder NS_DESIGNATED_INITIALIZER;
    // @reserved @property (copy) NSAttributedString *attributedStringValue — getter
    // @reserved @property (copy) NSAttributedString *attributedStringValue — setter
    // @reserved - (void)setFloatingPointFormat:(BOOL)autoRange left:(NSUInteger)leftDigits right:(NSUInteger)rightDigits API_DEPRECATED("", macos(10.0,10.0));
    // @reserved @property (class, nullable) Class cellClass — getter
    // @reserved @property (class, nullable) Class cellClass — setter
    // @reserved - (void)setNeedsDisplay API_DEPRECATED("Set the needsDisplay property to YES instead", macos(10.0,10.14));
    // @reserved - (void)calcSize API_DEPRECATED("Override -layout instead. This method should never be called", macos(10.0,10.14));

}
