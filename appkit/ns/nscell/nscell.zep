// GENERATED by scripts/gen-zep.php — DO NOT EDIT.
// Source of truth: the @zep/@zep-construct/@reserved annotations in src/*.h.

namespace AppKit\NS\NSCell;

class NSCell
{
    public static function init() -> int
    {
        return nscell_init();
    }

    public static function initTextCell(string string_) -> int
    {
        return nscell_init_text_cell(string_);
    }

    public static function initImageCell(int image) -> int
    {
        return nscell_init_image_cell(image);
    }

    public static function prefersTrackingUntilMouseUp() -> bool
    {
        int r;

        let r = nscell_prefers_tracking_until_mouse_up();

        return r == 1;
    }

    public static function controlView(int handle) -> int
    {
        return nscell_control_view(handle);
    }

    public static function setControlView(int handle, int controlView) -> void
    {
        nscell_set_control_view(handle, controlView);
    }

    public static function type(int handle) -> int
    {
        return nscell_type(handle);
    }

    public static function setType(int handle, int type) -> void
    {
        nscell_set_type(handle, type);
    }

    public static function state(int handle) -> int
    {
        return nscell_state(handle);
    }

    public static function setState(int handle, int state) -> void
    {
        nscell_set_state(handle, state);
    }

    public static function target(int handle) -> int
    {
        return nscell_target(handle);
    }

    public static function setTarget(int handle, int target) -> void
    {
        nscell_set_target(handle, target);
    }

    public static function action(int handle) -> var
    {
        var result;

        let result = nscell_action(handle);

        return result;
    }

    public static function setAction(int handle, var action) -> void
    {
        nscell_set_action(handle, action);
    }

    public static function tag(int handle) -> int
    {
        return nscell_tag(handle);
    }

    public static function setTag(int handle, int tag) -> void
    {
        nscell_set_tag(handle, tag);
    }

    public static function title(int handle) -> string
    {
        var result;

        let result = nscell_title(handle);

        return result;
    }

    public static function setTitle(int handle, string title) -> void
    {
        nscell_set_title(handle, title);
    }

    public static function isOpaque(int handle) -> bool
    {
        int r;

        let r = nscell_is_opaque(handle);

        return r == 1;
    }

    public static function isEnabled(int handle) -> bool
    {
        int r;

        let r = nscell_is_enabled(handle);

        return r == 1;
    }

    public static function setEnabled(int handle, bool enabled) -> void
    {
        nscell_set_enabled(handle, enabled);
    }

    public static function sendActionOn(int handle, int mask) -> int
    {
        return nscell_send_action_on(handle, mask);
    }

    public static function isContinuous(int handle) -> bool
    {
        int r;

        let r = nscell_is_continuous(handle);

        return r == 1;
    }

    public static function setContinuous(int handle, bool continuous) -> void
    {
        nscell_set_continuous(handle, continuous);
    }

    public static function isEditable(int handle) -> bool
    {
        int r;

        let r = nscell_is_editable(handle);

        return r == 1;
    }

    public static function setEditable(int handle, bool editable) -> void
    {
        nscell_set_editable(handle, editable);
    }

    public static function isSelectable(int handle) -> bool
    {
        int r;

        let r = nscell_is_selectable(handle);

        return r == 1;
    }

    public static function setSelectable(int handle, bool selectable) -> void
    {
        nscell_set_selectable(handle, selectable);
    }

    public static function isBordered(int handle) -> bool
    {
        int r;

        let r = nscell_is_bordered(handle);

        return r == 1;
    }

    public static function setBordered(int handle, bool bordered) -> void
    {
        nscell_set_bordered(handle, bordered);
    }

    public static function isBezeled(int handle) -> bool
    {
        int r;

        let r = nscell_is_bezeled(handle);

        return r == 1;
    }

    public static function setBezeled(int handle, bool bezeled) -> void
    {
        nscell_set_bezeled(handle, bezeled);
    }

    public static function isScrollable(int handle) -> bool
    {
        int r;

        let r = nscell_is_scrollable(handle);

        return r == 1;
    }

    public static function setScrollable(int handle, bool scrollable) -> void
    {
        nscell_set_scrollable(handle, scrollable);
    }

    public static function isHighlighted(int handle) -> bool
    {
        int r;

        let r = nscell_is_highlighted(handle);

        return r == 1;
    }

    public static function setHighlighted(int handle, bool highlighted) -> void
    {
        nscell_set_highlighted(handle, highlighted);
    }

    public static function alignment(int handle) -> int
    {
        return nscell_alignment(handle);
    }

    public static function setAlignment(int handle, int alignment) -> void
    {
        nscell_set_alignment(handle, alignment);
    }

    public static function wraps(int handle) -> bool
    {
        int r;

        let r = nscell_wraps(handle);

        return r == 1;
    }

    public static function setWraps(int handle, bool wraps) -> void
    {
        nscell_set_wraps(handle, wraps);
    }

    public static function font(int handle) -> int
    {
        return nscell_font(handle);
    }

    public static function setFont(int handle, int font) -> void
    {
        nscell_set_font(handle, font);
    }

    public static function keyEquivalent(int handle) -> string
    {
        var result;

        let result = nscell_key_equivalent(handle);

        return result;
    }

    public static function formatter(int handle) -> int
    {
        return nscell_formatter(handle);
    }

    public static function setFormatter(int handle, int formatter) -> void
    {
        nscell_set_formatter(handle, formatter);
    }

    public static function objectValue(int handle) -> int
    {
        return nscell_object_value(handle);
    }

    public static function setObjectValue(int handle, int objectValue) -> void
    {
        nscell_set_object_value(handle, objectValue);
    }

    public static function hasValidObjectValue(int handle) -> bool
    {
        int r;

        let r = nscell_has_valid_object_value(handle);

        return r == 1;
    }

    public static function stringValue(int handle) -> string
    {
        var result;

        let result = nscell_string_value(handle);

        return result;
    }

    public static function setStringValue(int handle, string stringValue) -> void
    {
        nscell_set_string_value(handle, stringValue);
    }

    public static function compare(int handle, int otherCell) -> int
    {
        return nscell_compare(handle, otherCell);
    }

    public static function intValue(int handle) -> int
    {
        return nscell_int_value(handle);
    }

    public static function setIntValue(int handle, int intValue) -> void
    {
        nscell_set_int_value(handle, intValue);
    }

    public static function floatValue(int handle) -> double
    {
        return nscell_float_value(handle);
    }

    public static function setFloatValue(int handle, double floatValue) -> void
    {
        nscell_set_float_value(handle, floatValue);
    }

    public static function doubleValue(int handle) -> double
    {
        return nscell_double_value(handle);
    }

    public static function setDoubleValue(int handle, double doubleValue) -> void
    {
        nscell_set_double_value(handle, doubleValue);
    }

    public static function integerValue(int handle) -> int
    {
        return nscell_integer_value(handle);
    }

    public static function setIntegerValue(int handle, int integerValue) -> void
    {
        nscell_set_integer_value(handle, integerValue);
    }

    public static function takeIntValueFrom(int handle, int sender) -> void
    {
        nscell_take_int_value_from(handle, sender);
    }

    public static function takeFloatValueFrom(int handle, int sender) -> void
    {
        nscell_take_float_value_from(handle, sender);
    }

    public static function takeDoubleValueFrom(int handle, int sender) -> void
    {
        nscell_take_double_value_from(handle, sender);
    }

    public static function takeStringValueFrom(int handle, int sender) -> void
    {
        nscell_take_string_value_from(handle, sender);
    }

    public static function takeObjectValueFrom(int handle, int sender) -> void
    {
        nscell_take_object_value_from(handle, sender);
    }

    public static function takeIntegerValueFrom(int handle, int sender) -> void
    {
        nscell_take_integer_value_from(handle, sender);
    }

    public static function image(int handle) -> int
    {
        return nscell_image(handle);
    }

    public static function setImage(int handle, int image) -> void
    {
        nscell_set_image(handle, image);
    }

    public static function controlSize(int handle) -> int
    {
        return nscell_control_size(handle);
    }

    public static function setControlSize(int handle, int controlSize) -> void
    {
        nscell_set_control_size(handle, controlSize);
    }

    public static function representedObject(int handle) -> int
    {
        return nscell_represented_object(handle);
    }

    public static function setRepresentedObject(int handle, int representedObject) -> void
    {
        nscell_set_represented_object(handle, representedObject);
    }

    public static function cellAttribute(int handle, int parameter) -> int
    {
        return nscell_cell_attribute(handle, parameter);
    }

    public static function setCellAttributeTo(int handle, int parameter, int value) -> void
    {
        nscell_set_cell_attribute_to(handle, parameter, value);
    }

    public static function imageRectForBounds(int handle, double x, double y, double width, double height) -> array
    {
        var result;

        let result = nscell_image_rect_for_bounds(handle, x, y, width, height);

        return result;
    }

    public static function titleRectForBounds(int handle, double x, double y, double width, double height) -> array
    {
        var result;

        let result = nscell_title_rect_for_bounds(handle, x, y, width, height);

        return result;
    }

    public static function drawingRectForBounds(int handle, double x, double y, double width, double height) -> array
    {
        var result;

        let result = nscell_drawing_rect_for_bounds(handle, x, y, width, height);

        return result;
    }

    public static function cellSize(int handle) -> array
    {
        var result;

        let result = nscell_cell_size(handle);

        return result;
    }

    public static function cellSizeForBounds(int handle, double x, double y, double width, double height) -> array
    {
        var result;

        let result = nscell_cell_size_for_bounds(handle, x, y, width, height);

        return result;
    }

    public static function highlightColorWithFrameInView(int handle, double x, double y, double width, double height, int controlView) -> int
    {
        return nscell_highlight_color_with_frame_in_view(handle, x, y, width, height, controlView);
    }

    public static function calcDrawInfo(int handle, double x, double y, double width, double height) -> void
    {
        nscell_calc_draw_info(handle, x, y, width, height);
    }

    public static function setUpFieldEditorAttributes(int handle, int textObj) -> int
    {
        return nscell_set_up_field_editor_attributes(handle, textObj);
    }

    public static function drawInteriorWithFrameInView(int handle, double x, double y, double width, double height, int controlView) -> void
    {
        nscell_draw_interior_with_frame_in_view(handle, x, y, width, height, controlView);
    }

    public static function drawWithFrameInView(int handle, double x, double y, double width, double height, int controlView) -> void
    {
        nscell_draw_with_frame_in_view(handle, x, y, width, height, controlView);
    }

    public static function highlightWithFrameInView(int handle, bool flag, double x, double y, double width, double height, int controlView) -> void
    {
        nscell_highlight_with_frame_in_view(handle, flag, x, y, width, height, controlView);
    }

    public static function mouseDownFlags(int handle) -> int
    {
        return nscell_mouse_down_flags(handle);
    }

    public static function getPeriodicDelayInterval(int handle) -> array
    {
        var result;

        let result = nscell_get_periodic_delay_interval(handle);

        return result;
    }

    public static function startTrackingAtInView(int handle, double startX, double startY, int controlView) -> bool
    {
        int r;

        let r = nscell_start_tracking_at_in_view(handle, startX, startY, controlView);

        return r == 1;
    }

    public static function continueTrackingAtInView(int handle, double lastX, double lastY, double currentX, double currentY, int controlView) -> bool
    {
        int r;

        let r = nscell_continue_tracking_at_in_view(handle, lastX, lastY, currentX, currentY, controlView);

        return r == 1;
    }

    public static function stopTrackingAtInViewMouseIsUp(int handle, double lastX, double lastY, double stopX, double stopY, int controlView, bool flag) -> void
    {
        nscell_stop_tracking_at_in_view_mouse_is_up(handle, lastX, lastY, stopX, stopY, controlView, flag);
    }

    public static function trackMouseInRectOfViewUntilMouseUp(int handle, int event, double x, double y, double width, double height, int controlView, bool flag) -> bool
    {
        int r;

        let r = nscell_track_mouse_in_rect_of_view_until_mouse_up(handle, event, x, y, width, height, controlView, flag);

        return r == 1;
    }

    public static function editWithFrameInViewEditorDelegateEvent(int handle, double x, double y, double width, double height, int controlView, int textObj, int delegate, int event) -> void
    {
        nscell_edit_with_frame_in_view_editor_delegate_event(handle, x, y, width, height, controlView, textObj, delegate, event);
    }

    public static function selectWithFrameInViewEditorDelegateStartLength(int handle, double x, double y, double width, double height, int controlView, int textObj, int delegate, int start, int length) -> void
    {
        nscell_select_with_frame_in_view_editor_delegate_start_length(handle, x, y, width, height, controlView, textObj, delegate, start, length);
    }

    public static function endEditing(int handle, int textObj) -> void
    {
        nscell_end_editing(handle, textObj);
    }

    public static function resetCursorRectInView(int handle, double x, double y, double width, double height, int controlView) -> void
    {
        nscell_reset_cursor_rect_in_view(handle, x, y, width, height, controlView);
    }

    public static function menu(int handle) -> int
    {
        return nscell_menu(handle);
    }

    public static function setMenu(int handle, int menu) -> void
    {
        nscell_set_menu(handle, menu);
    }

    public static function menuForEventInRectOfView(int handle, int event, double x, double y, double width, double height, int view) -> int
    {
        return nscell_menu_for_event_in_rect_of_view(handle, event, x, y, width, height, view);
    }

    public static function defaultMenu() -> int
    {
        return nscell_default_menu();
    }

    public static function sendsActionOnEndEditing(int handle) -> bool
    {
        int r;

        let r = nscell_sends_action_on_end_editing(handle);

        return r == 1;
    }

    public static function setSendsActionOnEndEditing(int handle, bool sendsActionOnEndEditing) -> void
    {
        nscell_set_sends_action_on_end_editing(handle, sendsActionOnEndEditing);
    }

    public static function baseWritingDirection(int handle) -> int
    {
        return nscell_base_writing_direction(handle);
    }

    public static function setBaseWritingDirection(int handle, int baseWritingDirection) -> void
    {
        nscell_set_base_writing_direction(handle, baseWritingDirection);
    }

    public static function lineBreakMode(int handle) -> int
    {
        return nscell_line_break_mode(handle);
    }

    public static function setLineBreakMode(int handle, int lineBreakMode) -> void
    {
        nscell_set_line_break_mode(handle, lineBreakMode);
    }

    public static function allowsUndo(int handle) -> bool
    {
        int r;

        let r = nscell_allows_undo(handle);

        return r == 1;
    }

    public static function setAllowsUndo(int handle, bool allowsUndo) -> void
    {
        nscell_set_allows_undo(handle, allowsUndo);
    }

    public static function truncatesLastVisibleLine(int handle) -> bool
    {
        int r;

        let r = nscell_truncates_last_visible_line(handle);

        return r == 1;
    }

    public static function setTruncatesLastVisibleLine(int handle, bool truncatesLastVisibleLine) -> void
    {
        nscell_set_truncates_last_visible_line(handle, truncatesLastVisibleLine);
    }

    public static function userInterfaceLayoutDirection(int handle) -> int
    {
        return nscell_user_interface_layout_direction(handle);
    }

    public static function setUserInterfaceLayoutDirection(int handle, int userInterfaceLayoutDirection) -> void
    {
        nscell_set_user_interface_layout_direction(handle, userInterfaceLayoutDirection);
    }

    public static function fieldEditorForView(int handle, int controlView) -> int
    {
        return nscell_field_editor_for_view(handle, controlView);
    }

    public static function usesSingleLineMode(int handle) -> bool
    {
        int r;

        let r = nscell_uses_single_line_mode(handle);

        return r == 1;
    }

    public static function setUsesSingleLineMode(int handle, bool usesSingleLineMode) -> void
    {
        nscell_set_uses_single_line_mode(handle, usesSingleLineMode);
    }

    public static function draggingImageComponentsWithFrameInView(int handle, double x, double y, double width, double height, int view) -> array
    {
        var result;

        let result = nscell_dragging_image_components_with_frame_in_view(handle, x, y, width, height, view);

        return result;
    }

    public static function refusesFirstResponder(int handle) -> bool
    {
        int r;

        let r = nscell_refuses_first_responder(handle);

        return r == 1;
    }

    public static function setRefusesFirstResponder(int handle, bool refusesFirstResponder) -> void
    {
        nscell_set_refuses_first_responder(handle, refusesFirstResponder);
    }

    public static function acceptsFirstResponder(int handle) -> bool
    {
        int r;

        let r = nscell_accepts_first_responder(handle);

        return r == 1;
    }

    public static function showsFirstResponder(int handle) -> bool
    {
        int r;

        let r = nscell_shows_first_responder(handle);

        return r == 1;
    }

    public static function setShowsFirstResponder(int handle, bool showsFirstResponder) -> void
    {
        nscell_set_shows_first_responder(handle, showsFirstResponder);
    }

    public static function performClick(int handle, int sender) -> void
    {
        nscell_perform_click(handle, sender);
    }

    public static function focusRingType(int handle) -> int
    {
        return nscell_focus_ring_type(handle);
    }

    public static function setFocusRingType(int handle, int focusRingType) -> void
    {
        nscell_set_focus_ring_type(handle, focusRingType);
    }

    public static function defaultFocusRingType() -> int
    {
        return nscell_default_focus_ring_type();
    }

    public static function drawFocusRingMaskWithFrameInView(int handle, double x, double y, double width, double height, int controlView) -> void
    {
        nscell_draw_focus_ring_mask_with_frame_in_view(handle, x, y, width, height, controlView);
    }

    public static function focusRingMaskBoundsForFrameInView(int handle, double x, double y, double width, double height, int controlView) -> array
    {
        var result;

        let result = nscell_focus_ring_mask_bounds_for_frame_in_view(handle, x, y, width, height, controlView);

        return result;
    }

    public static function wantsNotificationForMarkedText(int handle) -> bool
    {
        int r;

        let r = nscell_wants_notification_for_marked_text(handle);

        return r == 1;
    }

    public static function allowsEditingTextAttributes(int handle) -> bool
    {
        int r;

        let r = nscell_allows_editing_text_attributes(handle);

        return r == 1;
    }

    public static function setAllowsEditingTextAttributes(int handle, bool allowsEditingTextAttributes) -> void
    {
        nscell_set_allows_editing_text_attributes(handle, allowsEditingTextAttributes);
    }

    public static function importsGraphics(int handle) -> bool
    {
        int r;

        let r = nscell_imports_graphics(handle);

        return r == 1;
    }

    public static function setImportsGraphics(int handle, bool importsGraphics) -> void
    {
        nscell_set_imports_graphics(handle, importsGraphics);
    }

    public static function allowsMixedState(int handle) -> bool
    {
        int r;

        let r = nscell_allows_mixed_state(handle);

        return r == 1;
    }

    public static function setAllowsMixedState(int handle, bool allowsMixedState) -> void
    {
        nscell_set_allows_mixed_state(handle, allowsMixedState);
    }

    public static function nextState(int handle) -> int
    {
        return nscell_next_state(handle);
    }

    public static function setNextState(int handle) -> void
    {
        nscell_set_next_state(handle);
    }

    public static function hitTestForEventInRectOfView(int handle, int event, double x, double y, double width, double height, int controlView) -> int
    {
        return nscell_hit_test_for_event_in_rect_of_view(handle, event, x, y, width, height, controlView);
    }

    public static function expansionFrameWithFrameInView(int handle, double x, double y, double width, double height, int view) -> array
    {
        var result;

        let result = nscell_expansion_frame_with_frame_in_view(handle, x, y, width, height, view);

        return result;
    }

    public static function drawWithExpansionFrameInView(int handle, double x, double y, double width, double height, int view) -> void
    {
        nscell_draw_with_expansion_frame_in_view(handle, x, y, width, height, view);
    }

    public static function backgroundStyle(int handle) -> int
    {
        return nscell_background_style(handle);
    }

    public static function setBackgroundStyle(int handle, int backgroundStyle) -> void
    {
        nscell_set_background_style(handle, backgroundStyle);
    }

    public static function interiorBackgroundStyle(int handle) -> int
    {
        return nscell_interior_background_style(handle);
    }

    // ---- @reserved (not bound; see src/*.h) ----
    // @reserved - (instancetype)initWithCoder:(NSCoder *)coder NS_DESIGNATED_INITIALIZER;
    // @reserved @property (copy) NSAttributedString *attributedStringValue — getter
    // @reserved @property (copy) NSAttributedString *attributedStringValue — setter
    // @reserved @property NSControlTint controlTint API_DEPRECATED — getter
    // @reserved @property NSControlTint controlTint API_DEPRECATED — setter
    // @reserved - (NSInteger)entryType API_DEPRECATED("", macos(10.0,10.0));
    // @reserved - (void)setEntryType:(NSInteger)type API_DEPRECATED("", macos(10.0,10.0));
    // @reserved - (BOOL)isEntryAcceptable:(NSString *)string API_DEPRECATED("", macos(10.0,10.0));
    // @reserved - (void)setFloatingPointFormat:(BOOL)autoRange left:(NSUInteger)leftDigits right:(NSUInteger)rightDigits API_DEPRECATED("", macos(10.0,10.0));
    // @reserved - (void)setMnemonicLocation:(NSUInteger)location API_DEPRECATED("", macos(10.0,10.8));
    // @reserved - (NSUInteger)mnemonicLocation API_DEPRECATED("", macos(10.0,10.8));
    // @reserved - (NSString *)mnemonic API_DEPRECATED("", macos(10.0,10.8));
    // @reserved - (void)setTitleWithMnemonic:(NSString *)stringWithAmpersand API_DEPRECATED("", macos(10.0,10.8));

}
