// GENERATED by scripts/gen-zep.php — DO NOT EDIT.
// Source of truth: the @zep/@zep-construct/@reserved annotations in src/*.h.

namespace AppKit\NS\NSEvent;

class NSEvent
{
    public static function type(int handle) -> int
    {
        return nsevent_type(handle);
    }

    public static function modifierFlags(int handle) -> int
    {
        return nsevent_modifier_flags(handle);
    }

    public static function timestamp(int handle) -> double
    {
        return nsevent_timestamp(handle);
    }

    public static function window(int handle) -> int
    {
        return nsevent_window(handle);
    }

    public static function windowNumber(int handle) -> int
    {
        return nsevent_window_number(handle);
    }

    public static function clickCount(int handle) -> int
    {
        return nsevent_click_count(handle);
    }

    public static function buttonNumber(int handle) -> int
    {
        return nsevent_button_number(handle);
    }

    public static function eventNumber(int handle) -> int
    {
        return nsevent_event_number(handle);
    }

    public static function pressure(int handle) -> double
    {
        return nsevent_pressure(handle);
    }

    public static function locationInWindow(int handle) -> array
    {
        var result;

        let result = nsevent_location_in_window(handle);

        return result;
    }

    public static function deltaX(int handle) -> double
    {
        return nsevent_delta_x(handle);
    }

    public static function deltaY(int handle) -> double
    {
        return nsevent_delta_y(handle);
    }

    public static function deltaZ(int handle) -> double
    {
        return nsevent_delta_z(handle);
    }

    public static function hasPreciseScrollingDeltas(int handle) -> bool
    {
        int r;

        let r = nsevent_has_precise_scrolling_deltas(handle);

        return r == 1;
    }

    public static function scrollingDeltaX(int handle) -> double
    {
        return nsevent_scrolling_delta_x(handle);
    }

    public static function scrollingDeltaY(int handle) -> double
    {
        return nsevent_scrolling_delta_y(handle);
    }

    public static function momentumPhase(int handle) -> int
    {
        return nsevent_momentum_phase(handle);
    }

    public static function isDirectionInvertedFromDevice(int handle) -> bool
    {
        int r;

        let r = nsevent_is_direction_inverted_from_device(handle);

        return r == 1;
    }

    public static function characters(int handle) -> var
    {
        var result;

        let result = nsevent_characters(handle);

        return result;
    }

    public static function charactersIgnoringModifiers(int handle) -> var
    {
        var result;

        let result = nsevent_characters_ignoring_modifiers(handle);

        return result;
    }

    public static function charactersByApplyingModifiers(int handle, int modifiers) -> var
    {
        var result;

        let result = nsevent_characters_by_applying_modifiers(handle, modifiers);

        return result;
    }

    public static function isARepeat(int handle) -> bool
    {
        int r;

        let r = nsevent_is_a_repeat(handle);

        return r == 1;
    }

    public static function keyCode(int handle) -> int
    {
        return nsevent_key_code(handle);
    }

    public static function trackingNumber(int handle) -> int
    {
        return nsevent_tracking_number(handle);
    }

    public static function userData(int handle) -> int
    {
        return nsevent_user_data(handle);
    }

    public static function trackingArea(int handle) -> int
    {
        return nsevent_tracking_area(handle);
    }

    public static function subtype(int handle) -> int
    {
        return nsevent_subtype(handle);
    }

    public static function data1(int handle) -> int
    {
        return nsevent_data1(handle);
    }

    public static function data2(int handle) -> int
    {
        return nsevent_data2(handle);
    }

    public static function eventWithEventRef(int eventRef) -> int
    {
        return nsevent_event_with_event_ref(eventRef);
    }

    public static function eventWithCGEvent(int cgEvent) -> int
    {
        return nsevent_event_with_cg_event(cgEvent);
    }

    public static function isMouseCoalescingEnabled() -> bool
    {
        int r;

        let r = nsevent_is_mouse_coalescing_enabled();

        return r == 1;
    }

    public static function setMouseCoalescingEnabled(bool flag) -> void
    {
        nsevent_set_mouse_coalescing_enabled(flag);
    }

    public static function magnification(int handle) -> double
    {
        return nsevent_magnification(handle);
    }

    public static function deviceID(int handle) -> int
    {
        return nsevent_device_id(handle);
    }

    public static function rotation(int handle) -> double
    {
        return nsevent_rotation(handle);
    }

    public static function absoluteX(int handle) -> int
    {
        return nsevent_absolute_x(handle);
    }

    public static function absoluteY(int handle) -> int
    {
        return nsevent_absolute_y(handle);
    }

    public static function absoluteZ(int handle) -> int
    {
        return nsevent_absolute_z(handle);
    }

    public static function buttonMask(int handle) -> int
    {
        return nsevent_button_mask(handle);
    }

    public static function tilt(int handle) -> array
    {
        var result;

        let result = nsevent_tilt(handle);

        return result;
    }

    public static function tangentialPressure(int handle) -> double
    {
        return nsevent_tangential_pressure(handle);
    }

    public static function vendorDefined(int handle) -> int
    {
        return nsevent_vendor_defined(handle);
    }

    public static function vendorID(int handle) -> int
    {
        return nsevent_vendor_id(handle);
    }

    public static function tabletID(int handle) -> int
    {
        return nsevent_tablet_id(handle);
    }

    public static function pointingDeviceID(int handle) -> int
    {
        return nsevent_pointing_device_id(handle);
    }

    public static function systemTabletID(int handle) -> int
    {
        return nsevent_system_tablet_id(handle);
    }

    public static function vendorPointingDeviceType(int handle) -> int
    {
        return nsevent_vendor_pointing_device_type(handle);
    }

    public static function pointingDeviceSerialNumber(int handle) -> int
    {
        return nsevent_pointing_device_serial_number(handle);
    }

    public static function uniqueID(int handle) -> int
    {
        return nsevent_unique_id(handle);
    }

    public static function capabilityMask(int handle) -> int
    {
        return nsevent_capability_mask(handle);
    }

    public static function pointingDeviceType(int handle) -> int
    {
        return nsevent_pointing_device_type(handle);
    }

    public static function isEnteringProximity(int handle) -> bool
    {
        int r;

        let r = nsevent_is_entering_proximity(handle);

        return r == 1;
    }

    public static function touchesMatchingPhaseInView(int handle, int phase, int view) -> array
    {
        var result;

        let result = nsevent_touches_matching_phase_in_view(handle, phase, view);

        return result;
    }

    public static function allTouches(int handle) -> array
    {
        var result;

        let result = nsevent_all_touches(handle);

        return result;
    }

    public static function touchesForView(int handle, int view) -> array
    {
        var result;

        let result = nsevent_touches_for_view(handle, view);

        return result;
    }

    public static function coalescedTouchesForTouch(int handle, int touch) -> array
    {
        var result;

        let result = nsevent_coalesced_touches_for_touch(handle, touch);

        return result;
    }

    public static function phase(int handle) -> int
    {
        return nsevent_phase(handle);
    }

    public static function stage(int handle) -> int
    {
        return nsevent_stage(handle);
    }

    public static function stageTransition(int handle) -> double
    {
        return nsevent_stage_transition(handle);
    }

    public static function associatedEventsMask(int handle) -> int
    {
        return nsevent_associated_events_mask(handle);
    }

    public static function pressureBehavior(int handle) -> int
    {
        return nsevent_pressure_behavior(handle);
    }

    public static function isSwipeTrackingFromScrollEventsEnabled() -> bool
    {
        int r;

        let r = nsevent_is_swipe_tracking_from_scroll_events_enabled();

        return r == 1;
    }

    public static function startPeriodicEventsAfterDelayWithPeriod(double delay, double period) -> void
    {
        nsevent_start_periodic_events_after_delay_with_period(delay, period);
    }

    public static function stopPeriodicEvents() -> void
    {
        nsevent_stop_periodic_events();
    }

    public static function mouseEventWithTypeLocationModifierFlagsTimestampWindowNumberContextEventNumberClickCountPressure(int type, double x, double y, int flags, double timestamp, int windowNumber, int context, int eventNumber, int clickCount, double pressure) -> int
    {
        return nsevent_mouse_event_with_type_location_modifier_flags_timestamp_window_number_context_event_number_click_count_pressure(type, x, y, flags, timestamp, windowNumber, context, eventNumber, clickCount, pressure);
    }

    public static function keyEventWithTypeLocationModifierFlagsTimestampWindowNumberContextCharactersCharactersIgnoringModifiersIsARepeatKeyCode(int type, double x, double y, int flags, double timestamp, int windowNumber, int context, string characters, string charactersIgnoringModifiers, bool flag, int code) -> int
    {
        return nsevent_key_event_with_type_location_modifier_flags_timestamp_window_number_context_characters_characters_ignoring_modifiers_is_a_repeat_key_code(type, x, y, flags, timestamp, windowNumber, context, characters, charactersIgnoringModifiers, flag, code);
    }

    public static function enterExitEventWithTypeLocationModifierFlagsTimestampWindowNumberContextEventNumberTrackingNumberUserData(int type, double x, double y, int flags, double timestamp, int windowNumber, int context, int eventNumber, int trackingNumber, int userData) -> int
    {
        return nsevent_enter_exit_event_with_type_location_modifier_flags_timestamp_window_number_context_event_number_tracking_number_user_data(type, x, y, flags, timestamp, windowNumber, context, eventNumber, trackingNumber, userData);
    }

    public static function otherEventWithTypeLocationModifierFlagsTimestampWindowNumberContextSubtypeData1Data2(int type, double x, double y, int flags, double timestamp, int windowNumber, int context, int subtype, int data1, int data2) -> int
    {
        return nsevent_other_event_with_type_location_modifier_flags_timestamp_window_number_context_subtype_data1_data2(type, x, y, flags, timestamp, windowNumber, context, subtype, data1, data2);
    }

    public static function mouseLocation() -> array
    {
        var result;

        let result = nsevent_mouse_location();

        return result;
    }

    public static function modifierFlagsClass() -> int
    {
        return nsevent_modifier_flags_class();
    }

    public static function pressedMouseButtons() -> int
    {
        return nsevent_pressed_mouse_buttons();
    }

    public static function doubleClickInterval() -> double
    {
        return nsevent_double_click_interval();
    }

    public static function keyRepeatDelay() -> double
    {
        return nsevent_key_repeat_delay();
    }

    public static function keyRepeatInterval() -> double
    {
        return nsevent_key_repeat_interval();
    }

    public static function removeMonitor(int eventMonitor) -> void
    {
        nsevent_remove_monitor(eventMonitor);
    }

    // ---- @reserved (not bound; see src/*.h) ----
    // @reserved @property (nullable, readonly, strong) NSGraphicsContext *context API_DEPRECATED — getter
    // @reserved @property (nullable, readonly) const void *eventRef NS_RETURNS_INNER_POINTER — EventRef inner pointer
    // @reserved @property (nullable, readonly) CGEventRef CGEvent — CGEventRef, no CFType registry
    // @reserved - (void)trackSwipeEventWithOptions:(NSEventSwipeTrackingOptions)options dampenAmountThresholdMin:(CGFloat)minDampenThreshold max:(CGFloat)maxDampenThreshold usingHandler:(void (^)(CGFloat gestureAmount, NSEventPhase phase, BOOL isComplete, BOOL *stop))trackingHandler — block
    // @reserved + (nullable id)addGlobalMonitorForEventsMatchingMask:(NSEventMask)mask handler:(void (^)(NSEvent *event))block — block
    // @reserved + (nullable id)addLocalMonitorForEventsMatchingMask:(NSEventMask)mask handler:(NSEvent * _Nullable (^)(NSEvent *event))block — block

}
