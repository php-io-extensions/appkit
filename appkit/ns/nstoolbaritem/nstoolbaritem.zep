// GENERATED by scripts/gen-zep.php — DO NOT EDIT.
// Source of truth: the @zep/@zep-construct/@reserved annotations in src/*.h.

namespace AppKit\NS\NSToolbarItem;

class NSToolbarItem
{
    public static function initWithItemIdentifier(string itemIdentifier) -> int
    {
        return nstoolbaritem_init_with_item_identifier(itemIdentifier);
    }

    public static function itemIdentifier(int handle) -> string
    {
        var result;

        let result = nstoolbaritem_item_identifier(handle);

        return result;
    }

    public static function toolbar(int handle) -> int
    {
        return nstoolbaritem_toolbar(handle);
    }

    public static function label(int handle) -> string
    {
        var result;

        let result = nstoolbaritem_label(handle);

        return result;
    }

    public static function setLabel(int handle, string label) -> void
    {
        nstoolbaritem_set_label(handle, label);
    }

    public static function paletteLabel(int handle) -> string
    {
        var result;

        let result = nstoolbaritem_palette_label(handle);

        return result;
    }

    public static function setPaletteLabel(int handle, string paletteLabel) -> void
    {
        nstoolbaritem_set_palette_label(handle, paletteLabel);
    }

    public static function possibleLabels(int handle) -> array
    {
        var result;

        let result = nstoolbaritem_possible_labels(handle);

        return result;
    }

    public static function setPossibleLabels(int handle, array possibleLabels) -> void
    {
        nstoolbaritem_set_possible_labels(handle, possibleLabels);
    }

    public static function toolTip(int handle) -> var
    {
        var result;

        let result = nstoolbaritem_tool_tip(handle);

        return result;
    }

    public static function setToolTip(int handle, var toolTip) -> void
    {
        nstoolbaritem_set_tool_tip(handle, toolTip);
    }

    public static function menuFormRepresentation(int handle) -> int
    {
        return nstoolbaritem_menu_form_representation(handle);
    }

    public static function setMenuFormRepresentation(int handle, int menuFormRepresentation) -> void
    {
        nstoolbaritem_set_menu_form_representation(handle, menuFormRepresentation);
    }

    public static function tag(int handle) -> int
    {
        return nstoolbaritem_tag(handle);
    }

    public static function setTag(int handle, int tag) -> void
    {
        nstoolbaritem_set_tag(handle, tag);
    }

    public static function target(int handle) -> int
    {
        return nstoolbaritem_target(handle);
    }

    public static function setTarget(int handle, int target) -> void
    {
        nstoolbaritem_set_target(handle, target);
    }

    public static function action(int handle) -> var
    {
        var result;

        let result = nstoolbaritem_action(handle);

        return result;
    }

    public static function setAction(int handle, var action) -> void
    {
        nstoolbaritem_set_action(handle, action);
    }

    public static function isEnabled(int handle) -> bool
    {
        int r;

        let r = nstoolbaritem_is_enabled(handle);

        return r == 1;
    }

    public static function setEnabled(int handle, bool enabled) -> void
    {
        nstoolbaritem_set_enabled(handle, enabled);
    }

    public static function image(int handle) -> int
    {
        return nstoolbaritem_image(handle);
    }

    public static function setImage(int handle, int image) -> void
    {
        nstoolbaritem_set_image(handle, image);
    }

    public static function title(int handle) -> string
    {
        var result;

        let result = nstoolbaritem_title(handle);

        return result;
    }

    public static function setTitle(int handle, string title) -> void
    {
        nstoolbaritem_set_title(handle, title);
    }

    public static function isBordered(int handle) -> bool
    {
        int r;

        let r = nstoolbaritem_is_bordered(handle);

        return r == 1;
    }

    public static function setBordered(int handle, bool bordered) -> void
    {
        nstoolbaritem_set_bordered(handle, bordered);
    }

    public static function isNavigational(int handle) -> bool
    {
        int r;

        let r = nstoolbaritem_is_navigational(handle);

        return r == 1;
    }

    public static function setNavigational(int handle, bool navigational) -> void
    {
        nstoolbaritem_set_navigational(handle, navigational);
    }

    public static function view(int handle) -> int
    {
        return nstoolbaritem_view(handle);
    }

    public static function setView(int handle, int view) -> void
    {
        nstoolbaritem_set_view(handle, view);
    }

    public static function isVisible(int handle) -> bool
    {
        int r;

        let r = nstoolbaritem_is_visible(handle);

        return r == 1;
    }

    public static function isHidden(int handle) -> bool
    {
        int r;

        let r = nstoolbaritem_is_hidden(handle);

        return r == 1;
    }

    public static function setHidden(int handle, bool hidden) -> void
    {
        nstoolbaritem_set_hidden(handle, hidden);
    }

    public static function visibilityPriority(int handle) -> int
    {
        return nstoolbaritem_visibility_priority(handle);
    }

    public static function setVisibilityPriority(int handle, int visibilityPriority) -> void
    {
        nstoolbaritem_set_visibility_priority(handle, visibilityPriority);
    }

    public static function validate(int handle) -> void
    {
        nstoolbaritem_validate(handle);
    }

    public static function autovalidates(int handle) -> bool
    {
        int r;

        let r = nstoolbaritem_autovalidates(handle);

        return r == 1;
    }

    public static function setAutovalidates(int handle, bool autovalidates) -> void
    {
        nstoolbaritem_set_autovalidates(handle, autovalidates);
    }

    // ---- @reserved (not bound; see src/*.h) ----
    // @reserved @property (nullable, strong) UIImage *image API_AVAILABLE(ios(13.0)) — iOS duplicate getter
    // @reserved @property (nullable, strong) UIImage *image API_AVAILABLE(ios(13.0)) — iOS duplicate setter
    // @reserved @property NSSize minSize API_DEPRECATED — getter
    // @reserved @property NSSize minSize API_DEPRECATED — setter
    // @reserved @property NSSize maxSize API_DEPRECATED — getter
    // @reserved @property NSSize maxSize API_DEPRECATED — setter
    // @reserved @property (readonly) BOOL allowsDuplicatesInToolbar API_DEPRECATED

}
