// GENERATED by scripts/gen-zep.php — DO NOT EDIT.
// Source of truth: the @zep/@zep-construct/@reserved annotations in src/*.h.

namespace AppKit\GC\GCController;

class GCController
{
    public static function controllers() -> array
    {
        var result;

        let result = gccontroller_controllers();

        return result;
    }

    public static function current() -> int
    {
        return gccontroller_current();
    }

    public static function shouldMonitorBackgroundEvents() -> bool
    {
        int r;

        let r = gccontroller_should_monitor_background_events();

        return r == 1;
    }

    public static function setShouldMonitorBackgroundEvents(bool value) -> void
    {
        gccontroller_set_should_monitor_background_events(value);
    }

    public static function isAttachedToDevice(int handle) -> bool
    {
        int r;

        let r = gccontroller_is_attached_to_device(handle);

        return r == 1;
    }

    public static function playerIndex(int handle) -> int
    {
        return gccontroller_player_index(handle);
    }

    public static function setPlayerIndex(int handle, int playerIndex) -> void
    {
        gccontroller_set_player_index(handle, playerIndex);
    }

    public static function microGamepad(int handle) -> int
    {
        return gccontroller_micro_gamepad(handle);
    }

    public static function extendedGamepad(int handle) -> int
    {
        return gccontroller_extended_gamepad(handle);
    }

    public static function isSnapshot(int handle) -> bool
    {
        int r;

        let r = gccontroller_is_snapshot(handle);

        return r == 1;
    }

    public static function vendorName(int handle) -> var
    {
        var result;

        let result = gccontroller_vendor_name(handle);

        return result;
    }

    public static function productCategory(int handle) -> string
    {
        var result;

        let result = gccontroller_product_category(handle);

        return result;
    }

    // ---- @reserved (not bound; see src/*.h) ----
    // @reserved @property (nonatomic, copy, nullable) void (^controllerPausedHandler)(GCController *controller) API_DEPRECATED — getter (block)
    // @reserved @property (nonatomic, copy, nullable) void (^controllerPausedHandler)(GCController *controller) API_DEPRECATED — setter (block)
    // @reserved @property (nonatomic, strong, readonly) GCControllerLiveInput *input — not bound in this slice
    // @reserved @property (nonatomic, copy, readonly, nullable) GCDeviceBattery *battery — not bound in this slice
    // @reserved @property (nonatomic, strong, readonly) GCPhysicalInputProfile *physicalInputProfile — not bound in this slice
    // @reserved @property (nonatomic, strong, readonly, nullable) GCGamepad *gamepad API_DEPRECATED_WITH_REPLACEMENT — deprecated
    // @reserved @property (nonatomic, strong, readonly, nullable) GCMotion *motion — not bound in this slice
    // @reserved @property (nonatomic, retain, readonly, nullable) GCDeviceLight *light — not bound in this slice
    // @reserved @property (nonatomic, retain, readonly, nullable) GCDeviceHaptics *haptics — not bound in this slice
    // @reserved + (BOOL)supportsHIDDevice:(IOHIDDeviceRef)device — IOHIDDeviceRef, no CFType registry
    // @reserved - (GCController *)capture — not bound in this slice
    // @reserved + (GCController *)controllerWithMicroGamepad — not bound in this slice
    // @reserved + (GCController *)controllerWithExtendedGamepad — not bound in this slice
    // @reserved + (void)startWirelessControllerDiscoveryWithCompletionHandler:(nullable void (^)(void))completionHandler — block
    // @reserved + (void)stopWirelessControllerDiscovery — not bound in this slice
    // @reserved @property (nonatomic, strong) dispatch_queue_t handlerQueue — getter (dispatch_queue_t)
    // @reserved @property (nonatomic, strong) dispatch_queue_t handlerQueue — setter (dispatch_queue_t)
    // @reserved @property (nonatomic, strong, readonly) GCPhysicalInputProfile *physicalInputProfile API_DEPRECATED — protocol copy, deprecated there

}
