// GENERATED by scripts/gen-zep.php — DO NOT EDIT.
// Source of truth: the @zep/@zep-construct/@reserved annotations in src/*.h.

namespace AppKit\GC\GCMicroGamepad;

class GCMicroGamepad
{
    public static function dpad(int handle) -> int
    {
        return gcmicrogamepad_dpad(handle);
    }

    public static function buttonA(int handle) -> int
    {
        return gcmicrogamepad_button_a(handle);
    }

    public static function buttonX(int handle) -> int
    {
        return gcmicrogamepad_button_x(handle);
    }

    public static function buttonMenu(int handle) -> int
    {
        return gcmicrogamepad_button_menu(handle);
    }

    // ---- @reserved (not bound; see src/*.h) ----
    // @reserved @property (nonatomic, readonly, weak) GCController *controller — not bound in this slice
    // @reserved @property (nonatomic, copy, nullable) GCMicroGamepadValueChangedHandler valueChangedHandler — getter (block)
    // @reserved @property (nonatomic, copy, nullable) GCMicroGamepadValueChangedHandler valueChangedHandler — setter (block)
    // @reserved - (GCMicroGamepadSnapshot *)saveSnapshot API_DEPRECATED — deprecated
    // @reserved @property (nonatomic) BOOL reportsAbsoluteDpadValues — getter, not bound in this slice
    // @reserved @property (nonatomic) BOOL reportsAbsoluteDpadValues — setter, not bound in this slice
    // @reserved @property (nonatomic) BOOL allowsRotation — getter, not bound in this slice
    // @reserved @property (nonatomic) BOOL allowsRotation — setter, not bound in this slice
    // @reserved - (void)setStateFromMicroGamepad:(GCMicroGamepad *)microGamepad — snapshot write, not bound in this slice

}
