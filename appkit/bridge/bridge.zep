// GENERATED by scripts/gen-zep.php — DO NOT EDIT.
// Source of truth: the @zep/@zep-construct/@reserved annotations in src/*.h.

namespace AppKit\Bridge;

class Bridge
{
    public static function retain(int handle) -> bool
    {
        int r;

        let r = bridge_retain(handle);

        return r == 1;
    }

    public static function release(int handle) -> void
    {
        bridge_release(handle);
    }

    public static function isValid(int handle) -> bool
    {
        int r;

        let r = bridge_is_valid(handle);

        return r == 1;
    }

    public static function className(int handle) -> var
    {
        var result;

        let result = bridge_class_name(handle);

        return result;
    }

    public static function isKindOfClass(int handle, string className) -> bool
    {
        int r;

        let r = bridge_is_kind_of_class(handle, className);

        return r == 1;
    }

    public static function pump(double timeout) -> int
    {
        return bridge_pump(timeout);
    }

    public static function setAction(int handle, var callable_) -> bool
    {
        int r;

        let r = bridge_set_action(handle, callable_);

        return r == 1;
    }

    public static function removeAction(int handle) -> void
    {
        bridge_remove_action(handle);
    }

    public static function observeNotification(int object_, string name, var callable_) -> int
    {
        return bridge_observe(object_, name, callable_);
    }

    public static function removeObserver(int token) -> void
    {
        bridge_unobserve(token);
    }

    public static function delegateNew(string protocolName) -> int
    {
        return bridge_delegate_new(protocolName);
    }

    public static function delegateOn(int delegate, string selector, var callable_) -> bool
    {
        int r;

        let r = bridge_delegate_on(delegate, selector, callable_);

        return r == 1;
    }

    public static function delegateOff(int delegate, string selector) -> void
    {
        bridge_delegate_off(delegate, selector);
    }
}
